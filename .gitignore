node_modules/
dist/
.svelte-kit/
*.log
.DS_Store
.env
.env.*
.dev.vars
.dev.vars.*
.superpowers/

# vitest browser-mode artifacts
.vitest-attachments/
**/__screenshots__/

# temp Vite projects scaffolded by the vite-integration tests, rooted in the worktree so their
# nested Vite resolves @glw907/cairn-cms from the repo node_modules
.cairn-vite-test-*/

# machine-local Claude Code agent scratch memory (never a project deliverable)
**/.claude/agent-memory/
