# Facts: extend arm

Harvested 2026-09-15 from docs/extend/*, tightened the same day. Agent-facing; never shipped; not register-graded. Every fact carries a source. Format: one bullet per fact, then `Source:` then a status tag.

## docs/extend/add-a-custom-admin-screen.md

- `f:03zj56` A custom route under `src/routes/admin/` wins over the `[...path]` catch-all automatically because SvelteKit resolves the more specific route first. Source: SvelteKit routing precedence. [external: SvelteKit]
- `f:2sd4if` `createAuthGuard()` gates the whole `/admin` subtree before any load runs, but does not decide per-route authorization; that is `requireAccess`/`createSectionAction`'s job. Source: `src/lib/sveltekit/` guard plus `requireAccess`/`section-action.ts` split. [verified]
- `f:3j02dk` `createSectionAction<Env, Db>({ resolveDb })` builds a reusable per-section action wrapper; `resolveDb` reads the site's own `App.Platform['env']` binding. Source: `src/lib/sveltekit/section-action.ts`. [verified]
- `f:10ojk8` `requireAccess` in `load` and `createSectionAction` in every action share one fail-closed predicate: a session the access map does not admit is refused on both the read and the write. Source: `src/lib/sveltekit/section-action.ts` (rate-limit and access-map-not-attached branches reference the same guard). [verified]
- `f:3lbdl6` A role or session with no rule declared for a route's path gets a 403 from the access map, owner included, since `hasAccessRule` carries no owner exemption. Source: `src/lib/sveltekit/guard.ts#requireAccess` (`if (!hasAccessRule(access, resolvedTarget) || !canReach(...))`). [verified]
- `f:lblh3u` `createSectionAction` also runs the audit and authentication work `createAdminAction` does underneath it (editor identity, CSRF, one form read); a section built on `createSectionAction` never calls `createAdminAction` directly. Source: `src/lib/sveltekit/section-action.ts`. [verified]
- `f:7ik6ng` `@glw907/cairn-cms/admin-toolkit` exports `PageHeader`, `AdminTable`, `ListToolbar`, `ToolbarDisclosure`, `Pagination`, `StatusChip`, `EmptyState`, `FieldLabel`, `ExpandableRow`, and `MediaPicker` (plus the `format.js` helpers) as general-purpose scaffolding primitives. Source: `src/lib/admin-toolkit/index.ts` (full export list). [verified: page fixed at `docs/extend/add-a-custom-admin-screen.md:93-94` to name `ToolbarDisclosure` and `MediaPicker` too]
- `f:8x0qpa` `AdminTable` sets its own horizontal scroll; a wrapping `<div>` should carry `overflow-hidden card-shell card-shadow` but not `overflow-x-auto`. Source: `src/lib/admin-toolkit/AdminTable.svelte:189` (`.toolkit-admin-table-wrap { overflow-x: auto; }`), `card-shell`/`card-shadow` per `docs/internal/admin-design-system.md:354`. [verified]
- `f:mfa5vi` The admin-toolkit's charter is general-purpose primitives only; a component rendering one of cairn's own content concepts (a `ConceptList` row, an `EditPage` field) does not belong there. Source: `src/lib/admin-toolkit/index.ts` module comment ("each export's contract stays general-purpose, never a domain assumption from its first consumer"). [verified]
- `f:68h31z` `ctx.audit()` needs an `AuditSink` wired via `event.locals.cairnAuditSink`, set in `hooks.server.ts` from a bound D1 database (typically via `createD1AuditSink`). Source: `src/lib/ambient.ts:47` (`cairnAuditSink?: AdminActionAuditSink`), `src/lib/sveltekit/admin-action.ts:279` (`event.locals.cairnAuditSink?.(full)`), `src/lib/sveltekit/audit-sink.ts:92` (`createD1AuditSink`). [verified]
- `f:rurhey` `hooks.server.ts` holds exactly one `handle` export; multiple handles compose via `sequence()`, with `createAuthGuard` first since `wireAuditSink` only needs `resolve`. Source: SvelteKit `sequence` API plus page's own composition example. [candidate: excluded, this states the page's own worked hooks.server.ts example; no engine code asserts this ordering requirement]
- `f:ff3l1u` The `createD1AuditSink` sink is fail-open: it returns before the insert settles, so a persist failure never fails the action being audited; a rejected write logs rather than disappearing. Source: `src/lib/sveltekit/audit-sink.ts:66` (doc comment: "The sink is fail-open end to end: it returns synchronously, before the insert settles..."). [verified]
- `f:da6d2z` `createSectionAction` audits every refusal, not only a successful action, so an unrated action fills an audit table cheaply without a `rateLimit` option; the one exception is the rate-limit-exceeded refusal itself, which returns `fail(429)` with no `ctx.audit` call since back-pressure is not a domain-state change. Source: `src/lib/sveltekit/section-action.ts:16-17` (module comment) and its `ctx.audit(...)` calls at every other refusal branch (access-map-not-attached, no-access-rule, ownerOnly/canReach, db-not-bound). [verified]
- `f:9fwsz5` In the dialog-form recipe, `enhance`'s `'error'` result must skip `update()`, because `update()` calls `applyAction(result)`, which renders the nearest `+error` page for an `'error'` result and would destroy the dialog. Source: SvelteKit `enhance`/`applyAction` behavior. [external: SvelteKit]
- `f:cdllbv` `reset` (an `update()` option) is consulted only on a `'success'` result; SvelteKit never resets a `<form>`'s values after `'failure'`. Source: SvelteKit `enhance` behavior. [external: SvelteKit]
- `f:lmtfkt` A route nested under an already-gated `/admin` page inherits the auth guard (covers the whole subtree) but never inherits a page's own `requireAccess` rule, which is scoped to that page's route id only; a detail endpoint needs its own `requireAccess` call and its own access-map entry. Source: `src/lib/sveltekit/guard.ts:448-477` (`requireAccess`: `target` defaults to `event.route.id`, resolved per route; each route must call it explicitly, so a nested route's own rule is never inherited from a parent's call), consistent with `f:2sd4if`'s already-verified guard/requireAccess split. [verified]
- `f:g7zuji` If the root layout renders visible chrome around `/admin` (header, footer, width cap), the admin shell cannot fill the viewport; a dev-only ancestor-chain check (`detectChromeWrap`) catches this and logs one console error pointing at the route-structure doc, but only in dev and it never throws or changes rendering. Source: `src/lib/components/chrome-guard.ts` (`detectChromeWrap`, `warnIfChromeWrapped`). [verified]

## docs/extend/add-a-second-audience.md

- `f:4673n6` A role declared `{ capability: 'none', home: '/admin/classes' }` still authenticates through the same magic-link flow every editor uses; `none` capability means the engine's content and roster screens refuse this role. Source: `src/lib/auth/roles.ts:12` (`Capability = 'owner' | 'editor' | 'none'`), `src/lib/sveltekit/guard.ts#requireEditor` (`if (editor.capability === 'none') throw error(403, ...)`), `src/lib/auth/access.ts:157`. [verified]
- `f:kqcyv1` A `none`-capability role with no declared `home` lands on a minimal signed-in screen (`view: 'welcome'`, "no content access here") instead of `/admin`'s first concept. Source: `src/lib/sveltekit/content-routes-shell.ts:304-313` (`indexLoad`: `roleHome` absent and `capability === 'none'` returns `{ view: 'welcome', page: {...} }`). [verified]
- `f:hcjb3o` An instructor/staff-shaped second audience is still added and removed through the same invite screen as an editor once the role vocabulary is larger than the default owner/editor pair. Source: `src/lib/components/ManageEditors.svelte:38-55` (renders a labeled `<select>` over every entry in the declared vocabulary, not a hardcoded owner/editor pair). [verified]
- `f:we2lmp` `createAuthChannel` builds request, confirm, and logout actions over a numeric code, 8 digits by default, configurable from 8 to 10 digits. Source: `src/lib/auth-channel/factory.ts:473` (`codeLength: resolveLimit('code.length', code.length, 8, { min: 8, max: 10 })`). [verified]
- `f:rcpe0n` `createAuthChannel`'s backing D1 binding must never be `AUTH_DB`; it needs its own binding and its own `migrations_dir` distinct from the one the site's `AUTH_DB` uses. Source: `src/lib/auth-channel/factory.ts:265` (`AuthChannelConfig.resolveDb` doc comment: "The channel's own D1 binding, never `AUTH_DB` (spec, decision 1: physical separation)"), `migrations-channel/0000_channel.sql` (the channel's own migration path, distinct from `migrations/`). [verified]
- `f:41kl6q` `createAuthChannel` config requires `resolveDb`, `deliver`, `lookup`, `normalize`, `challenge`, and `cookie.name`. Source: `src/lib/auth-channel/factory.ts:264-296` (`AuthChannelConfig` interface). [verified]
- `f:ffskeq` `lookup` and the optional `verify` each take a `{ env }` context alongside their subject so a roster read can reach its own binding without the channel holding one; neither may read request-shaped data. Source: `src/lib/auth-channel/factory.ts#AuthChannelConfig.lookup` (`lookup: (contact, ctx: { env: Env | undefined })`) and `src/lib/auth-channel/factory.ts#AuthChannelConfig.verify` (`verify?: (subject, ctx: { env: Env | undefined })`). [verified]
- `f:pa2hqh` `normalize`, `lookup`, and `challenge` each carry a correctness obligation the factory cannot verify: a lossy `normalize` maps two people onto one identity's rate budget, and `challenge` is the whole economic bound on guessing a code. Source: `src/lib/auth-channel/factory.ts:283-296` (`AuthChannelConfig`'s `lookup`/`normalize`/`challenge` doc comments: "must be idempotent and canonical per identity, and injective across distinct people"; "the most load-bearing of the three correctness obligations this factory cannot itself verify"). [verified]
- `f:6ab2z6` The packaged channel migration ships at `node_modules/@glw907/cairn-cms/migrations-channel/0000_channel.sql`; every statement is idempotent (`CREATE TABLE IF NOT EXISTS`), so re-running it against an already-provisioned database changes nothing and records the `d1_migrations` marker. Source: `migrations-channel/0000_channel.sql` (module comment: "Every statement is idempotent"; `CREATE TABLE IF NOT EXISTS cairn_channel_meta`). [verified]
- `f:fcqs22` Testing a channel against a real D1-shaped double uses `@glw907/cairn-cms-dev`'s `createChannelDb`, which needs `node:sqlite`; that package's `engines.node` field enforces the floor at install time. Source: `packages/cairn-cms-dev/package.json:12` (`engines`), `packages/cairn-cms-dev/src/channel-db.ts:39-41` (comment: "this package's own `engines.node` (`>=24`)"). [verified]

## docs/extend/add-an-island.md

- `f:nm3bbo` An island is declared with `hydrate: true` (mount eagerly, on first load and after every client navigation) or `hydrate: 'visible'` (defer to first intersection). Source: `src/lib/render/registry.ts:74` (`hydrate?: boolean | 'visible'`) and `src/lib/islands/index.ts` (`observeIsland`/`mountIsland` dispatch on `data-cairn-hydrate === 'visible'`). [verified]
- `f:1uk9sw` `build()` still runs for a hydrating component; its output is the fallback shown before JS runs and the only content a no-JS reader ever sees. Source: `src/lib/render/registry.ts` `ComponentDef.build` contract (component context doc). [verified]
- `f:jz4qka` Every island prop is untrusted: the pipeline HTML-attribute-escapes on emit, and the client `JSON.parse`s inside a `try`/`catch`, safe against breakout only because the value never enters a script context. Source: `src/lib/islands/index.ts` `mountIsland` (`JSON.parse` wrapped in `try`/`catch`, returns on parse failure). [verified]
- `f:thjrvl` Islands are registered per directive name on the adapter's `rendering.islands`, alongside `rendering.render`. Source: `src/lib/islands/types.ts` `IslandRegistry` (keyed by directive name) plus page's adapter example. [verified]
- `f:mv684d` `defineAdapter` fails closed at declaration if a `hydrate` component has no matching `islands` entry, or vice versa, naming the mismatched directive. Source: `src/lib/content/adapter.ts:10-25` (`assertIslandsConsistent`, called from `defineAdapter`). [verified]
- `f:rmlwca` `hydrateIslands(islands, root = document)` tears down the previous pass before mounting again, so re-navigating to a page with an island mounts exactly one instance, never a stack of duplicates. Source: `src/lib/islands/index.ts` (`teardown()` called at top of `hydrateIslands`). [verified]
- `f:urw3z7` One bad island (unregistered directive name, a prop that fails to parse, a component that throws on mount) is isolated: the static fallback stays in place for that boundary, and every other island still mounts. Source: `src/lib/islands/index.ts` `mountIsland` (per-node try/catch restoring `fallback` on mount failure; unknown directive name skipped via `if (!Comp) continue`). [verified]
- `f:w8oe1v` `unmount` runs with `outro: false` so teardown is synchronous and deterministic on navigation. Source: `src/lib/islands/index.ts` `teardown()` (`unmount(instance, { outro: false })`). [verified]
- `f:59u3b9` An empty islands registry keeps the `@glw907/cairn-cms/islands` dynamic import out of a static site's bundle entirely. Source: page's root-layout example (`if (!islands || Object.keys(islands).length === 0) return;` before dynamic import) plus `islands/index.ts` module comment ("A site imports this dynamically, gated on a non-empty registry, so a static site never ships it"). [verified]
- `f:8o2gbl` The edit page's preview frame is sandboxed and never runs scripts, so it always shows the static fallback, never the mounted island component; verify a live island on the deployed page or `npm run preview`. Source: `src/lib/components/EditPage.svelte:2128` (`<iframe sandbox="" ... srcdoc={previewDoc}>`). [verified]

## docs/extend/add-cairn-to-a-sveltekit-app.md

- `f:5f4kmk` cairn commits through a GitHub App, never a personal access token; the commit author is always the signed-in editor and the App's own identity, `cairn-cms[bot]`, is the committer of record. Source: `src/lib/github/types.ts:20` ("A commit author: the signed-in editor... The committer is left to the App."), `src/lib/github/repo.ts:262` ("committer is omitted, so GitHub attributes the commit to the App"); the App's literal identity `cairn-cms[bot]` is GitHub's own App-naming convention, external to this repo. [verified]
- `f:vgw8x9` The GitHub App needs exactly one repository permission, Contents: Read and write, with Webhook cleared (Active unchecked) since cairn never receives a webhook. Source: GitHub App setup UI. [external: GitHub]
- `f:gglwt4` The Contents: Read and write permission is repository-wide; GitHub's installation token can write to any path in the installed repository, and cairn's confinement to its declared content directories is enforced by engine code, not by GitHub. Source: `src/lib/github/repo.ts:265-268` (comment: "Caller preconditions this layer cannot enforce (the save and lifecycle paths must): every `path` is confined to the site's content directories (the App token can write anywhere in the repo)"). [verified]
- `f:vpieos` The Worker secret `GITHUB_APP_PRIVATE_KEY_B64` must be the PEM contents base64-encoded onto a single line; the engine decodes it with `atob()` before signing, so a multi-line encoding will not parse. Source: `src/lib/env.ts:31` ("The GitHub App's private key, base64 of the PEM on one line, decoded with `atob()` before signing"), `src/lib/github/signing.ts:44,67,132` (`atob(creds.privateKeyB64)`). [verified]
- `f:if45on` The self-owned auth D1 database is created with `npx wrangler d1 create <name>` and bound as `AUTH_DB`, with `migrations_dir` pointing at a copied-in `migrations/` directory. Source: `src/lib/env.ts:75-79` (`requireDb`, throws `config.bindings-missing` unless the site wires the `AUTH_DB` binding); the `npx wrangler d1 create`/`migrations_dir` setup steps are Cloudflare Wrangler CLI mechanics, external to engine code. [verified]
- `f:gffvfd` The real count, settled against `migrations/`: five migration files (`0000_auth.sql`, `0001_roles.sql`, `0002_audit.sql`, `0003_preview.sql`, `0004_login_nonce.sql`) producing five tables. `0000_auth.sql` creates `editor`, `magic_token`, `session`; `0002_audit.sql` creates `audit_log`; `0003_preview.sql` creates `preview_tokens`. `0001_roles.sql` and `0004_login_nonce.sql` are ALTER-shaped migrations (rebuild `editor` to drop its role `CHECK`; add a `nonce_hash` column to `magic_token`) and create no table of their own. `add-cairn-to-a-sveltekit-app.md`'s "five migrations" is accurate; `data-tiers.md`'s "five tables across four migrations" undercounts the migration files by one (there are five files, not four; only three of them run a `CREATE TABLE`). Source: `migrations/0000_auth.sql`, `migrations/0001_roles.sql`, `migrations/0002_audit.sql`, `migrations/0003_preview.sql`, `migrations/0004_login_nonce.sql`. [verified: page fixed at `docs/extend/data-tiers.md:29` to say "five migrations"]
- `f:rn62i1` `0001_roles.sql` is opt-in, needed only if a site declares a role vocabulary beyond the default owner/editor pair; `0003_preview.sql` is opt-in, needed only for draft-preview links. Source: `migrations/0000_auth.sql` (original `role TEXT NOT NULL CHECK (role IN ('owner', 'editor'))`, already cited by `f:xkkt1o`), `migrations/0001_roles.sql` (lifts that CHECK, needed only for a role vocabulary beyond the default pair), `migrations/0003_preview.sql:1-2` ("Opt-in: only a site that mints preview links... ever touches this table"). [verified]
- `f:pkrwom` `0002_audit.sql` is a standalone `audit_log` table recommended on its own dedicated D1 binding (commonly `AUDIT_DB`) with its own `migrations_dir`, distinct from the auth database's, so heavy audit writes never contend with session and token lookups. Source: page text; corroborated in `data-tiers.md`. [candidate: excluded, the opt-in half traces to `migrations/0002_audit.sql`'s own header, already covered by `f:wnvqlz`; the "commonly AUDIT_DB" naming convention and the write-contention rationale have no corresponding constant or comment anywhere in the engine]
- `f:xhwl32` The Email Sending binding (`send_email: [{ name: 'EMAIL' }]`) needs the domain zone in `PUBLIC_ORIGIN` onboarded via `npx wrangler email sending enable <domain>`; a `workers.dev` subdomain has no zone to onboard. Source: Cloudflare Email Sending / wrangler CLI. [external: Cloudflare]
- `f:t4pwpw` Sending to a second person requires Cloudflare's Workers Paid plan ($5/month); sending to yourself is free on any plan. Source: Cloudflare Workers pricing. [external: Cloudflare]
- `f:zcwf5i` The App ID and Installation ID are not secrets; the engine treats them as plain identity, never as credentials to sign with, and they pass directly into `createGithubApp(...)`. Source: `src/lib/github/backend.ts:87,104` (`GithubAppProvider`: "the GitHub App's non-secret identity facts"; `GithubAppConfig`: "The non-secret GitHub App identity an adapter carries in source; the private key stays a Worker secret"). [verified]
- `f:pg2smj` The retired JS doctor's `github.app` check read its GitHub credentials from the local shell environment (`GITHUB_APP_ID`, `GITHUB_APP_INSTALLATION_ID`, `GITHUB_APP_PRIVATE_KEY_B64`), not from the pushed Worker secret, and reported a skip when not all three were set. The check is dropped in the doctor retirement, with no doctor-time substitute (`cairn doctor`'s eleven checks carry no `github.app` entry); the runtime equivalent is the `github.app-unreachable` condition, raised when the Worker's own `GITHUB_APP_PRIVATE_KEY_B64` secret is unset. Source: `src/lib/github/credentials.ts:9-22` (`appCredentials`, throws `CairnError('github.app-unreachable', ...)` when `env.GITHUB_APP_PRIVATE_KEY_B64` is unset) and `docs/reference/cli-cairn-doctor.md` (the checks table). [verified: the runtime `github.app-unreachable` half traces to `credentials.ts`; the retired doctor's credential-reading and skip behavior is historical, its source removed with the rest of `src/lib/doctor`]
- `f:m0ouh8` The page's "Verify" step, after wiring bindings by hand, runs `cairn doctor` (no flags, no credential) from the site's directory; its `config.bindings` check confirms `AUTH_DB` and `EMAIL` are both wired, replacing the retired npm bin's `--from`/`--repo` invocation. No command checks the GitHub App itself: that still needs a manual publish and a check that the resulting commit is authored by `cairn-cms[bot]`. Source: `tool/internal/doctor/check_bindings.go:17-28` (`config.bindings` check confirms `EMAIL` and `AUTH_DB` are declared); no `github.app`, D1, or auth-store check exists among the eleven `tool/internal/doctor/check_*.go` files. [verified]

## docs/extend/announce-on-publish.md

- `f:ix641z` `buildNewlyPublished(before, after)` returns entries where `draft` is false, `publishedAt` is set, and no prior entry at the same `concept`/`id` key carried a stamp. Source: `src/lib/delivery/manifest.ts:56-65` (`buildNewlyPublished`: `if (e.draft) return false; if (!e.publishedAt) return false; return !priorStamps.get(keyOf(e));`). [verified]
- `f:pm1osl` The draft check runs before the stamp check, so a currently-unpublished entry never matches even if it carries a stamp forward from an earlier publish. Source: `src/lib/delivery/manifest.ts:61-63` (order of the two `if` guards). [verified]
- `f:6cas58` `before: null` means no prior manifest, and `buildNewlyPublished` treats every stamped, non-draft entry in `after` as newly published (a full fan-out), matching the code comment: "A null `before` indexes to nothing... every currently-live stamped entry in `after` reads as newly published." Source: `src/lib/delivery/manifest.ts:57-59` (comment plus `priorStamps` built from `before?.entries ?? []`). [verified]
- `f:pc3iy5` `buildNewlyPublished` is pure: no I/O, no clock read, deterministic given both manifests. Source: `src/lib/delivery/manifest.ts:56-65` (function body reads only its two arguments, no side effects). [verified]
- `f:df3hqw` The entry identity key is `concept`/`id` together (`keyOf(e) = \`${e.concept}/${e.id}\``); renaming an entry changes that key, so a rename reads as a new publish since the old key's row disappears and the new key has no stamped counterpart. Source: `src/lib/delivery/manifest.ts:36` (`keyOf`). [verified]
- `f:w3m0ac` Deleting a published entry and recreating one under the same id also reads as a new publish, since the deleted row (stamp included) drops out of the prior manifest with no successor to carry the stamp forward. Source: `src/lib/delivery/manifest.ts:56-65` (`buildNewlyPublished`: `priorStamps` is keyed only by `keyOf(e)` from `before`; a key absent from `before`, whether never-published or deleted-then-recreated, reads identically as newly published, with no special case for either), consistent with `f:pm1osl`/`f:ix641z`. [verified]
- `f:xlj297` The engine sends nothing over the network on its own for publish announcements; persisting the prior manifest across deploys (KV, R2, or other storage) is the site's own responsibility. Source: `src/lib/delivery/manifest.ts:47-53` ("A consumer wiring announce-on-publish persists the prior manifest across deploys..."; "The engine performs no network sends; a consumer diffs and then acts."). [verified]

## docs/extend/architecture.md

- `f:a7qx4m` The engine's export map splits into six functional groups: core/adapter (root barrel), the SvelteKit layer (`/sveltekit`), admin UI (`/components`, `/admin-toolkit`, `/islands`), rendering (`/render`), delivery (`/delivery`, `/media`), and auth/platform (`/auth-store`, `/auth-channel`, `/auth-crypto`, `/cloudflare`, `/vite`, `/ambient`). Source: `package.json:88-179` (`exports` map: root `.`, `/sveltekit`, `/components`, `/admin-toolkit`, `/islands`, `/render`, `/delivery`, `/media`, `/auth-store`, `/auth-channel`, `/auth-crypto`, `/cloudflare`, `/vite`, `/ambient`). [verified]
- `f:0duu5p` Nothing on the root barrel imports SvelteKit, and nothing on `/sveltekit` is a `.svelte` file; a Svelte admin component lives on `/components`. Source: `src/lib/*.ts` (no `@sveltejs/kit` or `svelte` imports; `grep` finds none), `src/lib/sveltekit/*.svelte` (no matches). [verified]
- `f:4esdoz` A site declares a single `CairnAdapter` object (typically `src/theme/cairn.config.ts`); every route factory, admin screen, and delivery helper reads its behavior from that one object, and the engine never hard-codes a concept, directory, or field. Source: `src/lib/content/adapter.ts:26-29` (`defineAdapter`, one `CairnAdapter` object), `src/lib/content/types.ts:226-274` (`CairnAdapter`, the single seam the engine consumes). [verified: structurally against the single-adapter-object shape every module reads from; "never hard-codes" is the adapter pattern's design consequence, not one asserted line]
- `f:cjonmm` The write path: an editor's save commits to a per-entry holding branch named `cairn/<concept>/<id>`, with the editor as commit author and `cairn-cms[bot]` as committer; a separate Publish action copies that branch's content onto the default branch, which triggers the site's existing deploy. Source: `src/lib/content/pending.ts:1-11` (`pendingBranch`: "a pending entry lives on `cairn/<conceptKey>/<id>`"), `src/lib/sveltekit/content-routes-entry-write.ts:1-5` (module comment: "saveAction, publishAction... commits the entry file to its pending branch; save stops there, publish reuses the same held"), `src/lib/github/types.ts:20` (author/committer split). [verified]
- `f:cng7dr` The read path has two modes: a request needing one entry's full body (public page render, edit-form load) reads that file directly through the Backend's content API; a request needing corpus-wide facts (index, tag list, sitemap, link-integrity check) reads the committed content manifest instead. Source: `src/lib/sveltekit/content-routes-entry-read.ts:1-4` (module comment: `editLoad`/`historyLoad` "assemble one entry's admin view from a concurrent batch of GitHub reads"), `src/lib/content/manifest.ts:473` (`inboundIncludes`, a corpus-wide manifest read). [verified]
- `f:e69d0l` The content manifest is rebuilt at build time and patched in the same commit as a publish, not the intermediate save to the pending branch, specifically so the admin and delivery layer never crawl the whole repository through the GitHub API to answer "what links here" or "what's tagged this." Corrected against the code: a save commits no manifest change; the manifest patch lands only when publish copies the entry onto main. Source: `src/lib/sveltekit/content-routes-entry-write.ts:341-352` ("a save commits no manifest, so the moment an entry goes live is this [publish] commit"; `publishAction`'s `changes` array carries both the entry file and `formatManifest(manifest)`). [verified: corrects the page's "same commit as a save" framing, not amended on the page itself this pass]
- `f:n3cvf9` The directive-stamping and dispatch machinery inside the render pipeline, the exact commit tree-operation shape against the GitHub API, and the guard's internal CSRF/session resolution are engine-internal, not seams a site reaches into; each is instead covered by a stability-tiered public surface (`/render`, `/sveltekit`, `/auth-crypto`). Source: `package.json` `exports` map (the `/render` subpath resolves to `dist/render/authoring.js`, a curated re-export, not the full `src/lib/render/*` file set which includes `registry.ts`, `rehype-dispatch.ts`, etc.). [verified]
- `f:i87sd3` Six named seams a site extends through: the adapter's `content` map (`ConceptConfig`/`fieldset`), `render` (built with `createRenderer`), the access map and role vocabulary (`defineAccess`, `defineRoles`), `navLayout`, the Backend interface (`createGithubApp` the only shipped implementation), and `AssetConfig` (media). Source: `src/lib/content/types.ts:226-274` (`CairnAdapter`: `content`, `rendering`, `access`/`roles`, `media` fields), `src/lib/sveltekit/admin-nav.ts:300` (`navLayout` under `editor`), `src/lib/github/backend.ts:78,162` (`BackendProvider`, `createGithubApp` the one shipped implementation). [verified]

## docs/extend/auth-channel-security-model.md

- `f:pjjo64` The governing rule: no control keyed on the victim's identity may deny, delay, or destroy anything; denial keys only on the requester, and an identity-keyed control either escalates through a site-actionable channel or only logs. Source: `src/lib/auth-channel/factory.ts:10` (module comment: "identity-keyed control either escalates through `challenge-required` or only logs"), instantiated by the eviction, ceiling, and escalation facts below. [verified]
- `f:7idpoq` Escalation carries the wire result `challenge-required`, returned without a hard failure; a failed challenge on an already-escalated action returns `challenge-required` again, never a hard error. Source: `src/lib/auth-channel/factory.ts:62` ("`challenge-required` is a retry invitation, never a hard failure"), `:702`, `:976` (both `return { outcome: 'challenge-required' }`). [verified]
- `f:k6u4g2` Anything that evicts stored rows keys eviction on the requester's own bucket, so a caller can only crowd out its own pending rows, never another person's. Source: `src/lib/auth-channel/store.ts:232-239` (comment: "Filtered on `requester_bucket`, never `identity`, so an evictor..."; `WHERE requester_bucket = ?1`). [verified]
- `f:e0imm6` The anti-abuse spend ceiling does not deny; crossing it logs at error to alert the site operator rather than refusing a real member's login. Source: `src/lib/auth-channel/factory.ts:337` ("logs `auth.channel.ceiling_exceeded` only, never denies"), `:779` (`log.error('auth.channel.ceiling_exceeded', ...)`). [verified]
- `f:wu8x70` Every action (`request`, `confirm`, `logout`) asserts the request's `Origin` matches the site's own origin and that the connection is `https`, except on a local development host, before any code, budget, or session logic runs; both checks throw a plain 403 with no wire result. Source: `src/lib/auth-channel/factory.ts:70-78` (`assertOriginAndScheme`: `@throws HttpError 403 on an origin mismatch, or on plain http anywhere but a local host`), called at `:689` ahead of the rest of `request`. [verified]
- `f:fslodf` Identity correlates via a salted, prefixed hash: `hashToken(salt + 's:' + subject)` when a roster lookup resolved a stable subject, or `hashToken(salt + 'c:' + contact)` otherwise; logs correlate on the first 16 hex characters of this hash (`correlationId`), never on the raw contact. Source: `src/lib/auth-channel/identity.ts:6-14` (`deriveIdentity`), `src/lib/auth-channel/factory.ts#createAuthChannel.requestAction` (`correlationId = identity.slice(0, 16)`). [verified]
- `f:plcf5v` A numeric confirmation code is drawn through rejection sampling over a Web Crypto random byte range, not naive modulo, to avoid the low-end bias a non-power-of-range naive modulo introduces. Source: `src/lib/auth-channel/identity.ts:16-34` (`drawBelow`: draws bytes via `crypto.getRandomValues`, rejects draws at or above the largest multiple of the range that fits). [verified]
- `f:1rstld` A confirmed code mints a channel session resolved the same hashed-lookup way the built-in admin session is; `revokeSessions` lets site code end every session for one identity at once (roster removal, reported compromise), the one deliberate identity-keyed destructive capability since it is invoked by the site's own trusted code, not a public caller. Source: `src/lib/auth-channel/factory.ts:388,1163` (`revokeSessions(db, subject)`). [verified]
- `f:wuwk2q` A dev transport that prints a channel's code to console instead of sending it creates a roster oracle by construction: an unauthenticated caller can learn whether an arbitrary contact is on the roster with no code-guessing, since delivery only ever runs for a known subject; the same transport also lands plaintext one-time codes in Workers Logs if run in a deployed Worker with observability on. Source: `src/lib/auth-channel/factory.ts:867-868` (comment: "Step 8: deliver, only for a known subject"), `:855-864` (the `subject === null` branch returns without calling `config.deliver`). No such dev transport ships in engine code; this is a hazard warning about a pattern a site's own `deliver` implementation could introduce. [verified]
- `f:ez788q` `normalize` must be idempotent, canonical per identity, and injective across distinct people; `lookup`'s subject must be stable and canonical per person; `challenge` must actually verify a human (the factory cannot distinguish a real Turnstile call from `async () => true`). Source: `src/lib/auth-channel/factory.ts:283-296` (same doc comments already cited for `f:pa2hqh`). [verified]

## docs/extend/build-a-site-by-hand.md

- `f:fx0jbj` The five-milestone path: (1) bare SvelteKit site deployed to `workers.dev`, (2) cairn admin against a local in-memory dev backend, (3) public site rendering locally, (4) whole site deployed with the dev backend stripped from production, (5) real GitHub App and auth database, redeployed. Source: page's own mermaid milestone diagram and section headers. [candidate: excluded, describes the doc page's own tutorial structure (its milestone diagram and section headers), not a code-level fact]
- `f:75hawi` Node 24 or later, a GitHub account, and a Cloudflare account are required; everything through Milestone 4 runs on Cloudflare's free tier, and Milestone 5 needs a domain zone. Source: `package.json:7` (`"engines": { "node": ">=24" }`) for the Node floor; the Cloudflare account/tier/domain-zone requirements are Cloudflare's own product terms. [external: Cloudflare]
- `f:yegr67` `typescript` must stay on major version 6 for now: `svelte-check` cannot run on TypeScript 7 until the 7.1 compiler API lands; `npx sv create` already pins the correct major, and cairn's own code and shipped types are 7-clean. Source: `package.json:283` (`"typescript": "^6.0.3"`). [verified: the major-6 pin traces to package.json:283; the svelte-check and TypeScript 7 rationale, the `sv create` pin, and the 7-clean claim are not traced to code]
- `f:ifuvcl` A current `sv create` scaffold has no `svelte.config.js` file; SvelteKit's config (including the adapter) lives inline in the `sveltekit()` plugin call inside `vite.config.ts`. Source: `sv` CLI / SvelteKit tooling. [external: SvelteKit]
- `f:mhsere` `adapter-auto` picks an adapter at build time by guessing the deploy target; a self-deployed site should name `@sveltejs/adapter-cloudflare` explicitly instead. Source: SvelteKit adapter resolution. [external: SvelteKit]
- `f:q13lck` `wrangler.jsonc`'s minimal Cloudflare adapter config needs `main: ".svelte-kit/cloudflare/_worker.js"` and an `assets` block pointing at `.svelte-kit/cloudflare` bound as `ASSETS`. Source: `templates/waymark/wrangler.jsonc:6-9` (`"main": ".svelte-kit/cloudflare/_worker.js"`, `"assets": { "directory": ".svelte-kit/cloudflare" }`). [verified]
- `f:9mx680` `@glw907/cairn-cms-dev` stands in for the GitHub commit pipeline and the magic-link sign-in loop with in-memory fakes, gated behind a three-layer fence: a build-time flag that strips it from a production bundle, a `devDependency` boundary, and a runtime tripwire the guard enforces; it must never reach a deployed site. Source: `src/lib/dev-flag.ts:1-30` (`isDevBackendFlagSet`, the runtime tripwire `guard.ts` and `auth-channel/factory.ts` both read), `templates/waymark/package.json` (`@glw907/cairn-cms-dev` listed only under `devDependencies`), the already-external `__CAIRN_DEV_BUILD__` Vite `define` fold (`f:72mctx`). [verified]
- `f:vvgpr5` `src/app.d.ts` must `import '@glw907/cairn-cms/ambient'` to augment `App.Locals` with the five fields the engine reads and writes on every admin request (corrected from four: the module declares `cairnEditor`, `cairnBackend`, `cairnAuditSink`, `cairnAccess`, and `cairnIdentity`); a custom route reading `event.platform.env` needs `App.Platform` declared separately. Source: `src/lib/ambient.ts:41-49` (`App.Locals` augmentation, five members). [verified]
- `f:pdgkex` `createSiteIndexes` throws at build time for any declared concept with no matching `import.meta.glob` passed in, because Vite needs the literal glob pattern at the call site and cannot have one added programmatically. Source: `src/lib/delivery/site-indexes.ts:54-58` (throws `cairn: concept "${descriptor.id}" has no glob passed...` for any declared concept missing from the globs record). [verified]
- `f:n0laoh` The `cairnManifest` Vite plugin has two distinct failure modes, confirmed by reading the plugin directly. (1) It fails the build if the committed manifest has drifted from the markdown files on disk: in verify mode the plugin's virtual module runs `verifyManifest(built, committed)` inside `buildStart`, and `verifyManifest` throws when the built and committed manifests differ (after a `mediaRefs`/`references` migration-tolerant normalization). This throw fails the build as a hard error. (2) A concept declared in the adapter but missing from the plugin's own `content` option produces zero rows for that concept, with no throw: `buildSiteManifest` reads `globRecord[descriptor.id] ?? {}`, so a missing key falls back to an empty glob record and the concept's loop simply pushes no entries; nothing in the call path throws or warns. Both build-a-site-by-hand.md's drift-fails-the-build claim and declare-your-own-concept.md's missing-concept-produces-zero-rows claim are accurate as stated. Source: `src/lib/vite/internal.ts:53-80` (`virtualSource`, verify-mode `resultExpr`), `src/lib/content/manifest.ts:308-333` (`verifyManifest`), `src/lib/delivery/manifest.ts:18-33` (`buildSiteManifest`: `const record = globRecord[descriptor.id] ?? {};`). [verified]
- `f:6ebew3` Every concept defaults to the permalink pattern `/<id>/:slug`, except `pages`, which defaults to `/:slug`; the default `datePrefix` is `day`, stripping the entry filename's date stem from the slug. Source: `src/lib/content/concepts.ts:65-68` (`defaultPermalink`: `id === 'pages' ? '/:slug' : '/${id}/:slug'`), `:189` (`datePrefix: policy.datePrefix ?? 'day'`). [verified]
- `f:c8efq5` `ssr: { noExternal: ['@glw907/cairn-cms'] }` is required in `vite.config.ts` because the package ships its `.svelte` files straight from source under its `svelte` export condition, so Vite must run them through the site's own Svelte plugin rather than treating the package as pre-built; omitting this line breaks the admin components' build. Source: `package.json:86-90` (`exports["."]`: `"svelte": "./dist/index.js"`, alongside the durable gotcha that shipped dist `.svelte` files still carry `lang="ts"` and need the consumer's own Svelte plugin). [verified]
- `f:72mctx` `__CAIRN_DEV_BUILD__` must be named directly at every call site, never routed through a shared exported constant, because Vite's `define` substitution folds a literal only within the module that names it directly; a constant exported from one module and imported into another survives the fold and ships the dev-backend import in the deployed Worker. Source: Vite `define` substitution mechanics (string-literal replacement, not cross-module constant propagation). [external: Vite]
- `f:f2vudv` `bootstrapOwner` in `createCairnAdmin`'s `auth` config does nothing while the dev backend is active (which signs in without touching the auth store), but is the mechanism that creates the first real owner row automatically on first real sign-in, logged as `editor.bootstrapped`. Source: `src/lib/sveltekit/auth-routes.ts:44,201-203` (`bootstrapOwner`, `insertOwnerIfEmpty`, `log.info('editor.bootstrapped', ...)`). [verified]
- `f:d2jumm` `checkOrigin` is deprecated as of SvelteKit 2.61 in favor of `csrf.trustedOrigins`, but stays supported across cairn's tested range; cairn's admin guard needs double-submit CSRF, so kit's own origin check must be disabled (`csrf: { checkOrigin: false }`) to avoid fighting it. Source: `src/lib/sveltekit/csrf.ts:1` ("cairn owns CSRF for the admin once a site disables SvelteKit's global checkOrigin"), `src/lib/diagnostics/conditions.ts:103-111` (the doctor condition requiring `csrf: { checkOrigin: false }` plus a wired guard), `src/lib/sveltekit/guard.ts:204` ("they set checkOrigin: false to hand cairn the admin CSRF authority"). [verified]
- `f:7bch04` Milestone 5 changes exactly three files (`cairn.config.ts`'s `backend`/`email`, `wrangler.jsonc`'s bindings, `src/lib/content.ts`'s `ORIGIN` constant) and leaves `hooks.server.ts` untouched, since `__CAIRN_DEV_BUILD__` folds to `false` on a production build and the dev-backend import branch never runs. Source: page's own "3 files change and a fourth stops mattering" section. [candidate: excluded, describes the tutorial page's own "3 files change" milestone summary, not a code-level invariant]

## docs/extend/choose-an-ai-posture.md

- `f:rh0xv5` Setting `aiPosture` on the adapter alone changes no output bytes; `robotsResponse` reads its posture from its own `posture` option, which the site's `robots.txt` route must pass through explicitly from `cairn.aiPosture`. Source: `src/lib/delivery/robots.ts` (posture is a function parameter `opts.posture`, not read from any adapter object internally) plus page's own worked route example. [verified: against robots.ts's function signature reading `opts.posture`]
- `f:2edxez` Leaving `aiPosture` unset emits a `robots.txt` byte-identical to a site with no opinion (every cairn site's behavior before the field existed). Source: `src/lib/delivery/robots.ts:37-38` (both `Content-Signal` lines are conditional on `posture === 'decline'` or `'invite'`; no posture emits neither). [verified]
- `f:vmphgq` `'decline'` emits a `Content-Signal: ai-train=no` line plus a `User-agent`/`Disallow: /` pair for each token in the engine's own training-crawler table (`AI_CRAWLERS`). Source: `src/lib/delivery/robots.ts:23,37` (`CONTENT_SIGNAL.decline = 'ai-train=no'`; comment "`User-agent`/`Disallow` group per {@link AI_CRAWLERS} token plus a `Content-Signal` line declining training"). [verified]
- `f:qgtfu6` `'invite'` emits `Content-Signal: search=yes, ai-train=yes` and adds no crawler-specific `Disallow` lines, since no `robots.txt` directive grants access. Source: `src/lib/delivery/robots.ts#CONTENT_SIGNAL` (`CONTENT_SIGNAL.invite = 'search=yes, ai-train=yes'`) and `src/lib/delivery/robots.ts#buildRobots`. [verified]
- `f:ibespi` The `Content-Signal` directive syntax follows Cloudflare's published Content Signals Policy: directive `Content-Signal`, keys `search`/`ai-input`/`ai-train`, values `yes`/`no`, pairs separated by comma-space; an absent key expresses no preference. Source: `src/lib/delivery/robots.ts:4-7` (module comment citing the Cloudflare blog post). [verified]
- `f:duev87` The general-purpose `disallow` path list (e.g. `/admin`) is a separate mechanism from `aiPosture`; it always emits under the blanket `User-agent: *` group ahead of any posture-specific group, and has no way to target one named crawler. Source: page text; consistent with `robots.ts`'s separate `disallow` option existing alongside `posture`. [verified: structurally, exact group ordering not line-verified]
- `f:po5wx7` There is no per-crawler override or side door to add a training-crawler token beyond the fixed table `posture: 'decline'` iterates; cairn ships no crawler token it lacks first-party documentation for. Source: `src/lib/delivery/robots.ts:35` (`buildRobots(opts: { sitemapUrl, disallow?, posture? })`, no per-crawler parameter), `:42` (`for (const crawler of AI_CRAWLERS) ...`). [verified]

## docs/extend/configure-rendering.md

- `f:3l7f56` Every adapter declares exactly one `rendering.render` function (`SiteRender`); the edit page's live preview and every public page call the identical function, so there is no second render path to keep in sync. Source: `src/lib/content/types.ts:255,391` (`render: SiteRender`, one required field), `src/lib/components/EditPage.svelte:88-89,114` (`render?: SiteRender` prop: "The site's design-accurate render pipeline; the preview pane renders its output"), `src/lib/delivery/public-routes.ts:184` (public routes call the same `render(...)`). [verified]
- `f:dmn2uu` `createRenderer(registry)` returns `renderMarkdown` (HTML string) and `renderDocument` (HTML plus collected headings in document order, `{ id, text, depth }`); an empty registry still renders plain markdown, GFM tables, `cairn:` links, and `media:` references correctly. Source: `src/lib/render/pipeline.ts:86-105` (`createRenderer`, returns `renderMarkdown`/`renderDocument`; `registry: ComponentRegistry = defineRegistry({ components: [] })` defaults to empty). [verified]
- `f:s6n9zw` `defineComponent`'s `build` function returns a hast `Element` synchronously, runs inline inside the pipeline's tree transform once per occurrence, and cannot `await` anything; data a component needs must be fetched outside the render pipeline and passed through `attributes`. Source: `src/lib/render/registry.ts` `ComponentDef.build` (typed as returning an `Element`, not a `Promise`). [verified: against registry.ts type shape]
- `f:cxhx8w` `attributes` accepts exactly ten `fields.*` descriptor types that serialize to a single directive-attribute string: `text`, `textarea`, `number`, `select`, `url`, `email`, `date`, `datetime`, `boolean`, `icon`; everything else (`multiselect`, `object`, `array`, `reference`, `image`) throws at declaration. Source: `src/lib/render/registry.ts:245` (error message: "a directive attribute must be a single-value scalar (text, textarea, number, select, url, email, date, datetime, boolean, or icon)"). [verified]
- `f:34wt4f` A component's inner CSS classes should be named apart from any DaisyUI component class name (`card-body`, `card-title`, etc.), because DaisyUI's plugin generates CSS for any matching class name found anywhere in scanned source, not only markup a component-typed element wraps; a directive reusing a DaisyUI class name gets silently restyled if the site enables that component elsewhere. Source: DaisyUI's Tailwind content-scan mechanism. [external: DaisyUI]
- `f:wvcw71` `RendererConfig` (`createRenderer`'s second argument) exposes `sanitizeSchema` (extends the sanitize allowlist), `tableScroll` (controls the scrollable table wrapper), and `remarkPlugins`/`rehypePlugins` (composed after cairn's own pipeline steps, before stringification). Source: `src/lib/render/pipeline.ts:48,68,74,81` (`sanitizeSchema?`, `tableScroll?`, `remarkPlugins?`, `rehypePlugins?`). [verified]

## docs/extend/content-model.md

- `f:phdgof` A concept's fieldset is the single source of truth for the editor's form, the server-side validator, and the inferred TypeScript type a site reads its own entries as; there is no separate schema language and no second place a field is declared. Source: `src/lib/content/fields.ts` (fifteen `fields.*` builders feeding `defineFieldset`) plus page text. [verified: structurally against fields.ts export shape]
- `f:2hxm4t` The fifteen field builders are: text, textarea, number, select, multiselect, url, email, date, datetime, boolean, icon, image, object, array, reference. Source: `src/lib/content/fields.ts:145-180` (`export const fields = { text, textarea, ..., icon, image, object, reference, array, ... }`, matching page's own enumerated list). [verified]
- `f:68fh1k` `object` and `array` are the two container builders and both cap at one level of nesting: sub-fields/item shapes must all be leaves (no `object`/`array` inside a container); a `reference` field inside an `object` also throws, since a reference inside an object isn't supported this phase. Source: `src/lib/content/fields.ts:171-180` (typed as `Record<string, FieldDescriptor>` for object fields, `FieldDescriptor` for array items) plus page's stated error text "containers nest one level only." [verified: against fields.ts type signature; exact throw message not grepped]
- `f:t4hh8w` A field key never contains a dot, in a container or at the top level, since a dotted path is how the engine addresses a nested field internally. Source: `src/lib/content/fieldset.ts:390` (`if (k.includes('.')) throw new Error(...)`). [verified]
- `f:46edxb` An entry's id is its markdown filename with `.md` stripped; the on-disk filename is always exactly the id plus `.md`. A dated concept additionally carries a leading date on the filename (`2026-08-14-my-post.md`); the URL slug strips only that leading prefix. Source: `src/lib/content/ids.ts:1-3,18-20` (`idFromFilename`: "An entry's id is its markdown filename without `.md`"), `:44-58` (`DATE_PREFIX_RE`/`slugFromId`, already used for `f:z68na4`/`f:grhib4`). [verified]
- `f:zce3pt` The filename's date prefix only feeds the slug; an entry's actual date (used by dated permalinks and templates) always comes from the entry's own `date` frontmatter field, never the filename, which is why a dated permalink pattern makes `date` structurally required. Source: `src/lib/content/ids.ts:44-58` (`slugFromId` strips only the filename's leading date prefix), `src/lib/content/manifest.ts:141` (`e.date`, a distinct frontmatter-sourced field), `src/lib/content/concepts.ts` (`validateUrlPolicy`, a date-token permalink requires a `date` field, already `f:2i6u6o`). [verified]
- `f:s2zbzm` `routing` is one of three shorthands: `'feed'` (routable, dated, appears in feeds and the sitemap), `'page'` (routable, undated, the default when omitted), or `'embedded'` (not routable at all, no permalink, never listed or prerendered). Source: `src/lib/content/concepts.ts:15-19` (`ROUTING_SHORTHANDS`: `feed`, `page`, `embedded`). [verified]
- `f:hdrzxd` A concept's fieldset produces a validator that the admin's save action runs against submitted form data before any commit; invalid input bounces back with field-keyed error messages and never reaches the Backend. Source: `src/lib/sveltekit/content-routes-entry-write.ts:128,181` (`saveToBranch` calls `concept.validate(decoded, body)` before any commit), `src/lib/content/fieldset.ts` (the per-field validator built from the fieldset, already `f:phdgof`). [verified]
- `f:dccnyu` A `behavior.validate` function that throws is caught and the field is treated as valid rather than failing the whole save (a fail-open behavior on a throwing custom validator, distinct from the fieldset's own built-in validation which fails closed). Source: `src/lib/content/fieldset.ts:458-471` (`config.behavior[key].validate`'s throw is caught: "log and treat the field as valid rather than breaking the save"). [verified]
- `f:t5ebsr` The content manifest carries one row per entry with identity, routing facts, outbound `cairn:` links, reference-field edges, tags, and included fragments; a build regenerates and verifies it, and a publish patches its one changed entry in the same commit as the content change (corrected from "a save": a save to the pending branch commits no manifest change at all), so the manifest never drifts from the files it describes by more than one uncommitted edit. Source: `src/lib/sveltekit/content-routes-entry-write.ts:341-352` (`publishAction`: "a save commits no manifest, so the moment an entry goes live is this commit"). [verified]
- `f:a8ecm0` A `reference` field holds a typed pointer `{ concept, id }`; the manifest carries both sides of that edge so a rename or delete can warn about what still points at the referenced entry. Source: `src/lib/content/manifest.ts:11,34-39` (`extractReferenceEdges`, `ReferenceEdge[]` on each entry). [verified]
- `f:880bg6` Fragments are declared under the reserved `fragments` concept key and spliced verbatim into any entry that includes them with `::include{fragment="<id>"}`; a fragment has no permalink of its own and only reaches a reader through an including entry. Source: `src/lib/content/concepts.ts:28` (`FRAGMENTS_CONCEPT_ID = 'fragments'`), `:170-172` (throws unless `routing: 'embedded'`, "the include directive resolves against it"). [verified]

## docs/extend/data-tiers.md

- `f:b6gquz` Three data tiers: Git (content entries, content manifest, media manifest, keyed by concept/id or content-hash prefix), D1 (`editor`/`magic_token`/`session` baseline plus `audit_log`/`preview_tokens` opt-in, keyed by email or SHA-256 hash), R2 (uploaded media bytes, keyed by content hash, deduplicated). Source: `migrations/0000_auth.sql`, `migrations/0002_audit.sql`, `migrations/0003_preview.sql` (D1 tier, already `f:pzbmhq`/`f:wnvqlz`), `src/lib/media/store.ts:16,52` (R2 tier, already `f:2hnxsr`), `src/lib/content/compose.ts:12-13` (Git-committed manifests, already `f:lu67dk`). [verified: a summary of the three tiers, each independently verified elsewhere in this file]
- `f:pzbmhq` A token and a session id are both looked up by their SHA-256 hash, never the raw value. Source: `migrations/0000_auth.sql` (`token_hash TEXT PRIMARY KEY` on `magic_token`, `id TEXT PRIMARY KEY` on `session` is a generated id, not a hash, but lookups go through `hashToken`), `src/lib/auth/crypto.ts:96-98` (`hashToken`: `crypto.subtle.digest('SHA-256', data)`). [verified]
- `f:xkkt1o` `role` used to carry a database `CHECK` constraint limiting it to `owner`/`editor`; `0001_roles.sql` rebuilds the table to validate against a site's declared role vocabulary at the application layer instead, so a site can extend roles without a further schema migration. Source: `migrations/0000_auth.sql` (`role TEXT NOT NULL CHECK (role IN ('owner', 'editor'))`), `migrations/0001_roles.sql` (create-copy-drop-rename to `role TEXT NOT NULL` with no CHECK; comment: "Role validity moves to the app layer from here on"). [verified]
- `f:wnvqlz` `audit_log` (`0002_audit.sql`) holds one row per audited admin action (`actor`/`action`/`entity`/`entity_id`/`detail`), timestamped in ISO 8601 at millisecond resolution, written only by a site that wires `createD1AuditSink`. Source: `migrations/0002_audit.sql` (`CREATE TABLE audit_log (id, actor, action, entity, entity_id, detail, created_at TEXT DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ', 'now')))`; comment: "Opt-in: only a site that wires `createD1AuditSink`... needs this migration"). [verified]
- `f:dtwvtz` `preview_tokens` (`0003_preview.sql`) holds a hashed, single-use-per-link token naming the shared draft, swept on expiry the same no-cron way `magic_token` is; written only by a site that mints preview links. Source: `migrations/0003_preview.sql` (`token_hash TEXT PRIMARY KEY`, `expires_at INTEGER`, comment: "Opt-in: only a site that mints preview links... ever touches this table"; the expiry sweep comment cites the same idiom `idx_magic_token_email`/`idx_session_email` use). [verified]
- `f:2hnxsr` A site that declares a `media` block on its adapter gets an R2 bucket bound by name and resolved at request time from that declared binding, never a hard-coded one; the render resolver always returns the bare full-size delivery path, and attaches a responsive `srcset` (small fixed width ladder via Cloudflare Image Transformations) only when the zone has transformations on and the asset's width is known. Source: `src/lib/media/store.ts:16,52` (`r2Store(bucket: R2Bucket)`), `src/lib/render/resolve-media.ts:20,52,181` (`SRCSET_WIDTHS`, srcset built "once zone transformations are on and the asset's width is known"). [verified]
- `f:4t707i` The media manifest is keyed by a 16-hex content-hash prefix and is the dedup lookup an ingest checks before writing anything, distinct from the content manifest, which keys by concept and id. Source: `src/lib/media/manifest.ts:1-5,16-18` (module comment: "the dedup lookup: an ingest checks the content-hash prefix here before storing"; "16-hex content-hash prefix rather than concept and id"). [verified]
- `f:lu67dk` R2 holds media bytes because git does not carry binary assets at real scale and D1 is not sized for anything measured in megabytes; both manifests (content and media) stay committed JSON under `src/content/.cairn/`, and only the bytes they describe live in R2. Source: `src/lib/content/compose.ts:12-14` (`CONTENT_MANIFEST_PATH = 'src/content/.cairn/index.json'`, `MEDIA_MANIFEST_PATH = 'src/content/.cairn/media.json'`). [verified]
- `f:qrwncf` A site's own extension data (a member roster, an event schedule) is unconstrained by cairn's own three tiers, beyond the reserved `cairn_` cookie and table-naming conventions. Source: `src/lib/auth-channel/store.ts:51,63,80` (`cairn_channel_meta` table), `migrations-channel/0000_channel.sql` (`cairn_channel_*` table names); the `cairn_` prefix convention is consistent across every engine-owned D1 table and cookie name found in this pass. [verified]

## docs/extend/debug-your-site.md

- `f:99x86q` Three diagnostic surfaces divide by what they key on: `admin/setup-recovery.md` keys on `create-cairn-site`'s step names and state file; `admin/troubleshooting.md` keys on observed site behavior for a non-developer admin; `extend/debug-your-site.md` keys on log events whose cause is a site's own source code. All three read the same log event vocabulary at `docs/reference/log-events.md`. Source: page text (scope-dividing statement). [candidate: excluded, describes how the three doc-arm pages divide their own subject matter, not an engine code fact]
- `f:gy2h7p` Every event on this page fires from engine code running inside a site's own extension points (a concept's `fieldset`, a `createSectionAction`-wrapped route, a hand-built public route factory); none describes an engine bug, each reports that surrounding site code violated the contract. Source: `src/lib/content/fieldset.ts:466` (`content.field_behavior_failed`), `src/lib/delivery/public-routes.ts:47,210` (`media.resolver_absent`), `src/lib/sveltekit/admin-action.ts:321-322` (`admin.action.unaudited`), `src/lib/sveltekit/section-action.ts:229-278` (rate-limit events), `src/lib/log/events.ts:30` (`config.invalid`); each already-verified event on this page fires from a call site inside a site-supplied hook, config, or route wrapper. [verified: a summary of the individually verified events on this page]
- `f:lbeeak` `content.field_unmarked` (warn) fires when a concept declares a multiselect field named `tags`, `freetags`, or `categories` but no field carries `taxonomy: true`; the tag index only reads the field marked that way. Fix: add `taxonomy: true` to the actual tag field. Source: `src/lib/content/fieldset.ts:350-362` ("At most one top-level multiselect feeds a concept's taxonomy... there is no field-name default, since the record key is arbitrary"). [verified: structurally against fieldset.ts's taxonomy-marker logic, though the exact three field-name triggers (`tags`/`freetags`/`categories`) were not independently grepped]
- `f:qdfs37` `content.field_behavior_failed` (warn) fires when a field's `behavior.validate()` throws during save-time validation; the engine catches the throw and treats the field as valid rather than failing the whole save, so the bad value lands. The `owner` field names the schema and `error` carries the exception message. Source: `src/lib/content/fieldset.ts:458-471` (`log.warn('content.field_behavior_failed', { field, owner, error })`). [verified]
- `f:twqb0s` `media.resolver_absent` (warn, once at construction) fires when a public route factory is built with media configured (adapter declares a `media` block) but no `resolveMedia` function was wired into it; every `media:` reference then has nothing to resolve against, rendering a literal `media:` token. Source: `src/lib/delivery/public-routes.ts:47,210` (`log.warn('media.resolver_absent')`). [verified]
- `f:07efts` `admin.action.unaudited` throws `UnauditedActionError` in dev the moment a `createAdminAction`-wrapped route returns without calling `ctx.audit`; in production it logs at error instead of throwing, since a redirect or error response mid-request would be worse than a gap in the audit trail. Source: `src/lib/sveltekit/admin-action.ts:321-322` (`if (dev) throw new UnauditedActionError(...); log.error('admin.action.unaudited', ...)`). [verified]
- `f:r3l7eq` `audit.sink.call_failed` (error) fires when the site's own `event.locals.cairnAuditSink` throws synchronously or returns a rejected promise; `ctx.audit` already ran and the action completed, only the record was lost. If using the packaged `createD1AuditSink`, check `audit.sink.write_failed` instead, since that sink catches its own failures before this event fires. Source: `src/lib/sveltekit/admin-action.ts:255-280` (`audit.sink.call_failed`: fires when `event.locals.cairnAuditSink` throws or rejects; "the mutation this record describes already completed"), `src/lib/sveltekit/audit-sink.ts:66` (`createD1AuditSink` is fail-open, catches its own failures before this event fires). [verified]
- `f:i1dayf` `admin.action.rate_limit_absent` and `admin.action.rate_limit_failed` (both warn) both degrade to open (never block) on purpose: the former fires when the configured rate-limit binding resolves to nothing or its `key()` call throws; the latter when the resolved limiter's own `limit()` call throws (carries the `error` field). Source: `src/lib/sveltekit/section-action.ts:229-278` (`admin.action.rate_limit_absent` logged when `!limiter`; `admin.action.rate_limit_failed` logged in the `key()`-throw and `limit()`-throw catch blocks). [verified]
- `f:nup3og` `admin.action.misconfigured` (error) fires with `reason: 'db_not_bound'` when `config.resolveDb` returns null/undefined, or `reason: 'access_map_not_attached'` when `event.locals.cairnAccess` was never set (the admin guard never ran on this route). Source: page's own table row; consistent with `src/lib/sveltekit/section-action.ts`'s `resolveDb`/access-guard pattern. [verified: structurally against section-action.ts's config shape, exact reason-string literals not line-grepped]
- `f:8fyoha` `config.invalid` (error) fires when committed site config fails to parse or validate; two loads (`scope: 'nav'`, `scope: 'vocabulary'`) degrade to an empty result rather than failing the screen, and two saves (`scope: 'settings'`, `scope: 'vocabulary'`) answer `fail(500)` with generic copy while the log record carries the parser's real error message. Source: `src/lib/log/events.ts:30` (`'config.invalid'`), `src/lib/sveltekit/content-routes-settings.ts:151,168` (`config.invalid` with `scope: 'settings'`). [verified]
- `f:ogz5eu` A visual-regression baseline that reads the live system clock (`Date.now()` inside a component/route a test also renders) drifts daily even with no source change; the fix is a fixed-today seam read only from `platform.env` (`CAIRN_FIXED_TODAY`), never `Date.now()` directly. Source: `grep` over `src/lib` finds no `CAIRN_FIXED_TODAY` symbol, confirming this is a site-authored testing recipe with no corresponding engine seam, as the page itself frames it. [verified]
- `f:yomgrm` `new Date(env.CAIRN_FIXED_TODAY)` yields `Invalid Date` silently on a malformed value rather than throwing; write the string as an explicit instant (`2026-08-29T00:00:00Z`), not a date-only form, since a date-only string parses as UTC midnight and formats as the previous local day on machines west of UTC. Source: JavaScript `Date` constructor parsing rules. [external: JavaScript]
- `f:swjwxb` `event.platform` is defined under `wrangler dev`/`wrangler pages dev` but is `undefined` under plain `vite dev` or `vite preview`, so `event.platform?.env ?? {}` silently falls back to `{}` outside the Workers runtime, and `CAIRN_FIXED_TODAY` is never read there without a `process.env` fallback. Source: SvelteKit Cloudflare adapter `platform` binding. [external: Cloudflare/SvelteKit]

## docs/extend/declare-your-own-concept.md

- `f:r03vis` Posts and Pages are not reserved names; the concept set is entirely the site's own, each a key in the adapter's `content` map, and the admin nav, create affordances, and delivery layer follow whatever name is used. Two engine conveniences do key on the literal ids `posts`/`pages` (the default permalink pattern and the onboarding checklist), but a site can still rename or replace either concept freely; only those two conveniences stop applying. Source: `src/lib/content/concepts.ts:65-68` (`defaultPermalink`, `id === 'pages'` gets a distinct default), `src/lib/content/getting-started.ts:25-28` (onboarding checklist keys on `'posts'`/`'pages'`), `src/lib/content/types.ts:229` (`content: Record<string, ConceptConfig>`, any id accepted). [verified]
- `f:4mz4tp` `createSiteIndexes` throws at build time for any declared concept with no matching `import.meta.glob` pattern passed in, since Vite needs the literal glob at the call site and it cannot be added programmatically. Source: `src/lib/delivery/site-indexes.ts:54-58` (same throw as `f:pdgkex`). [verified]
- `f:7az6m2` If a site also builds the manifest with the `cairnManifest` Vite plugin, a concept missing from that plugin's own `content` option fails silently: the manifest simply never gets any rows for that concept, with no throw and no build warning (the mirror-image failure mode to the plugin's drift-detection throw; see the settled `cairnManifest` fact under build-a-site-by-hand.md). Source: `src/lib/delivery/manifest.ts:18-33` (`buildSiteManifest`: `const record = globRecord[descriptor.id] ?? {};`, so an absent key silently yields no entries). [verified]
- `f:6e06p2` `routing: 'page'` gives each entry its own routable, undated page and puts it in the sitemap; `'feed'` is for dated, chronological content appearing in feeds and an archive; `'embedded'` declares a concept with no public page at all (not routable, not in the sitemap, not in any feed), better suited to a reference-only concept like `authors` that is never browsed on its own. Source: `src/lib/content/concepts.ts:15-19` (`ROUTING_SHORTHANDS`, same as `f:s2zbzm`). [verified]
- `f:y3s3t9` The one reserved embedded concept is `fragments`, which the `::include` directive resolves against. Source: `src/lib/content/concepts.ts:28,170-173` (`FRAGMENTS_CONCEPT_ID = 'fragments'`, throws unless `routing: 'embedded'`). [verified]
- `f:vs77a6` An `'embedded'` concept still has its own directory, fields, and admin list/editor exactly like a `'page'` concept; the only thing `'embedded'` removes is a public page. Source: `src/lib/content/concepts.ts:16-19` (`ROUTING_SHORTHANDS.embedded = { routable: false, dated: false, inFeeds: false }`, the only fields the shorthand changes; `dir`, `fields`, admin wiring are unaffected by `routing`). [verified]
- `f:i2k4jp` A `reference` field connecting two concepts renders as a picker scoped to the target concept in the editor; on the public side, a route resolves the id to the target's title and permalink and renders a link. Source: `src/lib/content/fields.ts:109-110,174-175` (`ReferenceField`, `reference: (o) => ({ type: 'reference', ...o })`). [verified]

## docs/extend/define-an-adapter-and-schema.md

- `f:hkh101` `defineAdapter` requires four groups: `content` (concepts), `backend` (where commits land), `email` (magic-link sender), and `rendering` (markdown to HTML); `media`, `roles`, `access`, `aiPosture`, and `editor` are optional. Source: `src/lib/content/types.ts:226-274` (`CairnAdapter`: `content`, `backend`, `email`, `rendering` required; `roles?`, `access?`, `media?`, `aiPosture?`, `editor?` optional). [verified]
- `f:gwmmkx` `dir` names the concept's markdown directory; `label` names it in the admin nav and create button ("New Posts"); the page says `singular` should always be set too since it "has no default that reads naturally for a plural label." Corrected against the code: `singular` DOES have a default, it silently falls back to `label` itself (the plural label) when omitted, which is exactly the awkward-reading case the page warns about, so the fact should read "when omitted, `singular` defaults to the plural `label`, which reads awkwardly (`New Posts` instead of `New post`)," not "has no default." Source: `src/lib/content/concepts.ts:186` (`singular: config.singular ?? label`). [verified: page fixed at `docs/extend/define-an-adapter-and-schema.md:41` to say `singular` defaults to the plural `label`]
- `f:lkgubp` `defineConcept` validates at declaration time, not at first save: a bad `permalink`, `datePrefix`, or `routing` throws the moment the module loads. Source: `src/lib/content/concepts.ts:39` (`throw new Error('cairn: concept "${id}" routing "${routing}" must be one of feed, page, embedded')`) among other declaration-time throws in the same normalization pass. [verified]
- `f:2i6u6o` A `permalink` using a date token (`:year`, `:month`, `:day`) requires a `date` field on the concept; `defineConcept` throws at declaration if missing or the wrong type, and normalizes it to `required: true` automatically since the permalink cannot resolve without a value. Source: `src/lib/content/concepts.ts` (`validateUrlPolicy`, called at declaration time from `defineConcept`'s normalization pass; date-token permalinks paired with the dated-routing enforcement). [verified]
- `f:cg1eo5` `datePrefix` (`'year'`, `'month'`, or `'day'`) controls how much of the date lands in the filename stem, independently of the permalink shape, so a concept can carry a full date in frontmatter while only prefixing the filename with the year. Source: `src/lib/content/concepts.ts:189` (`datePrefix: policy.datePrefix ?? 'day'`, a field distinct from `permalink`). [verified]
- `f:twcsgm` `owner`, `repo`, `appId`, and `installationId` passed to `createGithubApp` are plain, non-secret identity; the private key that signs commits stays out of `cairn.config.ts` entirely, read from the Worker secret `GITHUB_APP_PRIVATE_KEY_B64` at request time. Source: `src/lib/github/backend.ts:87,104` (non-secret identity), `src/lib/env.ts:31` (private key stays a Worker secret), `src/lib/github/credentials.ts:18` (`env.GITHUB_APP_PRIVATE_KEY_B64` read at request time). [verified]
- `f:68w9k9` `defineAdapter` narrows the return type to a site's concrete concepts and fields, so a read against e.g. `cairn.content.posts` carries the declared field types through with no cast. Source: `src/lib/content/adapter.ts:28` (`export function defineAdapter<const A extends CairnAdapter>(adapter: A): A`, a `const` type parameter preserving the concrete shape). [verified]

## docs/extend/design-your-site.md

- `f:rxj43c` A scaffolded site ships Waymark (cairn's public reading template) wired into a chassis of genre-free plumbing; a hand-built site (via `build-a-site-by-hand.md`) has neither and brings its own theme and delivery wiring from the start. Source: `templates/waymark/src/chassis/` (directory exists, the scaffold ships chassis plumbing), contrasted with `docs/extend/build-a-site-by-hand.md`'s five milestones building no theme/chassis of their own. [verified: the scaffold's chassis traces to templates/waymark/src/chassis/; the hand-built half rests on build-a-site-by-hand.md's own milestone scope, not code]
- `f:s4prb0` `src/chassis/` holds plumbing every site shares regardless of look: content indexing, feed/sitemap builders, server runtime composition, the token *system* (CSS custom properties), the reading-surface foundation, and composition primitives (card, band, section, hero, sidebar layout); everything outside `src/chassis/` is Waymark's own content (adapter config, chrome components, color/type values, page compositions). Source: `examples/showcase/src/chassis/README.md:1-13` ("The boundary rule: a theme is everything that isn't chassis... Everything outside `src/chassis/`... is the theme's own content"), enforced by `npm run check:chassis-boundary` (`scripts/checks/check-chassis-boundary.mjs`). [verified]
- `f:lwrqfd` A theme file reaches chassis only through exported seams: the `$chassis` alias in TypeScript/Svelte, or a relative `@import` in CSS. cairn's own repo enforces this boundary on the showcase with a `check:chassis-boundary` gate; a scaffolded site inherits it as a convention only, with no gate of its own. Source: `scripts/checks/check-chassis-boundary.mjs:2-13` (parses the canonical seam table from `examples/showcase/src/chassis/README.md`, walks every `examples/*/src` with its own `chassis/` copy); the top-level `package.json` wires the script, and a scaffold produced by `packages/create-cairn-site` carries no such script of its own. [verified]
- `f:iel6v5` Every design-scale key `tokens.css` declares (`--font-*`, `--text-step-*`, `--spacing-*`, `--color-muted`, etc.) carries a generic default; a theme `@import`s `tokens.css` first, then redeclares the same keys with its own numbers in a later `@theme` block, and cascade order does the override. Named DaisyUI themes (role colors, geometry values) are never declared in chassis at all. Source: `examples/showcase/src/chassis/tokens.css:12-13` ("the actual named themes (`@plugin \"daisyui/theme\"`...) are" declared outside chassis), `:109-110` (`--color-muted`, generic `color-mix` default). [verified]
- `f:kt0epf` The committed re-skin recipe is about fourteen values, light and dark: rotate `--color-primary`'s hue while holding lightness and chroma, edit the `base-100/200/300` ladder plus `base-content`; optionally swap the two `--font-*` tokens or retune one type ratio or space-scale step. `prose.css` comes along for free at zero extra edits since it reads the same role tokens. Source: `examples/showcase/src/theme/theme.css` (the site's own `@plugin "daisyui/theme"` block carrying `--color-primary`/`base-100/200/300`/`base-content`), `examples/showcase/src/chassis/prose.css:1-6` (module comment: "every element bound to the DaisyUI roles and the cairn tokens in theme.css"). [verified]
- `f:ylmc9c` A full status-color rebrand costs more than the base recipe: each status carries both a fill (`@plugin` block) and a matching on-surface ink (`--cairn-{info,success,warning,error}-ink`, light and dark); re-tuning one without the other desyncs directive text and code-highlight colors from the surrounding fills. Source: `examples/showcase/src/chassis/tokens.css:125-135` (`--cairn-code-string: var(--cairn-success-ink)`, `--cairn-success-ink: var(--color-success)`, etc.). [verified]
- `f:xv2ien` Two CI gates hold the re-skin recipe honest on cairn's own showcase: `check:public-tokens` (dual-gamut AA contrast check plus a dangling-`var()` check) and `test:reskin` (a hue-rotated fixture theme proves AA still holds and prose reads from one token source); neither ships in a scaffolded site's own `package.json`. Source: `package.json:55,65` (`"check:public-tokens": "node scripts/checks/check-public-tokens.mjs"`, `"test:reskin": "node scripts/lab/reskin-fixture.mjs"`, both top-level engine scripts absent from `packages/create-cairn-site`'s template). [verified]
- `f:bdnzcy` `/styleguide` is the live demo surface rendering every registered directive (callout, alert, video facade, pull-quote, CTA, FAQ), the type scale, and component recipes against the current `theme.css`. Source: `examples/showcase/src/routes/(site)/styleguide` (route directory exists). [verified]
- `f:7pv6se` `cairn-media-seed --from <url>` seeds a local R2 simulator from a deployed site's media library; it is idempotent (a re-run overwrites the same keys with the same bytes), and a site with nothing deployed yet has nothing to seed (media uploaded through local `/admin` lands directly in the local bucket). Source: `src/lib/media-seed/assemble.ts:14,33,39,44` (`--from` flag; comment: "overwrites an earlier one"). [verified]
- `f:ctognq` `composition.css` exposes primitives `.cairn-card`, `.cairn-band`, `.cairn-section`, `.cairn-hero`, `.cairn-sidebar-layout`, each exposing its own `--cairn-<primitive>-*` custom properties for a per-instance override on top of shared tokens. Source: `examples/showcase/src/chassis/composition.css:18-28` (`.cairn-card { --cairn-card-padding: ...; --cairn-card-radius: ...; }`). [verified]
- `f:kj37zz` `feed.ts` has exactly two dependents (the two feed routes) and can be deleted along with those route bodies with nothing else changing; `content.ts` is load-bearing for six delivery routes at once and is not a bare deletion, requiring all six routes' imports to be replaced in the same change. `src/chassis/README.md` documents the full dependents table. Source: `examples/showcase/src/chassis/README.md:212-213` (dependents table: `content.ts` → "Every delivery route... a theme dropping it must replace all six route imports"; `feed.ts` → "`feed.xml/+server.ts`, `feed.json/+server.ts`... Delete the file and the two feed route files"). [verified]

## docs/extend/enable-tidy.md

- `f:glluwk` Tidy is opt-in, off by default, and never runs without an editor explicitly triggering it on a draft. Source: `src/lib/nav/site-config.ts:124-125` `TidyConfig.enabled` doc comment: "Master switch. Default false; tidy is opt-in (spec 2.8, decision 1)." The "never runs without an editor explicitly triggering it" half is consistent with `tidyAction` (`src/lib/sveltekit/content-routes-tidy.ts`) being a server action invoked only from the editor's own Tidy button, with no scheduled or automatic call site found under `src/lib`. [verified]
- `f:vcthwj` Turning tidy on requires `npm install @anthropic-ai/sdk`, an optional peer dependency. Source: `package.json:205,211-213` (`peerDependencies["@anthropic-ai/sdk"]`, `peerDependenciesMeta["@anthropic-ai/sdk"].optional: true`). [verified]
- `f:avtt69` `tidy.enabled: true` is set in `site.config.yaml`, and `ANTHROPIC_API_KEY` is bound as a Worker secret via `npx wrangler secret put ANTHROPIC_API_KEY`. Source: `src/lib/sveltekit/platform-bindings.ts` (grep hit for `ANTHROPIC_API_KEY`). [verified]
- `f:59aion` `ANTHROPIC_API_KEY` is the one optional member of `CairnPlatformBindings`; every other binding in that type is required. Source: `src/lib/sveltekit/platform-bindings.ts`. [verified]
- `f:fmv6m8` The default tidy model is `claude-sonnet-5`. Source: `src/lib/nav/site-config.ts:133` `export const DEFAULT_TIDY_MODEL = 'claude-sonnet-5';`. [verified]
- `f:4c92fh` `claude-haiku-4-5` is the documented cheaper/faster alternative model. Source: `src/lib/sveltekit/content-routes-settings.ts:132` (`'claude-haiku-4-5': 'Claude Haiku'`) and `CairnTidySettings.svelte:401`. [verified]
- `f:ssdn1f` Tidy runs at the "low" effort tier by default, since a proofread does not need extended reasoning. Source: `src/lib/sveltekit/content-routes-tidy.ts` (`output_config: { effort: 'low' }` per doc comment on `supportsEffort`). [verified]
- `f:wlohtk` Only models in `claude-sonnet-5`, `claude-opus-5`, `claude-sonnet-4-6` (by prefix) or `claude-opus-4-<n>` with n >= 6 accept the `output_config.effort` parameter; other models (including `claude-haiku-4-5`) reject it with a 400. Source: `src/lib/sveltekit/content-routes-tidy.ts:57-68` `supportsEffort()`. [verified]
- `f:3yte99` `tidy.conventions` builds a set of style rules beyond the always-on objective fixes (spelling, grammar, doubled words, stray whitespace, capitals, terminal punctuation); only `fixes` defaults on, every other convention defaults off. Source: `src/lib/nav/site-config.ts:174-176` `defaultTidyConventions()` returns `{ fixes: true, enDashRanges: false, smartQuotes: false, brandCaps: false }`; every other field (`oxfordComma`, `numberStyle`, `measurements`, `percent`, `emDash`, `ellipsis`, `timeFormat`) stays `undefined` (off) unless set. [verified]
- `f:sdz8sm` Convention fields: `fixes` (boolean, default true), `oxfordComma` (`always`/`complex-only`/`never`), `numberStyle` (`under-ten`/`under-hundred`/`always-numerals`), `measurements` (`abbreviate`/`spell-out`), `percent` (`sign`/`word`), `emDash` (`spaced`/`closed`), `enDashRanges` (boolean, default false), `ellipsis` (`single-char`/`three-dots`), `timeFormat` (three literal formats), `smartQuotes` (boolean, default false), `brandCaps` (boolean, default false). Source: `src/lib/nav/site-config.ts:142-176` `TidyConventions` interface and `defaultTidyConventions()`. [verified]
- `f:xijdh6` An editor's personal dictionary lives in a separate committed file, `src/content/.cairn/dictionary.txt`, not a config choice. Source: `src/lib/content/compose.ts:14` `const DICTIONARY_PATH = 'src/content/.cairn/dictionary.txt';` and `src/lib/sveltekit/content-routes-context.ts:402` (same literal default). [verified]
- `f:q412re` Before a proposed tidy change reaches an editor, cairn checks the model output against the original: directive structure, heading count/levels, fenced-code-block count, frontmatter byte-for-byte, the `media:` token multiset, and every code span must match, plus a wording-divergence bound; a failing result never reaches the review screen. Source: `src/lib/components/tidy-validate.ts` `validateTidy()` (checks `directiveSignature`, `headingSignature`, frontmatter span equality, `mediaHashes` multiset, `codeContents` multiset, then the `divergence` floor/fraction bound). [verified]
- `f:f5548q` The tidy input cap is roughly 24,000 characters (about 8,000 input tokens under Sonnet 5's tokenizer); a draft over that is refused before the model is ever called. Source: `src/lib/sveltekit/content-routes-tidy.ts:44` `const MAX_TIDY_CHARS = 24_000;`. [verified]
- `f:6zqexn` The settings screen's key-health check caches its result for ten minutes. Source: `src/lib/sveltekit/tidy-key-health.ts:19` `const TTL_MS = 10 * 60 * 1000;` (governs both the unhealthy-key mark and the settings-load probe cache). [verified]
- `f:38qxp3` In local development the Anthropic SDK is never actually called; the dev backend injects a stubbed Anthropic client. Source: `src/lib/sveltekit/content-routes-context.ts:135,371` `lazyAnthropicClient` (dynamic-imports the real `@anthropic-ai/sdk`) is the only default; `config.tidy?.client` is a caller-supplied override for tests, not a dev-backend auto-stub, and no dev-backend module (`src/lib/auth-channel/`, `src/lib/dev-flag.ts`) branches tidy calls on dev mode. [rejected: no dev-backend stub for the Anthropic client exists; a local `npm run dev` with a real ANTHROPIC_API_KEY calls the real SDK exactly as production does, unless a site's own test harness passes `tidy.client` itself]
## docs/extend/link-content-with-references.md

- `f:563h9u` A `reference` field stores another entry's permanent id in frontmatter, typed to one concept, declared via `fields.reference({ label, concept, required })`. Source: `src/lib/content/references.ts` (`ReferenceEdge`, `extractReferenceEdges`). [verified]
- `f:3zswxq` `array(reference)` renders in the editor as a removable chip list. Source: `src/lib/components/ReferenceField.svelte:6` ("`array(reference)` renders a removable chip list, each chip showing the target's resolved title"). [verified]
- `f:9i99mg` The engine records each reference as a `ReferenceEdge` (`{ field, concept, id }`), with `concept` taken from the field's own declaration rather than the stored value. Source: `src/lib/content/references.ts:22` (`export interface ReferenceEdge`) and `src/lib/content/manifest.ts:93` (`extractReferenceEdges(frontmatter, descriptor.fields)`). [verified]
- `f:gk88bv` The reference rewriter is byte-preserving: it splices the changed id into frontmatter by source offset rather than a YAML round-trip, so renaming a target updates every referencing entry's stored id without touching other bytes. Source: `src/lib/content/references.ts:2-3` ("this module surgically rewrites that id on rename. The rewriter is byte-preserving: it splices the value substring by source offset rather than round-tripping through gray-matter"). [verified]
- `f:nibxrd` `resolveReferences(site, descriptor, frontmatter)` resolves a `reference` field to one `ResolvedReference`, and an `array(reference)` field to an array of them in edge order. Source: `src/lib/delivery/site-resolver.ts:159` `export function resolveReferences(...)` and `:137` `projectReference`. [verified]
- `f:mpdshq` `resolveReferences` drops an id with no live target rather than throwing, since a build already guarantees every committed reference resolves. Source: `src/lib/delivery/site-resolver.ts:155` (doc comment: "build's `verifyReferences` gate already fails a true dangling edge, so an unresolved id at request..."). [verified]
- `f:6ggh4v` `verifyReferences` walks the manifest and throws, naming the source entry, field, and missing target, the moment a build runs it; a dangling reference fails the build. Source: `src/lib/content/manifest.ts:388` `export function verifyReferences(manifest: Manifest): void`. [verified]
- `f:l32sb1` Unlike a `cairn:` link, a reference has no prerender-time resolver fallback, so the build gate is its only integrity check. Source: `src/lib/delivery/site-resolver.ts:155` doc comment (`resolveReferences` drops an unresolved id rather than throwing, deferring entirely to `verifyReferences` at build) contrasted with `src/lib/render/resolve-links.ts`'s `cairn:` resolver, which runs a live resolve at render/prerender time. [verified]
- `f:2zl9zw` The delete guard checks two things before deleting an entry: inbound `cairn:` body links from the published manifest, and inbound reference edges from a cross-branch index built over the manifest plus every open `cairn/*` edit branch. Source: `src/lib/sveltekit/content-routes-entry-destructive.ts:102-138` (`inboundLinks(manifest, ...)` against the published manifest, then `buildReferenceIndex(backend, runtime.concepts, manifest, { strict: true })` unioning main plus every open branch). [verified]
- `f:7s52oz` The cross-branch reference index build runs with `strict: true`, and a read error during the index build refuses the delete rather than counting as an absent reference (fails closed). Source: `src/lib/sveltekit/content-routes-entry-destructive.ts:134-138` (`buildReferenceIndex(..., { strict: true })`; catch returns `fail(503, { error: 'Could not verify where this entry is referenced. Try again.' })`). [verified]
- `f:8on7h4` An unpublished draft's reference edge still counts: deleting or renaming its target is refused even though nothing about the edge is live yet. Source: `src/lib/sveltekit/content-routes-entry-destructive.ts:127-138,272-286` (the strict index unions "main's published edges" with every open `cairn/*` branch before the refusal check). [verified]
- `f:re5qlm` Renaming refuses outright, naming the blocking entries, when a different open branch than the entry's own holds an inbound edge. Source: `src/lib/sveltekit/content-routes-entry-destructive.ts:282-286` ("Refuse when a THIRD-PARTY open branch holds an inbound reference (symmetric with the pending-edits guard)"). [verified]
- `f:bz5j8f` Saving an entry whose `reference` field names a missing or still-draft target only warns; a body `cairn:` link to a missing target refuses the save outright. Source: `src/lib/sveltekit/content-routes-entry-write.ts:246-273` ("a link to a draft target commits with a warning... A reference NEVER blocks the save: unlike a body link, an absent or draft target only warns") with `fail(400, ...)` at line 261 for the body-link case. [verified]

## docs/extend/migrate-existing-content.md

- `f:qd7iat` cairn ships no importer; migration is done with a short script using `parseMarkdown`, the same parser cairn's own save path uses, to split frontmatter from body. Source: `src/lib/index.ts:53` `export { parseMarkdown } from './content/frontmatter.js';`. [verified]
- `f:3r6rqn` `serializeMarkdown` is an internal save-path detail, not a public export; a migration script must write the target `---`-delimited frontmatter block directly instead. Source: `src/lib/index.ts` (grep for `serializeMarkdown` finds no such export; only `parseMarkdown`, `formatManifest`, `parseManifest`, `verifyManifest`, `verifyReferences` are exported from `content/`). [verified]
- `f:z68na4` An entry's id is its filename stem, verbatim, the only identity cairn gives it. Source: `src/lib/content/ids.ts:1-3` ("An entry's id is its markdown filename without `.md`, so there is no slug codec") and `:18-20` `idFromFilename`. [verified]
- `f:grhib4` cairn strips only a date prefix at the concept's declared `datePrefix` granularity: a file named `2024-03-15-my-trip.md` under `datePrefix: 'month'` strips only `2024-03-`, leaving day digits (`15-my-trip`) in the slug; this never throws or fails a build. Source: `src/lib/content/ids.ts:44-58` `DATE_PREFIX_RE` (`month: /^\d{4}-\d{2}-/`) and `slugFromId()` (a non-matching prefix is simply not stripped, no throw). [verified]
- `f:nhjraz` A filename outside lowercase-letters/digits/single-internal-hyphens shape still serves its own page but is invisible to a `cairn:` link and to the rename/delete guards, since both treat that id as unparseable. Source: `src/lib/content/ids.ts:6-10` `ID_RE`/`isValidId` (the shape gate `splitConceptIdToken`/link and rename/delete tooling validate ids against); `docs/extend/migrate-existing-content.md`'s serving-still-works claim not independently re-traced through the delivery route in this pass. [verified: the id-shape gate is real and exactly as described; the "still serves its own page" half is consistent with `idFromFilename` doing no shape validation on read]
- `f:xl7d9s` cairn has no bulk media importer; an old image URL left as-is in a migrated body renders unchanged, since the render pipeline passes through anything that is not a `media:` token or recognized image source. Source: no media-importer module exists anywhere under `src/lib` (grep for "media importer"/"bulk import" finds nothing); `src/lib/render/resolve-media.ts:1-5` only visits and rewrites nodes carrying a parsed `media:` token (`parseMediaToken`), leaving every other image `src` untouched as it flows through the rest of the pipeline. [verified]
- `f:my9dgx` Validate migrated entries by starting the dev server against the local double and opening each migrated entry in the admin; a field that fails validation shows its error inline. Source: `docs/extend/migrate-existing-content.md`. [candidate: excluded, documented workflow recommendation, not a single verifiable source-code claim; the admin's inline field-validation behavior is a UI mechanic outside this pass's grep scope; tracked in `docs/internal/docs-friction-log.md`'s open findings per this file's own Provenance note]

## docs/extend/migration-notes.md

- `f:zgdkq7` `migration-notes.md` starts at `0.84.4`, the oldest version among the sites that currently depend on cairn, per `CHANGELOG.md`'s `0.94.0` entry; a version not listed carried no consumer action. Source: `CHANGELOG.md:3449-3450` (the `0.94.0` entry's consumer ranges, 907-life `^0.84.4` the oldest), `CHANGELOG.md:5220` (`## 0.84.4`). [candidate: excluded, which sites depend on cairn and at what range is an operational record dated to the 0.94.0 entry, not code, and the page's start point is the page's own editorial choice]
- `f:2hmbla` Unreleased: `cairn-manifest` now exits 2 on an unrecognized CLI flag instead of silently ignoring it; the "Bin evenness" entry originally covered all four npm bins of that era (the manifest, audit, media-seed, and, before its retirement, doctor bins), each answering `--help`. Source: `src/lib/vite/assemble.ts:4-17` (`USAGE`, argv parsing accepts only `--help`, rejects everything else at exit 2), corroborated by `CHANGELOG.md:298-305`. [verified]
- `f:ofdfyv` Unreleased: migration `0004_login_nonce.sql` must be applied before deploying; an unmigrated `AUTH_DB` is a total login outage since every confirm names the new `nonce_hash` column. No command checks this pre-deploy, since `cairn doctor` runs no D1 check; the failure surfaces only at the first sign-in attempt, post-deploy. Source: `migrations/0004_login_nonce.sql` (file exists), `src/lib/auth/store.ts:20-30` (error text: "cairn: AUTH_DB has no magic_token.nonce_hash column; apply migrations/0004_login_nonce.sql before signing in"), and `docs/reference/cli-cairn-doctor.md` (the eleven-check table, no D1 or auth.store entry). [verified]
- `f:rb5o81` `createContentRoutes` and `createCairnAdmin` (`/sveltekit`) now take one config bag instead of two positional arguments (`runtime, config` becomes `{ runtime, ...config }`). Source: `src/lib/sveltekit/content-routes.ts:174` (`createContentRoutes(config: ContentRoutesConfig)`), `src/lib/sveltekit/cairn-admin.ts:418` (`createCairnAdmin(config: CairnAdminConfig)`), each exactly one parameter. [verified]
- `f:tzr2v7` Six factory-level parameter/load-data bag types renamed: `AuthGuardOptions`→`AuthGuardConfig`, `RendererOptions`→`RendererConfig`, `FieldsetOptions`→`FieldsetConfig`, `DevBackendOptions`→`DevBackendConfig`, `NavLoadData`→`NavData`, `VocabularyLoadData`→`VocabularyData`. Source: `src/lib/sveltekit/guard.ts:34` (`AuthGuardConfig`), `src/lib/render/pipeline.ts:41` (`RendererConfig`), `src/lib/content/fieldset.ts:39` (`FieldsetConfig`), `packages/cairn-cms-dev/src/handle.ts:26` (`DevBackendConfig`), `src/lib/sveltekit/nav-routes.ts:25` (`NavData`), `src/lib/sveltekit/content-routes-settings.ts:86` (`VocabularyData`); none of the old names (`AuthGuardOptions`, `RendererOptions`, `FieldsetOptions`, `DevBackendOptions`, `NavLoadData`, `VocabularyLoadData`) exist anywhere in the tree. [verified]
- `f:9otso4` `OfficeList` (`/admin-toolkit`) is retired; replace with `PageHeader` beside `AdminTable` inside a bare `overflow-hidden card-shell card-shadow` div, with `overflow-x-auto` only on `AdminTable`'s own wrapper. Source: `src/lib/admin-toolkit/index.ts:30,43` (`AdminTable`, `PageHeader` exports; no `OfficeList` export). [verified]
- `f:7o9ch0` The packaged admin sheet also drops `gap-0` and `overflow-x-auto` utility classes since `OfficeList.svelte` was the engine's only user of each. Source: `CHANGELOG.md:281` ("shipped admin sheet also drops `gap-0` and `overflow-x-auto`: `OfficeList.svelte` was the tree's..."). [verified]
- `f:6a339y` Four production sites depend on cairn, each on its own version range at plan-authoring time: `ecxc-ski` (`^0.95.0`), `907-life` (`^0.84.4`), `xcathletes-org` (`^0.96.0` plus `@glw907/cairn-cms-dev` `^0.96.0`), `aksailingclub-org` (`^0.96.0`). Source: `docs/extend/migration-notes.md` "The four sites' upgrade order". [external: consumer-site version ranges live in each site's own repo, not this one; not independently checkable from `cairn-cms` source]
- `f:sjo4cx` `0.96.0` raises the Node floor to `>=24` (from `>=22`), requires `@sveltejs/kit ^2.70` and `svelte ^5.56.10`, and adds `@cloudflare/workers-types` as a peer dependency at `^5`. Source: `package.json:6-7` (`"engines": {"node": ">=24"}`), `:204-208` (`peerDependencies`: `@sveltejs/kit ^2.70`, `svelte ^5.56.10`, `@cloudflare/workers-types ^5`). [verified]
- `f:aj9516` `0.95.0`: a cairn site runs on Cloudflare's Workers Paid plan from its first deploy; `SiteConfig` no longer carries an index signature; `AdminShellData` gained a required `mediaBase` and `EditData` a required `singular`; `DeleteDialog`/`RenameDialog` renamed their `label` prop to `singular`. Source: `packages/create-cairn-site/src/cloudflare/catalogue.mjs:543-544` ("a cairn site needs that plan from its first deploy"), `src/lib/sveltekit/content-routes-shell.ts:89` (`mediaBase: string`, required), `src/lib/components/DeleteDialog.svelte:21`, `RenameDialog.svelte:21` (`singular: string` prop), `src/lib/nav/site-config.ts:80-90` (`SiteConfig`, no index signature). [verified]
- `f:wxe8fc` The Workers Paid plan's monthly price is Cloudflare's figure, restated in the setup command's cost copy and the `0.95.0` changelog entry; read it from Cloudflare's pricing page, never from this container. Source: `packages/create-cairn-site/src/money.mjs:11-12` (`WORKERS_PRICING_URL`, https://developers.cloudflare.com/workers/platform/pricing/). [vendor: link, not a repo fact]
- `f:5zun6z` `0.94.0` is a large consolidated breaking release (event shape, role type, deps types, admin-action refusal channels, admin-toolkit field names, `navLayout` types) whose full checklist lives only in CHANGELOG.md, deliberately not reproduced on this page. Source: `CHANGELOG.md:3443` (`## 0.94.0` section exists). [verified: confirms the section exists and is the sole record; not independently re-verified line by line against each named change]
- `f:cjp7gj` `0.92.0`: `register="stacked"` became the default for `FieldLabel`/`TextField`/`SelectField` (was `register="inline"`); `cairn-audit --rendered` now flags a filled header action stacked above a filled card action. Source: `src/lib/admin-toolkit/FieldLabel.svelte:48` (`register = 'stacked'` default), `src/lib/audit/rules/rendered/one-filled-action.ts:1-20` (a filled header action above a filled card action is "two primaries on one surface by definition"). [verified]
- `f:0d5w2g` `0.91.0`: `badge-ghost` retired from cairn's own tree, replaced by `StatusChip register="quiet"` or `.cairn-chip-quiet`; `cairn-audit`'s `stock-default-hazards` check now errors on `badge-ghost` directly. Source: `src/lib/audit/rules/static/stock-default-hazards.ts:15,115` ("class 'badge-ghost' is the stock daisyUI ghost badge, retired from cairn's own tree"). [verified]
- `f:qcc0zd` `0.87.0`: `routing: 'embedded'` became genuinely enforced; an embedded concept's entries no longer resolve through permalinks, prerender, or the sitemap. Source: `CHANGELOG.md` `## 0.87.0` section: "A `routing: 'embedded'` concept is now genuinely non-routable: its entries no longer resolve through `byPermalink`, prerender through `entries()`, or appear in `site.all()`", cross-checked against current `src/lib/content/concepts.ts:26-39`'s live enforcement of the same routing enum. [verified]
- `f:bjpv4j` `0.86.0`: `navLayout` shipped, giving a site one declarative tree for the whole admin sidebar; `AdminShellData`'s `customNav`/`canManageEditors`/`navLabel` fields moved to a consolidated `nav` field; `navFilter`'s parameter/return types widened from `ResolvedNavItem[]` to `ResolvedLayoutNode[]`. Source: `src/lib/sveltekit/content-routes-shell.ts:57` (`nav: ResolvedNavLayout`), `src/lib/sveltekit/content-routes-context.ts:224-227` (`navFilter` typed over `ResolvedLayoutNode[]`); no `customNav`/`canManageEditors`/`navLabel`/`ResolvedNavItem` anywhere in `src/lib`. [verified]

## docs/extend/organize-your-admin-nav.md

- `f:ys1iq1` `navLayout` is declared on the adapter's `editor` group in `cairn.config.ts`; a site declaring no `navLayout` gets a flat, unsectioned list of concepts and engine screens, resolved through the same function a declared layout uses. Source: `src/lib/content/types.ts:300,418` (`navLayout?: NavLayout` under the adapter's `editor` group), `src/lib/sveltekit/admin-nav.ts:574-578` ("Synthesize the flat zero-config default arrangement for a site that declares no navLayout"). [verified]
- `f:nvpcmk` `navLayout` is an ordered array mixing three node kinds: an engine reference (`{ screen: '<id>' }`), a site entry (`{ label, icon, href }`), and a section (`{ label, children }`, one level deep, no nesting). Source: `src/lib/sveltekit/admin-nav.ts:144` `export type NavLayout = (NavLayoutEntry | NavLayoutEngineRef | NavLayoutSection)[];` (`NavLayoutSection.children` at `:132` typed `(NavLayoutEntry | NavLayoutEngineRef)[]`, i.e. one level deep). [verified]
- `f:jsig0m` An engine reference's `screen` can name a declared concept or one of the fixed utility screens: `media`, `vocabulary`, `nav`, `settings`, `editors`, `help`. Source: `src/lib/sveltekit/admin-nav.ts:100` `const ENGINE_SCREEN_IDS = ['media', 'vocabulary', 'nav', 'settings', 'editors', 'help'] as const;` (the full `navLayout` engine-reference universe, validated by `validateNavLayout`). [verified]
- `f:hsk70g` The navLayout screen-id universe resolved: `navLayout`'s six engine-reference ids (`media`, `vocabulary`, `nav`, `settings`, `editors`, `help`) and the access map's four enforced ids (`media`, `vocabulary`, `nav`, `settings`) are deliberately different sets, not a contradiction between the two pages. `ACCESS_FIXED_SCREENS = ['media', 'vocabulary', 'nav', 'settings']` (`src/lib/sveltekit/admin-nav.ts:280`) is the set `validateAccessComposition` accepts as an access-map key alongside real concept ids; `editors` and `help` are excluded by design, per the doc comment at `admin-nav.ts:276-278`: "`editors` stays owner-only regardless of the map (canReach's own floor) and `help` stays open to any editor capability; neither route reads the map, so the map cannot name them." So a `navLayout` reference to `editors`/`help` is valid nav wiring that the access map can never gate directly (their fixed capability floor is the only enforcement). Source: `src/lib/sveltekit/admin-nav.ts:100,276-280,301-322` `validateAccessComposition`. [verified]
- `f:s4kynf` `icon` on a site entry must be a name from the bundled allowlist documented on `NavIcon`. Source: `src/lib/components/admin-nav-icons.ts` (grep hit for `navLayout`, contains the icon set). [verified: file exists and is the icon-set module referenced]
- `f:ld6xam` The whole `navLayout` tree validates once at runtime composition; a bad icon name, a colliding `href`, an unknown screen id, or an out-of-vocabulary role name fails the build with an actionable, `navLayout`-prefixed message. Source: `src/lib/sveltekit/admin-nav.ts:179` `export function validateNavLayout(...)`, `:79-93` `validateEntry` (throws `navLayout: icon "..."` and `navLayout: href "..." collides...`), `:203-211` `checkRoles` (throws on an out-of-vocabulary role name). [verified]
- `f:yf1rrr` A screen the tree never references still renders, in a trailing fallback group after a divider, in engine order; reference a screen with `hidden: true` to remove it from the sidebar without removing the route. Source: `src/lib/sveltekit/admin-nav.ts:563-568` (the `fallback` array collects every unreferenced, visible engine screen in `engineScreenOrder(opts)`) and `:118-121,530` `NavLayoutEngineRef.hidden` (`if (node.hidden || !engineVisible(...)) return null;`, so the route itself is untouched, only the sidebar entry drops). [verified]
- `f:bvfs3e` Hiding a screen from the nav is never authorization; the route itself must still be denied via the access map for a real deny. Source: `src/lib/sveltekit/admin-nav.ts:118-121,530` (`hidden` removes only the sidebar entry, the route itself is untouched, already `f:yf1rrr`), `src/lib/sveltekit/guard.ts:448-477` (`requireAccess`, the actual enforcement). [verified]
- `f:542r4m` A section's `collapsed: true` sets its starting state only for a visitor with no persisted nav-collapse cookie; once toggled, the cookie carries the whole collapsed set and wins over the declared default in both directions. Source: `src/lib/sveltekit/admin-nav.ts:135-140` (`NavLayoutSection.collapsed` doc comment: "Whether this group starts collapsed for a visitor with no persisted nav-collapse cookie... the `cairn-admin-nav-collapsed` cookie carries the full collapsed set and wins entirely"). [verified]
- `f:qzbfrd` `roles` on a section gates every child at once, on top of each child's own gate, and never widens what a child would otherwise show, only narrows; a site entry can carry its own independent `roles`. Source: `src/lib/sveltekit/admin-nav.ts:519,534,542,604` (a section gates all children via `roleMatches(node.roles, ...)` before rendering any child, each child then re-checked against its own `roles` at `:534`; `NavLayoutEntry.roles` at `:61` is independent per-entry). [verified]
- `f:zyjzeg` `ownerOnly` on a nav site entry is cosmetic only: it hides the link from non-owner sessions but gates nothing server-side; the route itself must still gate. Source: `src/lib/sveltekit/admin-nav.ts:59,69,465-467,533` (`ownerOnlyVisible` gates only the resolved nav tree's visibility). [verified]

## docs/extend/README.md

- `f:ahbsu6` The extend track assumes a working adapter already exists; two starting points build one: "Build a site by hand" (from nothing) or "Add cairn to a SvelteKit app" plus "Define an adapter and schema" (from an existing app). Source: `ls docs/extend/` confirms `build-a-site-by-hand.md`, `add-cairn-to-a-sveltekit-app.md`, and `define-an-adapter-and-schema.md` all exist. [candidate: excluded, describes the extend track's own page-organization scheme, not an engine code fact]
- `f:c1ujrl` The engine defines three export stability tiers: Extension API, Scaffold API (both frozen contracts), and Unstable API (importable today, no promise across minors). Source: `scripts/checks/reference-coverage.mjs:70,91` (the gate's own comment: "Extension and Scaffold API are the frozen contract, Unstable API marks a name..."; the Stability cell "must read 'Extension API', 'Scaffold API', or 'Unstable API'"), enforced via `npm run check:reference`. [verified]
- `f:zm9tp4` The nav fields on `AdminShellData` and `navFilter`'s parameter/return types changed shape at `0.86.0` (the version `navLayout` shipped), and `navLayout`'s own types were renamed at `0.94.0` (`AdminNavEntry` folded into `NavLayoutEntry`), both breaking changes inside the documented Extension API tier despite the stability promise. Source: `src/lib/sveltekit/admin-nav.ts:54` (`NavLayoutEntry`; no `AdminNavEntry` anywhere in `src/lib`), corroborated by the already-verified `f:bjpv4j`/`f:tzr2v7` version facts. [verified]
- `f:3pposq` Vocabulary defined once for the whole extend track: Concept, Adapter, Render, Seam, Island, Holding branch (`cairn/<concept>/<id>`), Manifest, Role/capability (role is a site-defined name; capability is one of `none`/`editor`/`owner`). Source: `src/lib/auth/roles.ts:12` (`Capability = 'owner' | 'editor' | 'none'`), `src/lib/content/pending.ts:6-11` (holding branch `cairn/<concept>/<id>`); the remaining glossary terms (Concept, Adapter, Render, Seam, Island, Manifest) name this repo's own `ConceptConfig`/`CairnAdapter`/`SiteRender`/`IslandRegistry`/`Manifest` types. [verified: structurally, a glossary list rather than one asserted line]
- `f:hr400i` Three reference pages serve the admin track as much as the extend track: `doctor`, `log-events`, `supported-toolchain`. Source: `ls docs/reference/` confirms `doctor.md`, `log-events.md`, `supported-toolchain.md` all exist. [candidate: excluded, states which reference pages the admin and extend tracks share, a doc-arm organization fact rather than a code fact]

## docs/extend/render-safety.md

- `f:zzbzo8` `createRenderer`'s pipeline runs a fixed nine-stage order: parse markdown, rehype-raw, sanitize floor, `build()` dispatch, slug/task-list/highlight, anchor hardening, sink guard, table-scroll wrap, site's own plugins. The `unsafeDisableSanitize` switch turns off exactly the sanitize floor and the sink guard; every other stage still runs. Source: `src/lib/render/pipeline.ts:93-140` `createRenderer` (`rehypePlugins` array built in exactly this order: `rehypeRaw`, `floor` (sanitize, empty when `unsafeDisableSanitize`), `rehypeDispatch`, `rehypeSlug`, `rehypeTaskListA11y`, `rehypeCairnHighlight`, `rehypeAnchorRel`, `rehypeSinkGuard` (also gated on `unsafeDisableSanitize`), `rehypeTableScroll`, then `options.rehypePlugins`). [verified]
- `f:z3a58a` Every renderer `createRenderer` builds runs a `rehype-sanitize` floor by default, seeded from GitHub's own `defaultSchema`, unless a site opts out explicitly; it strips `<script>` tags, inline event-handler attributes, and `javascript:`/`data:` URLs before anything else touches the tree. Source: `src/lib/render/pipeline.ts:111-113` (`floor` built from `rehypeSanitize` unless `options.unsafeDisableSanitize`) and `src/lib/render/sanitize-schema.ts:5,16-17` (`import { defaultSchema } from 'hast-util-sanitize'`; "Starts from hast-util-sanitize's defaultSchema, the GitHub-lineage allowlist that strips scripts, inline event handlers, and javascript:/data: URLs"). [verified]
- `f:2fwybn` `sanitizeSchema` is additive-only: a site receives cairn's default schema and returns an extended one; there is no supported way to remove an entry from the dangerous-protocol strip or re-admit `<script>` through this option. Source: `src/lib/render/sanitize-schema.ts:22-24` ("A site extends the result through `extend`, always starting from this safe base, so it can add to the allowlist but not weaken the core strip") and `:26-63` `buildSanitizeSchema` (the `extend` callback receives the already-built safe `schema` and its return replaces it wholesale, with no separate subtractive option). [verified]
- `f:r7sbt1` `unsafeDisableSanitize` is a code-level (not editor-facing/admin-UI) switch that removes both the pre-dispatch sanitize floor and the post-dispatch sink guard. Source: `src/lib/render/pipeline.ts:54` (`RendererConfig.unsafeDisableSanitize?: boolean`, a config field, not an admin-UI toggle) `:111-113,134` (gates both the floor and `rehypeSinkGuard`). [verified]
- `f:q9yndd` The built sanitize schema additionally admits: cairn's own directive markers (`data-primitive`, `data-slot`, `data-role`, `data-rise`, `data-attr-<key>`) as inert data attributes; `nav`, `details`, `summary`, `figure`, `figcaption` tags; `className` on any element (dropping the default schema's narrower per-tag anchor exception first); `srcSet`/`sizes` on `img`; and the inert `cairn:` URL scheme on `href` (resolved to a live permalink before delivery, or surviving as a literal inert token if it never resolves). `javascript:`/`data:` remain stripped regardless. Source: `src/lib/render/sanitize-schema.ts:13,36-60` (`FIXED_MARKERS`, `tagNames` addition, anchor `className` handling, `imgAttrs`, `protocols.href` gaining `cairn`). [verified]
- `f:gi3keo` `rehypeSinkGuard` runs after anchor hardening, over the fully built tree, and strips any `on*` attribute and inline `style`, scheme-checking every URL-bearing property (`href`, `src`, `srcSet`, and others) against the same safe-scheme list; it does not remove a `build()`-emitted `<script>`, `<style>`, or `iframe srcdoc` element outright. Source: `src/lib/render/sanitize-schema.ts:117-127,180-208` `URL_PROPS`, `rehypeSinkGuard` (doc comment: "It does not remove a build()-emitted raw script, style, or iframe srcdoc element node"). [verified]
- `f:296ar7` Raw HTML in markdown is parsed (`rehype-raw`) rather than escaped to literal text, then cleaned to the same allowlist as directive-authored content; never delivered verbatim, never dropped wholesale. Source: `src/lib/render/pipeline.ts:108-113,116-117` (`rehypeRaw` runs first in `rehypePlugins`, immediately followed by the same `floor` sanitize schema every other node passes through). [verified]
- `f:3yzqup` Every anchor with `target="_blank"` gets its `rel` forced (`noopener noreferrer` by default, configurable or disabled) after highlighting and ahead of the sink guard. Source: `src/lib/render/pipeline.ts:114,131,134` (`options.anchorRel ?? 'noopener noreferrer'`; `rehypeAnchorRel` pushed after `rehypeCairnHighlight`, before `rehypeSinkGuard`) and `src/lib/render/sanitize-schema.ts:71-81` `rehypeAnchorRel`. [verified]
- `f:21by9u` A site's own registered `build()` component can still bypass all render-safety guarantees by rendering trusted literal markup outside the sanitized tree (e.g., an SSR fetch injected directly); the page's guarantees stop at the boundary of what `createRenderer` actually produced. Source: `src/lib/render/sanitize-schema.ts:189-191` (`rehypeSinkGuard`'s doc comment: "A build() that emits those is running site-developer code, and author markdown is cleaned by the pre-dispatch floor"). [verified]

## docs/extend/restrict-admin-access.md

- `f:p1xmp5` Capability (`owner`/`editor`/`none`) is the floor cairn already enforces, resolved from the declared role vocabulary; the access map sits on top and only narrows, never widens, what a capability already permits. Source: `src/lib/auth/roles.ts:12` (`Capability`), `src/lib/auth/access.ts:156-175` (`canReach`: capability checked before the map; `'none'` always refused, `'owner'` always admitted, the map only narrows an `'editor'` capability's reach). [verified]
- `f:grxpu5` `defineAccess(roles, map)` validates a map's role names against the implicit `{ owner: 'owner', editor: 'editor' }` pair when `roles` is `undefined`. Source: `src/lib/auth/roles.ts:24` `export const DEFAULT_ROLES: { owner: 'owner'; editor: 'editor' } = { owner: 'owner', editor: 'editor' };` and `src/lib/auth/access.ts:75` `const vocabulary = new Set(Object.keys(roles ?? DEFAULT_ROLES));`. [verified]
- `f:0qb73i` An access-map key is either an engine screen id (a declared concept, or `media`/`vocabulary`/`nav`/`settings`) or an `/admin`-prefixed route path; its value is the admitted role names, matched against the site's `defineRoles` vocabulary. Owner capability always reaches every mapped target regardless of the role list. Source: `src/lib/auth/access.ts:160-161` (owner bypass), `src/lib/sveltekit/admin-nav.ts:280` (`ACCESS_FIXED_SCREENS = ['media','vocabulary','nav','settings']`). [verified]
- `f:iwf4nu` The same `access` value must be passed to both the adapter's own `access` member and `createAuthGuard`'s `access` option; the guard attaches the map to `locals.cairnAccess` on every request. Source: `src/lib/sveltekit/guard.ts:346,366` `event.locals.cairnAccess = access ?? {};` (both branches of the guard). [verified]
- `f:54d3ui` `requireAccess` checks `event.route.id` by default (not the request URL), since a parameterized or catch-all route's URL is attacker-chosen while the route id is not. Source: `src/lib/sveltekit/guard.ts:448-477` `requireAccess` (`target` "defaults to `event.route.id`, never [the URL]"; `const resolvedTarget = target ?? targetFromRouteId(event.route.id);`). [verified]
- `f:zo034s` A target absent from the map behaves oppositely for a route vs. an engine screen: an unmapped engine screen stays open to any editor-capability session (zero-config default), but a route calling `requireAccess` with no map entry for its path refuses every session, owner included, treated as a misconfiguration. Source: `src/lib/sveltekit/guard.ts:461-464` (`requireAccess`: "The unmatched case... 403s every session, owner included"), `src/lib/auth/access.ts:173-174` (`canReach`: `admitted === undefined ? true`, an unmapped engine screen stays open). [verified]
- `f:5pyozi` A page's `load` gating a read never gates the POST to its own form action; SvelteKit dispatches a matched form action with no ancestor `load` run first, so an unguarded action is reachable even behind a guarded page. Source: `docs/extend/restrict-admin-access.md`. [external: SvelteKit (framework routing/actions behavior, not cairn source; the seam it justifies, `requireAccess` gating each action explicitly rather than relying on page `load`, is cairn's own design response)]
- `f:iewhzh` Restricting the `media` screen also restricts the inline image picker inside every image-bearing concept's editor, since it calls the same access-gated endpoint. Source: `src/lib/sveltekit/content-routes-media-library.ts:67`, `content-routes-media-metadata.ts:164,222,300,424,497`, and `content-routes-media-delete.ts:118` all call `requireEngineAccess(runtime.access, editor, 'media')`; `src/lib/components/MediaInsertPopover.svelte` (the editor's inline insert popover) uploads/reads through these same media routes. [verified]
- `f:smbsa6` `ownerOnly` on `createSectionAction`/`createAdminAction` requires owner capability in addition to the access map's own rule for the target, never in place of it; it only ever narrows. Source: `src/lib/sveltekit/admin-action.ts:120-129` (`authorizeAdminTarget`: three ordered gates, `ownerOnly` checked last, on top of the map's own rule). [verified]

## docs/extend/reuse-content-across-entries.md

- `f:ojydmz` `fragments` is a reserved concept key; declaring it turns on the `::include` directive. Source: `src/lib/content/concepts.ts:170` (`if (id === FRAGMENTS_CONCEPT_ID && config.routing !== 'embedded')`). [verified]
- `f:7wiuwr` A `fragments` concept must use `routing: 'embedded'`; `defineConcept` throws otherwise. Source: `src/lib/content/concepts.ts:170-173` (throws `"cairn: concept \"fragments\" requires routing: 'embedded' (the include directive resolves against it)"`). [verified]
- `f:60zw5n` `fragments` needs its own glob in the content index (`src/lib/content.ts`) the same as any other declared concept; `createSiteIndexes` throws at build time if it's missing. Source: `src/lib/delivery/site-indexes.ts:54-58` (`throw new Error(\`cairn: concept "${descriptor.id}" has no glob passed; pass its import.meta.glob...\`)` when a declared concept, `fragments` included, has no matching key in the passed globs record). [verified]
- `f:i6zyab` If a site also uses the `cairnManifest` Vite plugin, `fragments` must be added to that plugin's own `content` option too; a concept missing there fails silently (no build error), so an entry that includes a fragment would find it missing from the manifest with no explanation. Source: `src/lib/vite/internal.ts:36-37` (`CairnManifestOptions.content: Record<string, string>`, per-concept globs keyed by concept id) and `src/lib/delivery/manifest.ts:18-22` `buildSiteManifest`: `const record = globRecord[descriptor.id] ?? {};`, so a concept id absent from the glob record degrades to an empty file set with no throw, unlike `createSiteIndexes` (verified above), which throws on the same omission. [verified]
- `f:0w7xeu` The include directive syntax is `::include{fragment="<id>"}`; the editor's "Include a fragment" insert-palette control writes this for the author. Source: `src/lib/render/resolve-include.ts:122` (`Resolve \`::include{fragment="<id>"}\` leaf directives`) and `src/lib/components/editor-include.ts:96-97` (`includeFragmentId`, the same `fragment="id"` shape). [verified]
- `f:zv3q45` The fragment resolver type is `FragmentResolve`, the type `SiteRender`'s `resolveFragment` option takes; a build supplies one backed by `createFragmentResolver`. Source: `src/lib/delivery/site-resolver.ts:207` `export function createFragmentResolver(site: SiteResolver): FragmentResolve` and `src/lib/content/types.ts:218` (`resolveFragment?: ... FragmentResolve`). [verified]
- `f:xlgfqm` A dangling `::include` (an id naming no real fragment) fails the build the same way a dangling `cairn:` link does. Source: `src/lib/render/resolve-include.ts:36-38,144` `FragmentResolve`'s doc comment: "a resolver that throws is the build backstop, exactly like `LinkResolve` and `MediaResolve`"; `const body = resolve(id); // may throw (build backstop); propagates out of render`. [verified]
- `f:k84qwy` A site's own `rendering.render` function must forward `resolveFragment` alongside `resolve` and `resolveMedia`; omitting it means `::include` directives publish as literal directive text with no error. Source: `src/lib/render/resolve-include.ts:129-132` `remarkResolveIncludes` ("With no resolver supplied every include directive is left untouched for the stamp step's literal-prose restore"; `if (!resolve) return;`). [verified]
- `f:82eo9p` A fragment cannot include another fragment: resolution runs exactly one pass deep, and the engine refuses the save outright rather than shipping a directive that can never resolve. Source: `src/lib/render/resolve-include.ts:12-20` (one-pass-only doc comment, "the defense-in-depth backstop for the engine's save-time nesting rejection") and `src/lib/sveltekit/content-routes-entry-write.ts:187-193` (`// A fragment can never include another fragment...`; `return fail(400, saveRefusal("A fragment can't include another fragment.", body));`). [verified]
- `f:3vndvj` Deleting a fragment still included somewhere is refused, using the same delete-guard shape as a `reference` field: every other entry's recorded `includes` edges are checked against the fragment's id. Source: `src/lib/sveltekit/content-routes-entry-destructive.ts:111-116` (the fragment delete guard reads `inboundIncludes`), `src/lib/content/manifest.ts:473` (`inboundIncludes`). [verified]

## docs/extend/rotate-the-github-app-key.md

- `f:ixr3ny` A GitHub App can hold more than one private key at once; generating a new key does not invalidate the old one, and the App keeps signing with whichever key a caller presents until a key is deleted by hand. Source: `docs/extend/rotate-the-github-app-key.md` (GitHub platform behavior, linked to GitHub's own docs). [external: GitHub]
- `f:i4fg3o` cairn's Worker reads the GitHub App key as `GITHUB_APP_PRIVATE_KEY_B64`, a base64 encoding of the PEM with no line breaks. Source: `src/lib/env.ts:31` (same as `f:vpieos`). [verified]
- `f:sszb7b` If something goes wrong before the old key is deleted, re-pushing the old key's base64 value with the same `wrangler secret put` command restores signing with it immediately. Source: `docs/extend/rotate-the-github-app-key.md`. [external: GitHub/Cloudflare (composes the already-verified GitHub fact that an undeleted key keeps signing, with Wrangler's own secret-write-takes-immediate-effect behavior; neither half is cairn source)]
- `f:lg2ae8` GitHub does not allow restoring a deleted key; if the old key is already deleted and the new one fails, generate a third key instead. Source: `docs/extend/rotate-the-github-app-key.md` (GitHub platform behavior). [external: GitHub]

## docs/extend/security-model.md

- `f:f39xqq` cairn's built-in magic-link path: a sign-in token lives 10 minutes, a session lives 30 days, and a repeat sign-in request from the same address is throttled to once per minute; all three are named constants an adapter cannot loosen. Source: `src/lib/auth/crypto.ts:64` `export const TOKEN_TTL_MS = 10 * 60 * 1000;` and `:67` `export const SESSION_TTL_MS = 30 * 24 * 60 * 60 * 1000;`. [verified]
- `f:s32y4a` The magic-link request path is non-enumerating: an address not on the roster gets the same `{ outcome: 'sent' }` response a real editor's address gets, except a *repeat* request inside the one-minute cooldown returns a distinct `throttled` status, which does reveal roster membership. Source: `src/lib/sveltekit/auth-routes.ts:50-56,210,236,259` (`{ outcome: 'sent'; sent: true }` for both the non-editor neutral answer (`:210`) and a normal send (`:259`); `{ outcome: 'throttled'; sent: false }` for a per-email cooldown repeat (`:236`), doc comment: "the identical `{ outcome: 'sent', sent: true }`, so the common case never leaks allowlist [membership]"). [verified]
- `f:6lmusm` Sign-in binds to the requesting browser via a `cairn_login_pending` cookie (`HttpOnly`, `SameSite=Lax`, `__Host-` prefixed on https, one hour long); the confirm action compares the cookie's nonce hash against the token row's `nonce_hash` inside the same atomic `DELETE` that consumes the token. Source: `docs/extend/security-model.md`, cross-checked against `src/lib/auth/store.ts` (`nonce_hash` column usage, e.g. `:182` `WHERE token_hash = ? AND expires_at > ? AND (nonce_hash IS NULL OR nonce_hash = ?)`). [verified: the DELETE-time nonce_hash comparison matches store.ts's query shape]
- `f:5iqvmt` A confirm from a browser holding no cookie passes SQL `NULL`; a row with no binding (`nonce_hash IS NULL`) still matches and signs the editor in. Source: `src/lib/auth/store.ts:182` (`nonce_hash IS NULL OR nonce_hash = ?`). [verified]
- `f:91dwrk` A throttled re-request rebinds the live token to the browser that just asked (last-requester-wins); the rebind skips an expired row or an unbound row. Source: `src/lib/auth/store.ts:141-142` (`UPDATE magic_token SET nonce_hash = ? WHERE email = ? AND expires_at > ? AND nonce_hash IS NOT NULL AND nonce_hash <> ?`). [verified]
- `f:9uhscf` `NO_PENDING_REQUEST_ERROR` is exported as the constant `'no-pending-request'` from `@glw907/cairn-cms/sveltekit`, naming the distinct error code for a confirm from a bound row with a missing/mismatched cookie. Source: `src/lib/sveltekit/auth-error-codes.ts:23` `export const NO_PENDING_REQUEST_ERROR = 'no-pending-request';`. [verified]
- `f:jud805` The engine writes the nonce hash to `magic_token.nonce_hash`, added by `migrations/0004_login_nonce.sql`; no command checks this pre-deploy today (the retired JS doctor's `auth.store` check left with it, and `cairn doctor` runs no D1 check), so an unmigrated `AUTH_DB` surfaces the failure only at the first sign-in attempt. Source: `migrations/0004_login_nonce.sql` (file present), `src/lib/auth/store.ts:20-30` (error naming that exact migration path), and `docs/reference/cli-cairn-doctor.md` (the eleven-check table, no D1 or auth.store entry). [verified]
- `f:wzcgs3` The session cookie carries the `__Host-` prefix on every https deploy (requiring `Secure`, `Path=/`, forbidding `Domain`); local http development drops the prefix since `__Host-` requires `Secure` unconditionally. Source: `src/lib/auth/crypto.ts:5-15` `sessionCookieName` (doc comment: "On https the cookie is Secure and takes the __Host- prefix, which binds it to the origin (the browser enforces Secure, Path=/, and no Domain). On local http dev the prefix is dropped, since __Host- requires Secure"). [verified]
- `f:njh87y` The CSRF double-submit cookie shares the session cookie's `__Host-`/`Secure` derivation rule, and both cookie forms (bare and `__Host-`, session and CSRF) are deleted on logout regardless of which name the current request's own derivation would produce. Source: `src/lib/auth/crypto.ts:20-28` `csrfCookieName` (doc comment: "mirroring sessionCookieName's prefix shape... Both names now also share their `secure` INPUT: `csrf.ts`'s `csrfSecure` is the one derivation every session- and CSRF-cookie call site feeds, so the two cookies cannot resolve different `secure` values on the same request"). [verified: the shared-derivation half is confirmed in source; the logout dual-name deletion behavior itself was not located in `src/lib` this pass]
- `f:9ik061` The CSRF cookie's full attribute set: `HttpOnly`, `Path=/`, `SameSite=Lax` set explicitly, and a `Max-Age` matching the session cookie's 30-day lifetime; it rotates at exactly two moments on the magic-link path, a successful login and a logout. Source: `src/lib/sveltekit/csrf.ts:109-127` (`issueCsrfToken`: `path: '/'`, `httpOnly: true`, `sameSite: 'lax'`, `maxAge` from `SESSION_TTL_MS`; doc comment: "a login... and a logout (which deletes it). Nothing else ever changes the value."). [verified]
- `f:fhit7f` `createAuthGuard`'s `identity` option replaces the entire built-in sign-in path (no token minted, no session created, no session cookie set); `identity.resolve` reads the gate's own proof of identity and the guard looks the proven email up against the roster, exactly as it looks up a magic-link session's email. Source: `src/lib/sveltekit/guard.ts:285-293` (the `identity` branch runs in place of magic-link session resolution on every non-public admin path) combined with the already-verified fact above that the guard performs no bootstrap write and sets no session cookie under `identity`. [verified]
- `f:2glcaf` Under `identity`, the effective session lifetime moves to the gate (e.g. Cloudflare Access, default 24 hours, configurable up to a month); cairn's own 30-day session constant no longer applies. Source: `src/lib/sveltekit/guard.ts:285-293` (the `identity` branch sets no session cookie and mints no session, already `f:fhit7f`); Cloudflare Access's own 24-hour default and configurable ceiling are external Cloudflare facts. [verified]
- `f:diro7m` `hasSession` on a `guard.refused` CSRF-rejection log record is structurally always `false` under `identity`, since the guard never sets a session cookie in that mode. Source: `src/lib/sveltekit/guard.ts:271-280` (`hasSession = event.cookies.get(sessionCookieName(...)) !== undefined`, logged on the `csrf` reason) combined with the already-verified fact that `identity` mode sets no session cookie, so the cookie read here always misses. [verified]
- `f:7qqhda` The guard's fixed request order: (1) dev-backend tripwire, (2) non-admin origin check, (3) HTTPS help page, (4) bindings check, (5) CSRF check, (6) session resolve (or identity resolve). Every refusing step except step 6 on the magic-link path logs a named `guard.refused` reason: `dev_backend_in_prod`, `origin`, `https`, `bindings`, `csrf`. Source: `src/lib/sveltekit/guard.ts:178-283` `createAuthGuard`'s `handle` function, in exactly this order: `log.error('guard.refused', { reason: 'dev_backend_in_prod' ...})` (:197), the non-admin origin check with `reason: 'origin'` (:205), the https help page with `reason: 'https'` (:218), the bindings check with `reason: 'bindings'` (:228-232), the CSRF check with `reason: 'csrf'` (:274-280), then session/identity resolve. [verified]
- `f:gh73p5` `CAIRN_DEV_BACKEND` is only honored as `1` or boolean `true` in either `platform.env` or `process.env`; a `CAIRN_DEV_BACKEND=true` string in one context reads as unset (only exact truthy forms count). Source: `src/lib/dev-flag.ts:27-29` `export function isDevBackendFlagSet(raw: unknown): boolean { return raw === '1' || raw === true; }` (doc comment: "`'true'`, `'yes'`, and `'0'` are all unset here, deliberately") and `src/lib/sveltekit/guard.ts:193-196` (reads both `event.platform?.env?.[CAIRN_DEV_BACKEND_FLAG]` and `process.env?.[CAIRN_DEV_BACKEND_FLAG]`). [verified]
- `f:gncd64` A site itself sets `csrf: { checkOrigin: false }` in `svelte.config.js` (SvelteKit's own build-time config; `createAuthGuard` does not set this at runtime), and `createAuthGuard` enforces its own Origin-independent double-submit CSRF check on every unsafe `/admin` form POST instead, restoring an equivalent strict Origin check for every non-admin route. Source: `src/lib/sveltekit/guard.ts:203-210` ("restore the framework's strict Origin check the consumer disabled when they set checkOrigin: false to hand cairn the admin CSRF authority"), `src/lib/sveltekit/csrf.ts` (the admin double-submit CSRF implementation). [verified]
- `f:vqh4a9` `canReach` is the one function deciding both route enforcement and nav visibility for the access map, so they can never drift apart; an access map narrows only the targets it names, and an unmapped screen/concept stays reachable to any editor-capability session (the roster screen `editors` stays owner-only regardless of the map). Source: `src/lib/auth/access.ts:1-4` (module comment: "canReach is the one authority function... so route enforcement and sidebar visibility can never drift apart"), `:163-165` (`editors` stays owner-only regardless of the map). [verified]
- `f:4q8kin` `authorizeAdminTarget` (used by `createSectionAction` and `createAdminAction`'s opt-in `access` option) is fail-closed at three ordered gates: (1) no rule for the target refuses, (2) a session whose role is absent from an existing rule refuses (except owner capability, always admitted), (3) `ownerOnly` against a non-owner session refuses. Source: `src/lib/sveltekit/admin-action.ts:120-129` `authorizeAdminTarget`: `if (!hasAccessRule(access, opts.target)) return { outcome: 'no-rule' }; if (!canReach(access, editor, opts.target)) return { outcome: 'not-admitted' }; if (opts.ownerOnly && editor.capability !== 'owner') return { outcome: 'not-owner' };`. [verified]
- `f:ubuj1w` Every admin response carries `X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `frame-ancestors 'none'`, `Referrer-Policy: no-referrer` (scoped to `/admin` only, never site-wide), a `Permissions-Policy` denying camera/microphone/geolocation, `Strict-Transport-Security` (opt-in subdomain pinning), and `Cache-Control: private, no-store`. Source: `src/lib/sveltekit/admin-response.ts:35-46` (`headers.set('X-Content-Type-Options', 'nosniff')`, `'X-Frame-Options', 'DENY'`, `'Content-Security-Policy', "frame-ancestors 'none'"`, `'Referrer-Policy', 'no-referrer'`, `'Strict-Transport-Security', opts.includeSubDomains ? '...includeSubDomains' : 'max-age=63072000'`, `'Permissions-Policy', 'camera=(), microphone=(), geolocation=()'`, `'Cache-Control', 'private, no-store'`). [verified]
- `f:yzbvk4` `/admin` carries no full Content-Security-Policy by design; the XSS surface that matters (arbitrary visitor content) is governed on the public render path instead (render-safety.md). Source: `src/lib/sveltekit/admin-response.ts:35-46` (admin responses set only `frame-ancestors 'none'`, no `script-src`/`default-src`, already `f:ubuj1w`), `src/lib/render/sanitize-schema.ts` (the actual XSS defense on the public render path, already `f:z3a58a`). [verified]
- `f:kkp5bi` Publishing authenticates to GitHub as the site's own GitHub App (never a personal account); the private key lives as a single Worker secret, decoded and used to sign a short-lived installation token per request, never written to disk in the deployed runtime and never logged. Source: `src/lib/github/signing.ts:1-5,49,67` (in-Worker RS256 JWT signing, exchanged for a short-lived installation token per request), `src/lib/env.ts:31` (single Worker secret). [verified]

## docs/extend/share-a-draft-preview.md

- `f:6gmzt1` Draft preview is opt-in per site: the affordance ships on every upgraded edit screen, but nothing happens until the migration is applied and the route is mounted. Source: `docs/extend/share-a-draft-preview.md`; consistent with `migrations/0003_preview.sql` existing as a separate, not-auto-applied migration (verified below) and `loadPreview` throwing `config.bindings-missing`/`table_missing` when the migration or route is absent (`src/lib/sveltekit/preview.ts:449-465`). [verified]
- `f:049lo2` Draft preview requires `migrations/0003_preview.sql`, which adds one table, `preview_tokens`, to the existing `AUTH_DB`: a hashed token, the shared entry, the minting editor, and an expiry. Source: `migrations/0003_preview.sql` (file present in `migrations/`). [verified: file exists; table contents not opened but filename and existence confirmed]
- `f:rr00qr` Until the preview migration runs, both the mint and revoke actions answer with an actionable failure naming the migration, rather than a raw database error. Source: `src/lib/sveltekit/content-routes-preview.ts:28-33,71,122` `missingPreviewTableFailure()` ("The preview_tokens table is missing. Apply migrations/0003_preview.sql to AUTH_DB, then try again."), returned by both the mint (`:71`) and revoke (`:122`) actions on `isMissingTableError(err)`. [verified]
- `f:gvim4v` `loadPreview` renders a minted link through the same composition an entry's public page already uses, and must be mounted inside the same route group as the entry pages so it inherits the site's layout/stylesheets/chrome; the route must set `export const prerender = false` since a preview link is a bearer credential. Source: `src/lib/sveltekit/preview.ts:385` (renders "through the site's own public composition"), `:414-415,442-443` (throws unless `export const prerender = false`: "A preview link is a bearer credential; prerendering it would ship a token in a static asset"). [verified]
- `f:xssf06` `PreviewBanner` exposes four CSS custom properties for palette override (`--cairn-preview-bg`, `--cairn-preview-fg`, `--cairn-preview-border`, `--cairn-preview-link`), read via `var(--x, <fallback>)` with no scoped-element declaration, so a site's own `:root`/`[data-theme]` declaration always wins with no specificity fight. Source: `src/lib/components/PreviewBanner.svelte:25-32,90-97` (four custom properties, `var(--x, <fallback>)`, no scoped-element declaration). [verified]
- `f:afkvsb` `loadPreview` reads no cookie and touches neither `locals.cairnEditor` nor `locals.cairnAccess`; the token alone is the credential, so anyone holding the link can view the draft with no sign-in. Source: `src/lib/sveltekit/preview.ts` `loadPreview`'s doc comment: "No cookie is read or set, and `locals.cairnEditor`/`locals.cairnAccess` are never read: the token itself is the sole credential, an unauthenticated read on one entry". [verified]
- `f:zke3iw` The preview response sets `cache-control: private, no-store`, `x-robots-tag: noindex, nofollow`, and locked-down `referrer-policy`/`x-frame-options`, on every response including refusals. Source: `src/lib/sveltekit/preview.ts:248-257` `PREVIEW_HEADERS` (`'x-robots-tag': 'noindex, nofollow'`, `'cache-control': 'private, no-store'`, `'referrer-policy': 'no-referrer'`, `'x-content-type-options': 'nosniff'`, `'x-frame-options': 'DENY'`), set via `event.setHeaders(PREVIEW_HEADERS)` at the top of `loadPreview` before any refusal branch runs. [verified]
- `f:l4waby` While a pending branch exists, a preview renders with `preview.state: 'draft'`; once the branch is gone (publish or discard both delete it) and a live file exists on the default branch, the page renders "this preview has ended" linking the real permalink; every other failure (malformed token, expired row, concept/id mismatch, gone branch with nothing published) answers an identical uninformative 404. Source: `src/lib/sveltekit/preview.ts:497-528` `loadPreview` (`state: 'draft'` while the branch resolves; `state: 'published'` with `published: { permalink }` once the branch read misses but the default-branch entry exists) and `:262,477-479` `rejectPreview` (every one of `unknown`/`expired`/`branch_gone`/`row_invalid`/`draft_invalid`/`table_missing` throws the identical `error(404, NOT_FOUND_MESSAGE)`). The doc's "this preview has ended" is the `PreviewBanner`'s UI copy for `state: 'published'`, not a literal enum value (the actual state literal is `'published'`, not `'ended'`). [verified]
- `f:pimnjy` `previewMintAction` refuses with `fail(400)` when the entry carries no pending draft to share; `previewRevokeAction` deletes every outstanding link for the entry in one call and is idempotent (revoking nothing still succeeds with a zero count). Source: `src/lib/sveltekit/content-routes-preview.ts:13-27,55-83` (`previewMintAction`'s `no-draft` case answers `fail(400, ...)`), `:111-135` (`previewRevokeAction` returns `{ count: result.count }` with no special case for zero). [verified]
- `f:a7yrhq` Renaming, deleting, or discarding a never-published entry each clear its outstanding preview rows as part of their own cascade; publishing deliberately leaves the rows in place so `loadPreview` can answer a stale link with "this preview has ended" instead of a bare 404. Source: `src/lib/sveltekit/content-routes-entry-destructive.ts:167,205,407` `clearPreviewTokens(event, concept, id)` called from the delete path (`:167,205`) and the rename path (`:407`, with the comment explaining the id-reuse collision it closes); the publish path's own preview-row handling was not located in this pass, consistent with the doc's claim that publish leaves the rows in place (`loadPreview`'s `state: 'published'` branch at `preview.ts:497-511` depends on the row still existing after publish). [verified]
- `f:cnz554` A parent layout load runs on every route beneath it, the preview route included, with no session and no route-specific gate, so a privileged value a group layout returns reaches an anonymous preview holder exactly as it reaches a public visitor; branch on `event.route.id` or mount the preview route in a sibling route group to avoid leaking it. Source: `docs/extend/share-a-draft-preview.md` (general SvelteKit load-composition behavior). [external: SvelteKit (framework layout-load-inheritance behavior, not cairn source; the mitigation it recommends is a site-authoring practice, not an engine mechanism)]

## docs/extend/sign-in-through-your-organization.md

- `f:v5ndhs` Configuring `createAuthGuard`'s `identity` option replaces the whole magic-link path; there is no partial adoption where some editors use magic links and others go through the gate. Source: `src/lib/sveltekit/guard.ts:285-293` (same `identity` branch as `f:fhit7f`, runs in place of magic-link resolution on every non-public admin path, with no partial mode). [verified]
- `f:gs23fb` Every Access application carries an AUD tag distinct from the application's id; the verifier must validate against the AUD tag, found on the application's Overview tab. Source: `docs/extend/sign-in-through-your-organization.md` (Cloudflare platform fact, external to this repo). [external: Cloudflare]
- `f:g0206o` Before enabling `identity`, every roster row's email must match the primary address the IdP will assert (not an alias), fixable from the roster screen or via `wrangler d1 execute` against `AUTH_DB`; skipping this locks out affected editors instantly. Source: `docs/extend/sign-in-through-your-organization.md`; consistent with `src/lib/auth/store.ts:36-37` `normalizeEmail` (trim + lowercase) being the sole matching rule the roster lookup applies to a resolved identity's email, with no alias-resolution step. [verified: the exact-match (modulo case/whitespace) lookup confirms why an alias would not match; the `wrangler d1 execute` fix path itself is operational advice, not a source-code claim]
- `f:emrebl` Enable only a login method that proves control of the asserted address (a directory connection or a one-time PIN); never a social IdP or generic OIDC whose `email` claim the end user can edit, since cairn cannot distinguish a directory-asserted address from a self-asserted one. Source: `docs/extend/sign-in-through-your-organization.md`, consistent with `docs/extend/security-model.md`'s "Which login methods are safe" cross-reference. [candidate: excluded, operational advice about which third-party IdP or login method to trust; cairn's own code has no visibility into or opinion on the IdP a site's gate chooses]
- `f:k40l86` The Access application and the cairn roster are two independent admission lists that never reconcile automatically: the application decides who reaches `/admin` at all, the roster decides who among those may edit. Source: `src/lib/sveltekit/guard.ts:285-293` (the guard's roster lookup against the identity gate's proven email, already `f:fhit7f`); the Access application's own admission decision is external Cloudflare configuration outside cairn's code. [verified: the roster-independent half traces to code; the Access-application half is external Cloudflare configuration]
- `f:4olp4u` The free Cloudflare Zero Trust plan covers 50 users, counted as editors authenticating through Access, not cairn's own roster size. Source: `docs/extend/sign-in-through-your-organization.md` (Cloudflare platform fact). [external: Cloudflare]
- `f:qhmydf` The first owner must be seeded before enabling `identity`, since nothing in identity mode can bootstrap an owner (the guard never reaches the routes' bootstrap config); seed via `create-cairn-site`'s own method or `wrangler d1 execute` against `AUTH_DB`. Source: `src/lib/sveltekit/guard.ts:287-293` (the `identity` branch never reaches `auth-routes.ts`), `src/lib/sveltekit/auth-routes.ts:44,201-203` (`bootstrapOwner` lives only in the magic-link routes). [verified]
- `f:zt0oag` No Cloudflare cache rule may match `/admin` under an identity gate, since a cached response would serve one editor's page to the next matching request regardless of the gate. Source: `docs/extend/sign-in-through-your-organization.md`. [external: Cloudflare (cache-rule configuration guidance; cairn's own `Cache-Control: private, no-store` on every admin response, verified above, is the engine-side half of this defense, but a Cloudflare cache rule can override response headers, so the platform-side exclusion is still necessary)]
- `f:vnm1p5` The Access application must cover `/admin`, every path beneath it, and `/admin/__data.json` (SvelteKit's data-only fetch); it must NOT cover `/preview/<token>`, since a preview link's reviewer has no reason to be admitted to `/admin`. Source: `docs/extend/sign-in-through-your-organization.md`. [external: Cloudflare (Access application path-coverage configuration; the `/preview/<token>` exclusion is a cairn seam-design recommendation resting on `loadPreview`'s already-verified no-cookie/no-session behavior, not itself a platform fact)]
- `f:0gltjq` Access application CORS settings must stay off, since enabling them can make the `X-Cairn-CSRF` header settable cross-origin, collapsing the CSRF witness the admin guard relies on. Source: `docs/extend/sign-in-through-your-organization.md`; header name confirmed at `src/lib/components/client-ingest.ts:237` and other action call sites (`'X-Cairn-CSRF': opts.csrf`). [verified: the `X-Cairn-CSRF` header this warning protects is real cairn wiring, though the Access CORS mechanic itself is an external Cloudflare platform behavior]
- `f:51uyqr` Setting `workers_dev: false` alone does not close a preview-URL exposure: Wrangler defaults `preview_urls` to `workers_dev`'s own value, but a site that explicitly set `preview_urls: true`, or has previews toggled on in the dashboard on an older Wrangler, still serves `/admin` on an ungated `<alias>-<name>.<subdomain>.workers.dev` hostname; `preview_urls: false` must also be set. Source: `docs/extend/sign-in-through-your-organization.md`, consistent with `docs/extend/migration-notes.md`'s Unreleased note. [candidate: excluded, pure Wrangler/Cloudflare configuration behavior (workers_dev/preview_urls interaction) with no cairn engine or tool code touchpoint; consistent with the doc's own "no tool checks this yet" admission]
- `f:iaqcq6` The npm-era doctor's `--probe` flag and its two arms (a login probe under `identity`, and a `workers.dev` exposure check) retired with the rest of that bin; no tool checks either arm until a later 1.x release of the `cairn` CLI. A site checks both by hand: an unauthenticated `GET` of the primary hostname's `/admin/login`, confirming a redirect lands on the gate's own hostname, and the same `GET` against `<worker-name>.<subdomain>.workers.dev/admin` and the preview alias, watching for any Worker-served response there. Source: absence of any `workers_dev`, `preview_urls`, or login-probe check across every file under `tool/internal/doctor/` and `tool/cmd/cairn/doctor.go` (confirmed by grep); `tool/internal/spine/condition.go:44` declares `admin.login-probe-failed`, but no check emits it. [verified: an absence proof, confirmed by grep across the current Go doctor's check set; "a later 1.x release" is roadmap intent, not code]
- `f:lyaf6p` The verifier code is application code the site writes and owns; cairn ships no OIDC client and does not depend on `jose`, so nothing in cairn typechecks or verifies the recipe's logic. Source: `package.json` (grep for `jose` in dependencies/devDependencies/peerDependencies finds no entry). [verified]
- `f:42fdqq` The Access-token verifier must check `Cf-Access-Jwt-Assertion` header only, never the `CF_Authorization` cookie, since the cookie is a browser-attached bearer value replayable on any hostname reaching the same Worker and settable by hand. Source: `docs/extend/sign-in-through-your-organization.md`. [external: Cloudflare (Access JWT delivery mechanics; the verifier is site-authored application code cairn does not ship, per the adjacent verified fact)]
- `f:zas1dk` The verifier must check `payload.type === 'app'` (not the team-scoped `org` session token) and must refuse a token with no `email` claim (a service-token request verifies with `common_name` and an empty `sub`/no `email`). Source: `docs/extend/sign-in-through-your-organization.md`. [external: Cloudflare]
- `f:q0icwk` On Cloudflare Access, a user-initiated logout clears the browser's authorization cookie immediately, and Access stops accepting previously issued tokens within 20 to 30 seconds; an administrator's manual token revocation has a lockout of up to a minute. Source: `docs/extend/sign-in-through-your-organization.md` (Cloudflare's own documented timing, linked). [external: Cloudflare]
- `f:icjjtd` `identity`, `IdentityResolver`, `ResolvedIdentity`, and `IdentityRefusal` are Unstable API tier: not yet committed across a minor version, since no site has adopted it yet; the first production consumer promotes it to the frozen Extension API tier. Source: `docs/reference/sveltekit.md:2015-2018` (Types table: `IdentityResolver`/`ResolvedIdentity`/`IdentityRefusal` all "Unstable API"). [candidate: excluded, the tier is declared only on the reference page; `src/lib/sveltekit/guard.ts:76-110` carries no stability tag and `scripts/checks/reference-coverage.mjs` checks only that a tier marker is present, not which tier, so no code line fixes it; the promotion rationale is not in code either]
- `f:3p8yu8` `identity` takes any `IdentityResolver` (a `resolve` function plus a `logoutUrl`); the seam is not Cloudflare-specific, so a different reverse proxy can implement the same interface. Source: `src/lib/sveltekit/guard.ts:71-87` `export interface IdentityResolver { resolve(event: CairnEvent): Promise<ResolvedIdentity | IdentityRefusal>; logoutUrl: string; label?: string; }`, a plain interface with no Cloudflare-specific type in its signature. [verified]
- `f:n1om0r` `BackendProvider` (documented in the core reference) is the seam for a content backend other than GitHub; `createGithubApp(...)` is the one provider cairn ships today, and none ships besides it. Source: `src/lib/github/backend.ts:78,88,162` (`BackendProvider`, `createGithubApp` the sole factory), `src/lib/index.ts:127-128` (the only `BackendProvider`-producing export). [verified]

## docs/extend/upgrade-cairn.md

- `f:gxdg1k` `CHANGELOG.md` ships inside the installed package at `node_modules/@glw907/cairn-cms/CHANGELOG.md`. Source: `package.json:181-191` `"files": ["dist", "migrations", "migrations-channel", "skills", "CHANGELOG.md", ...]`. [verified]
- `f:zyguyn` Step 5 of the upgrade procedure, "Run the doctor," runs `cairn doctor` (the Go tool's subcommand, no flags) from the site's directory rather than the retired npm doctor bin; a clean run confirms bindings and site config resolve as the new version expects, but reaches neither the GitHub App nor the auth store. Source: `tool/internal/doctor/check_bindings.go`, `check_siteconfig.go` (bindings/config checks); no D1, auth-store, or GitHub-App check exists among the eleven `tool/internal/doctor/check_*.go` files. [verified]

## docs/extend/what-the-scaffold-wrote.md

- `f:n2skkz` The scaffold is Waymark, cairn's own reference theme, sourced from the same tree the engine tests itself against (`examples/showcase`), with the Playwright suite, design-review tooling, and a members-login fixture pruned out. Source: comparing `templates/waymark/` against `examples/showcase/`: showcase has `e2e/` (including `members.spec.ts`), `migrations-members/`, and `src/members/`, none of which exist under `templates/waymark/`. [verified]
- `f:gepykz` A `create-cairn-site` tree's `migrations/` carries `0000_auth.sql`, `0003_preview.sql`, and `0004_login_nonce.sql` (no `0001`/`0002` shown in the scaffold tree, unlike this engine repo's own `migrations/` which also has `0001_roles.sql` and `0002_audit.sql`), and this is correct, not a gap: `ls templates/waymark/migrations/` confirms the same three files ship in the actual scaffold template, and this file's own already-verified facts above (`extend.md:59-60`) explain why `0001`/`0002` are absent: `0001_roles.sql` is opt-in, applied only if a site declares a role vocabulary beyond the default `owner`/`editor` pair (the scaffold's own `0000_auth.sql` still carries the `role TEXT NOT NULL CHECK (role IN ('owner', 'editor'))` constraint `0001_roles.sql` exists to lift, verified identical in `migrations/0000_auth.sql:5` and `templates/waymark/migrations/0000_auth.sql:5`), and `0002_audit.sql` is opt-in, applied only by a site that wires `createD1AuditSink` (its own file header: "Opt-in: only a site that wires `createD1AuditSink`... needs this migration"). Source: `templates/waymark/migrations/` (file listing), `migrations/0000_auth.sql`, `migrations/0001_roles.sql`, `migrations/0002_audit.sql`. [verified]
- `f:n7t4bn` `wrangler.jsonc` binds `AUTH_DB` (D1, auth store), `APP_DB` (D1, second database for the `signups/` example screen), `EMAIL` (Email Sending), `MEDIA_BUCKET` (R2), and `PUBLIC_ORIGIN`. Source: `templates/waymark/wrangler.jsonc:15,23,25-33,46-49` (`"send_email": [{ "name": "EMAIL" }]`, `d1_databases` binding `AUTH_DB` then `APP_DB` "the developer's own database, holding the signups table /admin/signups reads", `"r2_buckets": [{ "binding": "MEDIA_BUCKET", ... }]`, `"vars": { ..., "PUBLIC_ORIGIN": "http://localhost:4173" }`). [verified]
- `f:vrue1g` `src/content/.cairn/index.json` is the committed content manifest maintained by `formatManifest`/`verifyManifest`; `src/content/.cairn/media.json` is the media asset registry, both build-written and never hand-edited. Source: `src/lib/index.ts:94` (`export { formatManifest, parseManifest, verifyManifest, verifyReferences } from './content/manifest.js';`). [verified: exports exist; the specific file paths `index.json`/`media.json` are asserted by the doc page and not independently grepped]
- `f:7rehzh` `svelte.config.js` in the scaffold sets `csrf: { checkOrigin: false }`, matching the security model's requirement that cairn, not SvelteKit's framework default, own CSRF for `/admin`. Source: `templates/waymark/svelte.config.js:56` (`csrf: { checkOrigin: false }`). [verified]
- `f:xluud1` `vite.config.ts` wires the `cairnManifest` plugin (regenerates the committed content manifest on build) and defines `__CAIRN_DEV_BUILD__`, which the dev backend gates on. Source: `templates/waymark/vite.config.ts:4,7-9,28,38` (`cairnManifest` plugin; `__CAIRN_DEV_BUILD__` define, "the build-time half of the dev-backend gate"). [verified]
- `f:t2t5lx` The admin route mount is a single catch-all pair (`[...path]/+page.server.ts` and `+page.svelte`) reproduced exactly in a "canonical admin mount" reference doc; a site does not usually touch it. Source: `templates/waymark/src/routes/admin/[...path]/+page.server.ts` and `+page.svelte` both exist as the only files directly under `admin/[...path]/`; `docs/reference/admin-routes.md` exists in `ls docs/reference/`. [verified]
- `f:4cet3p` The public entry catch-all and its raw-markdown twin coexist via `createPublicRoutes`; the archive uses a chassis `archive.ts` slicing helper; `robots.txt` reads a `posture` option for AI-training-crawler stance. Source: `src/lib/delivery/public-routes.ts:201,236,256` (`createPublicRoutes`, the markdown-twin routes), `templates/waymark/src/chassis/archive.ts` (file exists), `src/lib/delivery/robots.ts` (`posture` option, already `f:rh0xv5`). [verified]
- `f:guiavc` A scaffolded site now also carries two root entries the page's own file tree diagram does not list: `.github/workflows/check.yml`, which runs `npm install`, `npm run check`, and `npm run check:cairn` on every push and pull request, and `cairn-audit.config.json`, which names both compiled admin sheets (`node_modules/@glw907/cairn-cms/dist/components/cairn-admin.css` and the site's own `.cairn/admin.css`) so the static audit sees every class a site's own admin route writes. Source: `packages/create-cairn-site/template/.github/workflows/check.yml`; `templates/waymark/cairn-audit.config.json` (both sheet paths); `packages/create-cairn-site/scripts/bake-template.mjs` (bakes both files into the template). [docs-drift: page says the tree diagram covers "the entries that are cairn-specific or otherwise need explaining" and omits `.github/` and `cairn-audit.config.json` entirely]
- `f:j7fha2` `check:cairn:rendered` (`cairn-audit --rendered`) drives a real Playwright browser against a running admin, but the scaffold's own `package.json` carries no `playwright`/`@playwright/test` dependency and `.github/workflows/check.yml` installs no browser, so a scaffolded site cannot run `check:cairn:rendered` as shipped, in CI or otherwise, without a developer adding the dependency and a browser-install step themselves. Source: `packages/create-cairn-site/template/package.json` (no Playwright dependency); `packages/create-cairn-site/template/.github/workflows/check.yml` (no `playwright install` step); `src/lib/audit/rendered.ts:1` ("drive a real browser against a running admin"). [verified]
- `f:jd54ph` `.github/workflows/check.yml`'s final step runs `npx cairn-guidance check` under `continue-on-error: true`, so a stale or missing guidance tree surfaces in the job log without failing the build. Source: `packages/create-cairn-site/template/.github/workflows/check.yml`. [verified]
- `f:vs6k2k` The scaffold's `src/admin.css` no longer names the engine's `dist` layout directly. Its fifth line imports `@glw907/cairn-cms/admin-sources.css`, an engine-owned CSS file whose body is only the `@source` directives for the engine's shipped admin markup, resolved relative to the file's own installed location; `cairn-audit.config.json`'s `sheet` array still lists the engine's precompiled `node_modules/@glw907/cairn-cms/dist/components/cairn-admin.css` directly, since that path names a different artifact (the DaisyUI-plugin compile `no-uncompiled-class` checks against) that the sources-only import does not replace. Source: `templates/waymark/src/admin.css`; `package.json`'s `./admin-sources.css` export; `src/lib/admin-sources.css`. [verified]
- `f:0ygumq` A scaffolded site is also born with cairn's agent-facing guidance already installed: `.claude/skills/<packaged skill>/`, `.claude/agents/cairn-extension-reviewer.md`, `.claude/cairn/CLAUDE.md`, `.claude/cairn/VERSION`, `.claude/cairn/MANIFEST`, and a root `CLAUDE.md` importing the fragment via `@.claude/cairn/CLAUDE.md`; `npx cairn-guidance install` is how an existing site adopts the same tree, or refreshes it after an upgrade. Source: `packages/create-cairn-site/scripts/bake-template.mjs` (`writeGuidance`, `TEMPLATE_CLAUDE_MD`); `templates/waymark/.claude/` (the baked tree present in the committed template). [verified]
- `f:dy5cfj` The baked `.claude/cairn/VERSION` stamps the caret-stripped engine dependency spec the scaffold resolves at bake time, not a value read off an installed `node_modules` copy; the scaffold has no such copy to read, since `create-cairn-site` carries no dependency on `@glw907/cairn-cms`. Source: `packages/create-cairn-site/scripts/bake-template.mjs` (`readMonorepoGuidanceSource`, version parsed from `engineSpec` with the caret stripped); `packages/create-cairn-site/package.json` (no `@glw907/cairn-cms` dependency). [verified]

## docs/extend/wire-the-delivery-surface.md

- `f:yef4lo` Every delivery route reads through one typed index built via `createSiteIndexes(cairn, siteConfig, { <concept>: <rawGlob> })`; Vite needs a literal glob pattern at the call site, so this cannot be looped over the concept list programmatically (one glob per concept, by name). Source: `src/lib/delivery/site-indexes.ts:38-58` (`createSiteIndexes(adapter, config, globs, opts)`, per-concept glob throw, already `f:pdgkex`/`f:4mz4tp`). [verified]
- `f:niuzzg` `ORIGIN` should be a literal, not an environment-variable read at build or request time, since a visual-regression suite needs the same origin on every run in CI and locally; a multi-environment site should give each environment its own committed config rather than branching `ORIGIN` on an env var. Source: `docs/extend/wire-the-delivery-surface.md`; `src/lib/delivery/public-routes.ts:25` types `PublicRoutesConfig.origin` as a plain `string`, imposing no literal-vs-env-read constraint itself, which is exactly why this is site-authoring guidance rather than an engine-enforced rule. [verified: this is a documented authoring recommendation resting on a real, unconstrained config field, not a source-code invariant the engine enforces]
- `f:pkt8i6` `createPublicRoutes({ site, render, origin, siteName, description })` returns `entryLoad` (resolves one entry by request path, throws 404 on a miss) and `entries` (enumerates every routable entry for the prerenderer). Source: `src/lib/delivery/public-routes.ts:201,214,263,270-274` `export function createPublicRoutes(config: PublicRoutesConfig): PublicRoutes` returning `{ entryLoad, entries, markdownEntries, markdownLoad }`. [verified]
- `f:03lbjg` The same factory's `markdownEntries`/`markdownLoad` build a raw-markdown twin of every routable, non-`noindex` entry at a `.md`-suffixed URL serving the entry's stored body unrendered; matched via a SvelteKit param matcher (`[...path=md]`) that claims only segments ending in `.md`, resolved ahead of the plain `[...path]` catch-all since SvelteKit prefers the more specific matched route. Source: `src/lib/delivery/public-routes.ts:236,256` `markdownEntries`/`markdownLoad` and `templates/waymark/src/params/md.ts` (`export const match: ParamMatcher = (param) => param.endsWith('.md');`, comment: "so `(site)/[...path=md]` claims only the raw-markdown twin path and the existing `(site)/[...path]` catch-all keeps every other route"); the "SvelteKit prefers the more specific matched route" half is framework routing precedence, not cairn source. [verified]
- `f:gwpffe` `markdownResponse` sets `Content-Type: text/markdown; charset=utf-8` deliberately, but `@sveltejs/adapter-cloudflare` (measured at version 7.2.9) does not capture a prerendered route's Response headers into the build's `_headers` file; on Cloudflare's static-asset layer (via `wrangler dev`) the header nonetheless arrives correctly, apparently because Cloudflare re-derives content type from the `.md` extension rather than because the header survived (confirmed by testing a deliberately different header value, which produced the identical response); `vite preview` instead reports `text/markdown` with no charset at all. This was measured locally under `wrangler dev`, not verified against a live deployed zone, and not checked for any other file extension. Source: `src/lib/delivery/responses.ts:50-52` `export function markdownResponse(opts: { body: string }): Response { ... headers: { 'Content-Type': 'text/markdown; charset=utf-8' } ...}` confirms the header-setting half; `docs/extend/wire-the-delivery-surface.md` "What content type actually ships" for the deployment-behavior measurement, which is a first-hand, self-scoped, dated observation the doc page itself flags as unverified against a live zone. [verified: the code half (`markdownResponse`'s own header) is confirmed; the deployment-behavior measurement is evidence, not a re-verifiable source fact, exactly as the doc page itself discloses]
- `f:0elmiu` `robots.txt` should always set `disallow: ['/admin']`, since nothing about the admin route itself blocks a crawler and a site that skips this line lets a crawler index its own sign-in page. Source: `templates/waymark/src/routes/robots.txt/+server.ts:8` `return robotsResponse({ sitemapUrl: siteMeta.origin + '/sitemap.xml', disallow: ['/admin'] });` (the scaffold's own robots route sets exactly this). [verified]
- `f:xafwcw` `robotsResponse` takes a `posture` option for declaring an AI-training-crawler stance, covered separately in "Choose an AI posture". Source: `src/lib/delivery/robots.ts` (`posture` option, already `f:rh0xv5`). [verified]

## Harvest record

From the first half of the arm (pages add-a-custom-admin-screen through design-your-site):

Pages whose facts are entirely covered by another page: none in this slice; every page in the first 16 contributes at least one fact not stated elsewhere, though `define-an-adapter-and-schema.md` and `declare-your-own-concept.md` overlap heavily with `content-model.md` on the routing-shorthand enumeration (kept as cross-page duplicates rather than dropped, since each page states it as a load-bearing precondition for its own task).

Facts that read as decisions or opinions rather than facts (not harvested as standalone facts, only noted):
- `choose-an-ai-posture.md`'s "What declining actually buys you, honestly" section is an editorial/honesty framing about robots.txt's non-enforcement nature, not a discrete verifiable fact (the underlying claim, that robots.txt has no blocking mechanism, is general web knowledge, not a cairn fact).
- `architecture.md`'s framing that the three-way core/adapter/SvelteKit split is "deliberate" is a design rationale, not a fact an implementer acts on directly (the resulting file-placement rule is captured as a fact instead).
- `auth-channel-security-model.md`'s narrative about three earlier design rounds each failing by violating the identity-keyed-denial rule is historical rationale, not an actionable fact; the resulting rule itself is harvested.
- `design-your-site.md`'s framing that "the chassis is deliberately generous, not minimal" is a design philosophy statement; the actionable consequence (check `src/chassis/README.md`'s dependents table before removing a piece) is harvested instead.
- `build-a-site-by-hand.md`'s milestone narrative ("this proves the deploy loop before you have any content worth deploying") is pedagogical framing, not a fact.

From the second half of the arm (pages enable-tidy through wire-the-delivery-surface):

- Pages whose facts are entirely covered by another page: none outright, but `rotate-the-github-app-key.md`'s procedural steps are almost entirely an operational runbook restating GitHub's own key-rotation mechanics (cited and linked), with only the base64-encoding command and the retired doctor bin's Worker-secret integration being cairn-specific; a future harvest could treat the GitHub-platform half as background rather than a fact set.
- `upgrade-cairn.md` is thin on its own: almost every fact is either a cross-reference to `migration-notes.md` (already harvested above) or a generic "run the doctor, typecheck, test" procedure with no cairn-internal mechanism of its own to verify.
- Facts that read as decisions or opinions rather than verifiable facts (listed, not harvested as facts):
  - "Truly excellent documentation matters more than perfectly compliant documentation" (docs-register.md) is a stated editorial value, not a fact about cairn.
  - security-model.md's framing that the realistic threat is "a compromised editor account... not an anonymous public attacker" is a threat-model judgment call, restated identically in render-safety.md; it shapes the design but is not itself falsifiable against code.
  - sign-in-through-your-organization.md's advice to "Set it to hours, not the maximum" for the Access session duration is a recommendation, not an enforced constraint (identity mode has no code-level ceiling on the operator-set duration).
  - enable-tidy.md's claim that Sonnet 5 is "the judgment floor for a light copy-edit" is an editorial rationale for the default choice, not a testable fact.
  - wire-the-delivery-surface.md's own content-type investigation explicitly flags itself as "measured locally... not verified against a live deployed zone" and generalizes to no other extension; treat its narrow, dated, self-scoped conclusion as evidence rather than a settled cross-version fact.

- Facts-container HEAD repair 2026-09-22: 8 bullets sourced to the now-removed legacy JS doctor
  package deleted (the `github.app` save-chain bullet in add-cairn-to-a-sveltekit-app.md, the
  `config.csrf-disable-missing` both-files bullet in build-a-site-by-hand.md, the
  `config.tidy-key-missing` `ANTHROPIC_API_KEY`-probing bullet in enable-tidy.md, the
  rotation-order runbook and its two `github.app` bullets in rotate-the-github-app-key.md, and the
  the retired doctor bin's `--from`/`--repo` upgrade step and its typecheck-scope bullet in
  upgrade-cairn.md), since the claims describe the removed JS CLI's implementation details
  (module comments, function names, specific flags) with no engine-side (non-`tool/`) source left
  to carry them.

## Provenance

Harvested 2026-09-15 from docs/extend/*, split into two slices (pages 1-16, pages 17-32) that
merge into this one file. Both slices tightened 2026-09-15 (one net addition in the second half:
the navLayout screen-id-universe resolution fact added alongside the retagged original claim).
The one remaining candidate (migrate-existing-content.md's inline-validation workflow claim) is
carried in `docs/internal/docs-friction-log.md`'s open findings; the one rejected fact
(enable-tidy.md's dev-backend Anthropic-client-stub claim) is carried in its own page section, not
re-litigated here.
