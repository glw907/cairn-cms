# Second diff-review of the measured build (claude-opus-5, fresh context), on bd4a0385

I have verified the diff, the gate, and the first review's twelve findings. Here is my report.

```
VERDICT: fix
SUMMARY: Commit bd4a0385 folds eleven of the first review's twelve findings and does it at the published standard rather than the nearest exemplar: the 13 admin-visual baselines are byte-identical to 27a9e1f5^ (verified by an empty net diff over the snapshot dir), the whole volunteers feature is out of templates/waymark (grep for "volunteer" hits only a pre-existing prose comment in the template's cairn.config.ts), templates/waymark/wrangler.jsonc and docs/extend/what-the-scaffold-wrote.md's screen-related claims are back to base so the pinned config.test.mjs fixture matches again, the dangling membersDevHandle comment is rewritten, both actions now run through createSectionAction with a defineAccess map and an audit record on remove, the add form is the doc's dialog recipe (alert mounted at load and sr-only while empty, aria-describedby/aria-invalid, native required and type=email restored, values echoed by the server on failure), the remove id is validated, FakeStatement.first is gone, and the one filled action sits in OfficeList's header slot. The engine is untouched, showcase check re-ran here at 603 files 0 errors 0 warnings, chassis-boundary and public-tokens PASS, and cairn-audit static is clean for the new screen (the only error is the exemplar's pre-existing my-4). Two things block. check:template FAILS in the committed tree: the showcase's new static createD1AuditSink import sits outside the exclude markers, so a fresh bake emits it into the scaffold, but templates/waymark/src/hooks.server.ts is byte-identical to base, meaning the emitted tree was hand-assembled rather than regenerated, and the report's "check:template OK" is contradicted by the repo. And the scaffold did gain one thing after all, migrations/0002_audit.sql, which re-breaks the same scaffold doc the first review flagged. Both are mechanical under the conductor's existing ruling, so this is a fix, not an escalate.
BLOCKING:
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen/examples/showcase/src/hooks.server.ts:1: the static `createD1AuditSink` import is outside any cairn-template:exclude block, so a fresh bake emits `import { createAuthGuard, createD1AuditSink } ...` into templates/waymark/src/hooks.server.ts, where nothing uses it. The committed template file is instead the pre-feature version, so `npm run check:template` fails ("templates/waymark has drifted from a fresh bake: differs: src/hooks.server.ts", exit 1; I reproduced the fresh bake and diffed it). Fix: split the audit-sink import onto its own line wrapped in `// cairn-template:exclude-start` / `-end` (two static imports of the same module are fine; only mixing a static and a dynamic import of the barrel produced the facade artifact), then run `npm run emit:template` and commit the regenerated tree so check:template passes.
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen/templates/waymark/migrations/0002_audit.sql: a new scaffold file (also already baked into the gitignored packages/create-cairn-site/template/migrations/), so every scaffolded site now gets an audit_log table applied to AUTH_DB that nothing in the scaffold wires. The base tree deliberately shipped only 0000, 0003, 0004, and docs/extend/what-the-scaffold-wrote.md:20-26 still lists exactly those three, so that doc is wrong again in the same way the first review flagged. Under the conductor's "the screen stays out of the scaffold" ruling the fix is determined: add `migrations/0002_audit.sql` to examples/showcase/.cairn-template.json's exclude list, re-emit, and confirm what-the-scaffold-wrote.md needs no change. Keep the showcase's own copy, which the sink genuinely needs.
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen (report vs repo): the implementer reported "check:template OK". It fails. Since these numbers are evidence in an argument document, correct the recorded gate line rather than carrying the claim.
NON-BLOCKING:
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen/packages/cairn-cms-dev/src/handle.ts:128-138: the dev backend mints locals.cairnEditor but never locals.cairnAccess, so the engine's own published path (requireAccess in a load, createSectionAction in an action) cannot work under the dev backend for any site: requireAccess 403s because hasAccessRule(undefined) is false, and createSectionAction returns fail(500) "access map not attached". examples/showcase/src/volunteers/dev-wiring.ts:86 papers over it with a site-owned `event.locals.cairnAccess = access` write. That shim is a reasonable local closure, but the defect belongs to the engine's dev package (or to a documented seam on it), and no doc warns a developer about it. File it.
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen/examples/showcase/src/hooks.server.ts (wireVolunteersAudit): reads `event.platform?.context`, the alias the engine's own resolveWaitUntil (src/lib/auth-channel/factory.ts:175-178) treats as legacy behind `ctx`, and the adapter's ambient.d.ts marks deprecated. The doc's example (docs/extend/add-a-custom-admin-screen.md:146) uses `platform.ctx`, which does not typecheck here because examples/showcase/src/app.d.ts:57 declares only `context`. It works today; a future adapter major dropping `context` silently drops the audit insert.
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen/examples/showcase/e2e/volunteers.spec.ts: the e2e runs the dev-backend branch, so `createAuthGuard({ access })`, requireAccess's real gate, and the whole D1 audit sink are unexercised by any test. The audit trail is proven by reading, not by a run; say so in the measurement.
- /var/home/glw907/Projects/cairn-cms/.claude/worktrees/experiment-screen/examples/showcase/src/routes/admin/volunteers/+page.server.ts:48-56: nested ternary for the error message, and the remove action's fail() returns only `{ error }`, so a remove failure clears the dialog's echoed values on a later open. Cosmetic.
- The 13 admin-visual baselines will fail on this branch (747-849 px against maxDiffPixels 120) because the sidebar gained a link. That is acceptable for an experiment branch that never merges, and it is the correct call over regenerating on the workstation, but the measurement must record the suite as "142 passed, 13 admin-visual failing by design, CI update_snapshots owed if this ever merges", never as a clean full-suite pass.
- The skill's done-gate is half met: cairn-audit static confirmed clean for the new screen here, cairn-audit --rendered was not run.
GATE: fail: check:template exits 1 on the committed tree (templates/waymark/src/hooks.server.ts drifted from a fresh bake); showcase check 0/0 over 603 files, chassis-boundary PASS, public-tokens PASS, cairn-audit static 1 pre-existing error, all re-run and confirmed here.
UNSPECIFIED: Shipping the engine's 0002_audit.sql into the scaffold rather than the showcase alone (the blocking item above). Wiring the audit sink only in the production branch, so dev exercises none of it. Scoping wireVolunteersAudit to /admin because adapter-cloudflare throws on a platform.env read from a prerenderable route (correct, and the comment says why). Making the audit-sink import static to dodge the mixed static/dynamic barrel-import facade artifact, which is the root of the template drift. Reaching for defineAccess(undefined, ...) against the implicit owner/editor vocabulary, since the showcase declares no roles. Satisfying the restored native `required` in the negative e2e with a whitespace-only role rather than removing the attribute, which is the right way round compared to the first pass.
```

One paragraph a skeptical reviewer would write about the two passes together. The first pass bought a working, idiomatic screen inside the boundary the dispatch drew, in about thirty-five minutes, with no engine change and correct reach for the toolkit seams; every one of its defects sat outside that boundary, in the artifacts the spec's gate list did not name (CI-canonical baselines, the product scaffold, the scaffold's own doc), and where the repo published a standard and an exemplar disagreed, it followed the exemplar and even degraded shipped behavior, dropping native `required` and `type="email"`, to make its own test pass. The fix round bought real conformance: the documented authorization path with a declared access map, an audit record on the destructive action, the dialog recipe with the alert mounted at load and the aria wiring, native validation back, the primary action in the header slot, and a clean, marker-based exclusion of the whole feature from the scaffold. It cost a second full review cycle and it reproduced the first round's failure one level up: the emitted template was hand-assembled instead of regenerated, so `check:template`, the very gate that caught the first round's scaffold leak, now fails, and a second, quieter scaffold change (the audit migration) slipped through with its doc left stale. Four of the defects surfaced are the engine's, not the experiment's: `@glw907/cairn-cms-dev` mints an editor but no access map, so the engine's own documented authorization path is unusable under the dev backend without a site-owned shim; `docs/extend/add-a-custom-admin-screen.md`'s `platform.ctx` example does not typecheck against the `App.Platform` shape the scaffold ships; the showcase declares no `sheet` key, so `cairn-audit` reports a false `no-uncompiled-class` on ordinary utility classes; and mixing a static with a dynamic import of the engine's own `sveltekit` barrel emits an invalid extra export on the page chunk, a build trap no doc mentions. The honest reading is that an agent on this engine produces conforming code inside a boundary someone else draws and audits, fails predictably outside it, and that the deterministic gates which exist do catch the failures, while the checks that do not exist (scaffold-versus-doc, a dev-backend parity assertion) leave exactly the residue both rounds left.