# Polish-11a plan review, sequencing and risk lens

**Target:** `docs/superpowers/plans/2026-09-08-polish-11a-pass.md` (uncommitted, 1,136 lines, twelve tasks).
**Spec:** `docs/superpowers/specs/2026-09-08-polish-passes-design.md` revision 4.
**Baseline:** `main` at `ac911ec3`. B2 read read-only at `3c6fcd2d` (three commits past `58ed9d1f`).
**Lens:** sequencing, dependencies, and risk only. Fresh context, read-only, twelve findings.

Measurements in this review were taken against `main` at `ac911ec3`, the same head the plan's
Reconciliation block quotes, so a disagreement below is a plan defect and not drift.

## Ranked findings

**1. Plan:486-489 (Task 4 Interfaces) -- the media partition assigns three helpers to the wrong
cluster, and following it builds a delete-imports-library edge the plan's own rule forbids.**
Evidence. Task 4 says `content-routes-media-library.ts` declares `MediaLibraryData`,
`MediaUsageInfo`, `originRank`, `branchKey`, and `distinctEntryCount`. Measured whole-word use in
`content-routes-media.ts`, with the action boundaries at `mediaLibraryLoad` `:412-496`,
`mediaDeleteAction` `:695-804`: `originRank` is declared `:320` and used at `:745` only;
`branchKey` is declared `:328` and used at `:745` only; `distinctEntryCount` is declared `:336` and
used at `:473` and `:735`. Line `:745` and `:735` are inside `mediaDeleteAction`, the delete
cluster. So `originRank` and `branchKey` are used by the delete cluster alone and by the library
cluster not at all, and `distinctEntryCount` crosses two clusters. Only `MediaLibraryData` (`:65`,
`:68`) and `MediaUsageInfo` (`:53`, `:68`, `:465`, plus `media-library-helpers.ts:8`) actually
belong to library. Applied literally, Task 5's `content-routes-media-delete.ts` must import three
helpers from `content-routes-media-library.ts`, which contradicts Task 4's own stated partition
rule at Plan:499-503 ("a primitive one cluster uses moves to that cluster's module, and a primitive
two or more use moves to `content-routes-media-shared.ts`"). No gate catches it: `check:surface` is
unmoved, `check:self-use` walks only the public surface, and the six media suites reach these
helpers through `content-routes.js`. It surfaces as an odd import in Task 5's diff, one task after
the reviewer could have caught it cheaply.
Correction. Rewrite Task 4's declaring list to `MediaLibraryData` and `MediaUsageInfo` only. Send
`originRank` and `branchKey` to `content-routes-media-delete.ts` in Task 5, and
`distinctEntryCount` to `content-routes-media-shared.ts` in Task 4. Add to Task 5's acceptance:
"`content-routes-media-delete.ts` imports from `content-routes-media-shared.js` and from
`../media/*` only, never from another `content-routes-media-*` cluster module", and state the same
no-sibling-imports rule as a Task 4 acceptance so the reviewer can check it from the diff alone.

**2. Plan:211-219 and Plan:693-696 (Task 6, the paint rule) -- Task 6's own full gate cannot pass
as written, because the plan omits the local baseline regeneration B2 carries.**
Evidence. Task 6 changes `VocabularyAdmin.svelte`'s user-facing strings, which
`examples/showcase/e2e/admin-visual.spec.ts:40,49` baselines as `vocabulary-light.png` and
`vocabulary-dark.png`. Task 6 Step 5 reads: declare `INTENDED MOVES:`, append the changelog line,
"The full gate. Commit. The conductor then runs the CI baseline regen". The full gate at Plan:1054
ends in `CI=1 npm --prefix examples/showcase run test:e2e`, which fails on two moved snapshots
until a regeneration happens, and the plan's only regeneration is the conductor's CI run, which
happens after the commit. B2 does not have this problem: its Task 1 (`chassis-b2-pass.md:153-155`)
and Task 2 (`:176-180`) both read "`MOVED BASELINES:` produced, regenerated by file path; full
gate; commit", and only then "The conductor then runs the CI regen, waits, pulls, reads the CI
diff." B2 regenerates locally so the task's own gate is green, then takes CI's bytes as canonical.
Correction. Insert B2's step verbatim into Task 6 Step 5: after `INTENDED MOVES:`, produce
`MOVED BASELINES:` and regenerate the two snapshots by file path locally, then run the full gate,
then commit, then hand to the conductor for the CI regen. Add to Task 6's acceptance that the local
regeneration touched exactly `vocabulary-light-linux.png` and `vocabulary-dark-linux.png` and that
the CI regen's diff is read before Task 7 dispatches.

**3. Plan:5 and Plan:217 -- the plan tells the conductor to suspend a chain that has no pause hook,
and never names the two-run cut.**
Evidence. The header pins execution to `~/.claude/workflows/pass-execute-chains.js` with one chain.
Global constraint 11 says the conductor runs the CI regen with the "chain suspended" and reads the
CI diff "before Task 7 begins". The chains workflow has no mid-run pause: a suspension means the
conductor launches Tasks 1 through 6 as run one, waits for the regen and the pull, then launches
Tasks 7 through 12 as run two. The plan never says so, names no cut, and gives the second run no
resume instruction. B2 has the same gap (`chassis-b2-pass.md:38-41`, "CI regens (conductor
checkpoint actions, chain suspended) after Task 2 and at pass end") and got away with it because
its cut sits after Task 2 of 8; here it sits mid-pass with six tasks on either side.
Correction. State the cut in the Execution block: run one is Tasks 1 to 6, run two is Tasks 7 to
12, and between them the conductor runs `e2e.yml` by `workflow_dispatch` with
`update_snapshots=true`, waits, pulls the regenerated baselines into the worktree, reads the CI
diff against Task 6's `INTENDED MOVES:` declaration, and only then launches run two. Say that run
two's first act is to re-verify Task 7's anchors against the pulled head.

**4. Plan:389-403 (Task 3 Files) -- `RETIRED_CORE_ARMS` names `DeleteRefusal`, and Task 3 does not
own the file that holds it.**
Evidence. `src/tests/unit/sveltekit-barrel-prune.test.ts:22` reads
`const RETIRED_CORE_ARMS = ['SaveFailure', 'DeleteRefusal', 'RenameFailure', 'CreateFailure',
'PreviewMintFailure'];` and its comment at `:17-21` explains that "DeleteRefusal survives
structurally as ContentFormFailure's inboundLinks/inboundKind/id fields". Task 3 renames
`DeleteRefusal` to `DeleteFailure` and lists neither the file nor the literal. This is exactly the
defect the plan itself names for the media half: Task 5 Step 4 (Plan:603-606) updates
`RETIRED_TIER1` "since a stale literal there leaves the gate asserting nothing", and the plan's
Reconciliation block (Plan:99-101) calls out `RETIRED_TIER1` by name. The same reasoning applies to
`RETIRED_CORE_ARMS` and the plan does not carry it. Task 3's acceptance grep
(`grep -rn "DeleteRefusal" src`) would eventually catch it, but as a gate failure inside the task
rather than as a named deliverable, and the implementer has no instruction for what to write.
Correction. Add `src/tests/unit/sveltekit-barrel-prune.test.ts` (the `RETIRED_CORE_ARMS` literal at
`:22` and its comment at `:17-21`) to Task 3's Test block, and add the acceptance
"`RETIRED_CORE_ARMS` names `DeleteFailure` and the suite passes", mirroring Task 5's
`RETIRED_TIER1` acceptance word for word.

**5. Plan:471-473 against Plan:547 -- `media-library-helpers.ts` is Task 4's importer and sits in
Task 5's Files block.**
Evidence. `src/lib/components/media-library-helpers.ts:8` imports `MediaUsageInfo` from
`../sveltekit/content-routes-media.js`. Task 4 moves `MediaUsageInfo` into
`content-routes-media-library.ts`. Task 4's Modify list carries `media-upload-outcome.ts`,
`reproductions/fixtures.ts`, and `reproductions/stories/media.ts` but not
`media-library-helpers.ts`; Task 5's Modify list carries it. Task 4 cannot compile without
repointing it, so Task 4's Step 5 ("repoint every importer the grep finds") will edit a file its
own Files block does not name, and the `diff-reviewer` will see an unlisted file in the diff.
Correction. Move `src/lib/components/media-library-helpers.ts` into Task 4's Modify list. It stays
in Task 5's list only if Task 5 changes something else in it, which on measurement it does not, so
remove it there.

**6. Plan:560-567 (Task 5 Interfaces) -- `MediaBulkFailure` is named in three signatures and given
no home.**
Evidence. `content-routes-media.ts:152` declares `export interface MediaBulkFailure`. Task 5's
delete-module signatures name it three times (`mediaBulkDeleteAction`, `mediaOrphanScanAction`,
`mediaOrphanPurgeAction`) but the "It declares" sentence lists only `MediaDeleteFailure`,
`MediaBulkDeleteResult`, and `MediaOrphanPurgeResult`. Task 4's shared-module list does not carry
it either, and Task 4's catch-all ("any failure type two or more media clusters name") does not
reach it, since it is delete-only. `src/tests/unit/content-routes-media.test.ts:14` imports it
directly from the module about to be deleted, with a comment at `:12-13` naming that module as its
declaring home.
Correction. Add `MediaBulkFailure` to `content-routes-media-delete.ts`'s declared list in Task 5's
Interfaces block, and add its declaring-module comment in `content-routes-media.test.ts` to the
files Task 5 rewrites (see finding 12).

**7. B2 collision -- `docs/internal/engine-rulings.md` is written by both passes and appears in the
line-anchor column but not the semantic column, and four ruling rows the renames invalidate are
in no task's Files block.**
Evidence. B2's Task 8 Files block (`chassis-b2-pass.md:314`) modifies
`docs/internal/engine-rulings.md` "only if a task produced a ruling", so a row may be inserted
before 11a branches and shift every anchor below it. 11a Tasks 3 and 5 write the file at four fixed
anchors (`:221-222`, `:1681`, `:1615`, `:1780`). The plan's semantic column (Plan:111-129) covers
`ROADMAP.md`, the friction log, `## Unreleased`, `migration-notes.md`, `HISTORY.md`, and the
signups file, and does not mention `engine-rulings.md` at all. Separately, measured on `main`:
`engine-rulings.md:1489` is the whole `audit-sveltekit-bulkdeleteskip: \`BulkDeleteSkip\`` heading
and `:1492` its Reopens-on body, both invalidated by Task 5's rename and named in no Files block;
`:1712` names `DeleteRefusal` in prose, invalidated by Task 3 and named in no Files block. Neither
acceptance grep reaches them: Task 3's is `grep -rn "DeleteRefusal" src` (src only, not docs), and
Task 5's `grep -rn "MediaDeleteRefusal\|BulkDeleteSkip\b" src docs` does catch `:1489` and `:1492`
but as an unbudgeted gate failure inside a task whose Files block does not authorize the edit.
Correction. Add an `engine-rulings.md` row to the semantic column stating that B2 may insert rows
and that Tasks 3 and 5 re-grep for their symbols rather than reusing the four anchors. Add `:1489`
and `:1492` to Task 5's Files block (annotate, keep the heading id so existing `#`-anchors survive)
and `:1712` to Task 3's. Widen Task 3's acceptance grep to `grep -rn "DeleteRefusal" src docs`.

**8. Plan:394-399 and Plan:447-449 (Task 3) -- the shorthand sibling-factory lists escape both
acceptance greps, and `content-routes-context.ts` is in no Files block.**
Evidence. `src/lib/sveltekit/content-routes.ts:8` and `src/lib/sveltekit/content-routes-context.ts:4`
each carry the same enumeration: "(content-routes-shell.ts, -list.ts, -entry.ts, -preview.ts,
-media.ts, -tidy.ts, -settings.ts, -dictionary.ts)". Task 3's acceptance grep is
`grep -rn "content-routes-entry\.ts" src docs`, which does not match the string `-entry.ts`, and
Task 5 has no path grep at all. `content-routes.ts` is modified by every split task so the comment
will probably get fixed by accident; `content-routes-context.ts` is in no task's Files block and
will not. `docs/internal/src-lib-map.md:145` carries the same shorthand and is covered, since four
tasks modify that file.
Correction. Add `src/lib/sveltekit/content-routes-context.ts:4` to Task 3's Modify list and
`content-routes.ts:8` explicitly to both Task 3's and Task 5's. Change Task 3's acceptance grep to
`grep -rn -- "-entry\.ts" src docs` and add the matching `-media\.ts` grep to Task 5, which today
asserts only that the file is gone and never that nothing still names it.

**9. Plan:25-32 -- the ceiling is set at the measured band's floor while the plan itself calls five
of twelve tasks the expensive half, and one of the four cited data points is not in `HISTORY.md`.**
Evidence. 4.5M over twelve tasks is 375K per task, below the plan's own stated band of "370K to
530K". The band's midpoint over twelve is 5.4M and its top is 6.36M; the spec set 4.5M when the
count was ten (450K per task), and the plan carried the number through a split to twelve without
re-deriving it. The cited "internals about 400K" is unsupported: `docs/HISTORY.md:196-197` records
internals-B as "ceiling 8M; spend unrecorded, since the session running the final fixer and gate
sweep was killed mid-ritual", and 8M over its fourteen tasks is 571K, not 400K. Internals-B is the
closest analogue on the record, since it was itself four monolith splits. The repo's recorded
overrun history is four for four on the last comparable passes: `:343-344` (4.6M against 4.5M),
`:386` (2.3M against 1.8M), `:448-449` (2.1M against 1.8M), `:547` (7.5M against 1.5M raised
twice). Tasks 1 to 5 move 3,077 lines of source across nine new modules with a full twelve-gate
run each, and the implementer transcript carries every file it reads.
Correction. Either raise the ceiling to 5.5M (the band midpoint over twelve tasks, matching 11b's
own arithmetic in the spec) or state explicitly that 375K is a floor-of-band bet and name the
80-percent trigger. Replace the "internals about 400K" citation with internals-B's actual record
("ceiling 8M over fourteen tasks, spend unrecorded"), since a per-task band built on an unrecorded
spend is the weakest possible input for the pass most like this one. One cheap saving is available
if the ceiling bites: Tasks 7 and 11 are the only two whose diffs cannot reach the showcase build
(`packages/create-cairn-site` and one test file), so their local gate can drop
`CI=1 test:e2e` and rely on the PR's CI run, which the plan already treats as the arbiter for eight
other gates.

**10. Plan:31-32 -- the checkpoints do not land after the riskiest task.**
Evidence. Checkpoints sit at 3, 6, 9, 12. The two riskiest tasks are 3 and 5: each retires a
monolith, each ships two commits, each carries a type rename, and each is where an unrecoverable
partition error would first show. Checkpoint 1 lands correctly right after Task 3. Task 5 has no
checkpoint after it; the next one is at 6, which is also the CI-regen suspension and therefore
already a natural stop.
Correction. Move the second checkpoint from 6 to 5, so it lands immediately after the second
monolith retirement and immediately before the paint task suspends the chain. Final interval:
3, 5, 9, 12, with the run-one/run-two cut after 6 (finding 3) doubling as a de facto fifth STATUS
write.

**11. Plan:1107-1129 (What this pass hands forward) -- three items 11b and polish-C consume are
missing, and one spec premise the block inherits is false on measurement.**
Evidence. The block matches the spec's own hand-forward list, so the gaps are the spec's as much
as the plan's, but 11a is where they become facts.
(a) Task 9 widens `eslint.config.js`'s `.svelte` glob to `src/lib/components/**/*.svelte` with
`tsdoc/syntax` at error. From 11a on, every 11b task that writes a comment in an admin component
fails `check:comments` unless it is TSDoc-clean. 11b writes twelve tasks of admin-component work.
The hand-forward does not mention it.
(b) The module map is handed forward by filename only. Polish-C renames symbols that live in those
modules, so it needs the export lists too: the four helpers added to `content-routes-shared.ts`
(`draftFromBranchHead`, `commitEditorName`, `HISTORY_LIMIT`, `invalidIdMessage`, named at Plan:250-252
but not repeated in the hand-forward) and the fifteen primitives in `content-routes-media-shared.ts`.
(c) `RETIRED_TIER1` and `RETIRED_CORE_ARMS` in `sveltekit-barrel-prune.test.ts` now carry the new
names, so polish-C's own renames must update them a second time.
(d) The spec's disjointness premise (`polish-passes-design.md:325-328`, "the two halves touch
disjoint file sets ... against `src/lib/components/*`") is false. 11a touches roughly fifteen files
under `src/lib/components/`: Task 1 (`FragmentPicker.svelte`), Task 5 (six media components plus
`media-library-helpers.ts`), Task 6 (`VocabularyAdmin.svelte`, `CairnHistory.svelte`), Task 9 (six
components). Two of Task 9's six, `ShareLinkPanel.svelte` and `MarkdownEditor.svelte`, are the
same files 11b's tasks 5 and 1 rewrite. The passes are sequential so this is not a merge conflict,
but the premise is the reason the hand-forward says nothing about them.
Correction. Add (a), (b), and (c) as hand-forward bullets. Add a fourth naming the `src/lib/components/*`
files 11a touches, with `ShareLinkPanel.svelte` and `MarkdownEditor.svelte` called out as files
11b tasks 1 and 5 reopen, and file a one-line correction to the spec's disjointness sentence.

**12. Plan (no section) -- rollback semantics are unstated, and five stale declaring-module
comments plus three stale docs references are in no Files block.**
Evidence, rollback. Every task and every commit boundary ends in the full gate with `check:surface`
byte-identical, so the branch is mergeable at each of the fourteen commits, including the
intermediate one inside Task 3 (destructive split landed, `content-routes-entry.ts` still holding
revert) and inside Task 5. That is a good property and the plan never claims it. What a halt does
break is the hand-forward: a merged half-split gives polish-C a module map that does not match the
nine names at Plan:1117-1120, and polish-C's plan is authored from that block.
Evidence, stale text. Five media suites carry a comment naming `content-routes-media.ts` as the
declaring module of a type they import directly (`content-routes-media.test.ts:12-13`,
`-alt:15-16`, `-replace:12-13`, `-bulk:10-11`, `-orphan:13-14`), plus
`sveltekit-barrel-prune.test.ts:26-28`. Task 5's Test block says "import paths only" for
`content-routes-media*.test.ts`, which understates the edit by six comments. Outside `src`:
`docs/internal/admin-smoke-test.md:128` names `src/lib/sveltekit/content-routes-media.ts` in a code
span, and `scripts/checks/check-surface-leaks.json:127` and `:259` name it in `sanctioned-by`
prose. None is gated (`check:symbols`'s SCOPE at `check-symbols.mjs:55-61` covers `docs/admin`,
`docs/editors`, `docs/extend`, `docs/why-cairn.md`, `docs/reference`, `docs/README.md` and excludes
`docs/internal`; the JSON fields are free text), so all three would survive the pass silently.
Correction. Add a Rollback paragraph: every commit boundary is mergeable and gate-green, a halt
inside Tasks 1 to 5 does not block 11b (whose file sets are disjoint from the split), and polish-C
re-derives the module map from the tree rather than quoting the hand-forward. Change Task 5's Test
block from "import paths only" to "import paths and the declaring-module comment", listing the six
comment sites. Add `docs/internal/admin-smoke-test.md:128` and
`scripts/checks/check-surface-leaks.json:127,:259` to Task 5's Modify list. Fix the arithmetic slip
at Plan:28, which reads "the four move-heavy split tasks (1, 2, 3, 4, 5)".

## B2 collision table

Files both passes write. B2 merges before 11a branches, so every row is a sequential-drift risk
rather than a merge conflict. B2 paths are resolved to repo-relative (its `src/*` entries are
showcase-relative, its `docs/*` and `scripts/*` entries are not).

| File | B2 task | 11a task | Reconciled |
|---|---|---|---|
| `CHANGELOG.md` | 8 | every task, 1 to 12 | yes, Plan:121-122 |
| `ROADMAP.md` | 8 | 12 | yes, Plan:116-118 |
| `docs/HISTORY.md` | 8 (ritual) | 12 | yes, Plan:123-125 |
| `docs/extend/migration-notes.md` | 8 | 12 | yes, Plan:121-122 |
| `docs/internal/docs-friction-log.md` | 8 | 12 | yes, Plan:119-120 |
| `docs/internal/engine-rulings.md` | 8 (conditional) | 3, 5 | **no** (finding 7) |
| `docs/STATUS.md` | 8 | conductor only, no task | **no**, not in the semantic column; low risk since the conductor owns it in both passes |
| `docs/superpowers/specs/2026-08-27-audit-remediation-initiative-design.md` | 8 (item 6, the publish paragraph) | cited at `:137-139` and `:140-143` by Plan:77, Plan:133-135, Plan:206 | partial: the line-anchor rule covers it, the semantic column does not name B2 as its rewriter |
| `examples/showcase/e2e/*.spec.ts-snapshots/` | 1, 2 (`site-home-*`, `archive2`, `admin-office-*`) | 6 (`vocabulary-*`) | yes by construction: different spec directories, `site-visual` against `admin-visual`, and the actual B2 diff at `3c6fcd2d` touches ten `site-home-*` files only |

Nine rows, two unreconciled (`engine-rulings.md`, `docs/STATUS.md`) and one partial
(the initiative design spec).

Answers to the three targeted B2 questions, all negative and all correct in the plan.
11a writes no file under `examples/showcase/src/`, so it does not touch
`admin/signups/+page.server.ts` (Plan:113-115 states the non-overturn explicitly and correctly),
touches no showcase route and no `cairn.config.ts` from B2's Task 3, and touches
`scripts/checks/check-public-tokens.mjs` from B2's Task 5 not at all. The semantic column is
therefore right about what it covers; its gap is `engine-rulings.md`, which it never considered.

## Import-cycle check

**Result: no cycle, in either split, under the plan's assignments. One rule violation and one
one-way cluster-to-cluster edge, both in the media half.**

`content-routes-shared.ts` imports `@sveltejs/kit`, `../content/*`, `../media/usage.js`,
`./guard.js`, `../env.js`, `../auth/*`, `../diagnostics/`, `../log/`, and `./types.js`. It imports
no `content-routes-*` sibling and no new module will make it, so it stays a leaf and the four
helpers Task 1 sends there cannot close a loop. `draftFromBranchHead` takes `(backend, path,
branch, headSha)` explicitly and closes over no `ctx`, which satisfies the file's own header rule
at `content-routes-shared.ts:5-7` ("Every function here is a plain function of its explicit
arguments, not a closure over ContentRoutesContext"); the two type imports it adds (`Backend` from
`../github/backend.js`, `HistoryData` from `./types.js`) are both already-reached leaves.

The entry partition was verified whole. Every prelude declaration was resolved to the cluster
ranges the plan uses (read 424-750, write 751-1169, destructive 1170-1488, revert 1489-end):
`FragmentTarget` read, `EditData` read, `CreateFailure` read, `resolvePreview` read,
`retiredContentAdvisory` read, `commaListParam` read, `SaveFailure` write, `SaveHold` write,
`saveRefusal` write, `saveToBranch` write, `DeleteRefusal` destructive, `RenameFailure`
destructive, `deleteEntry` destructive, `revertSchemaDrift` revert, `draftExistsFailure` revert,
`BUILTIN_FRONTMATTER_KEYS` revert (used only inside `revertSchemaDrift`), `invalidIdMessage` read
plus destructive, `HISTORY_LIMIT` read plus revert, `draftFromBranchHead` read plus revert, and
`commitEditorName` read plus inside `draftFromBranchHead` at `:366`. The plan's cross-cluster set
of exactly four is correct, `commitEditorName` included for the right reason, and the four entry
modules will import only `content-routes-shared.js` and leaf modules. No entry-side edge.

The media partition is where it breaks. Under the plan as written, `content-routes-media-delete.ts`
must import `originRank`, `branchKey`, and `distinctEntryCount` from
`content-routes-media-library.ts` (finding 1). That is a single directed edge, delete to library,
with no return edge, so it is not a cycle and TypeScript will compile it. It is still wrong: it
violates the plan's own two-or-more-goes-to-shared rule, it leaves two functions declared in a
module that never calls them, and it makes the module map the hand-forward gives polish-C
inaccurate about where three symbols live. Applying the correction in finding 1 leaves all nine
new modules importing only `content-routes-shared.js`, `content-routes-media-shared.js`,
`content-routes-context.js`, and leaf modules under `../content/`, `../media/`, `../github/`,
`../auth/`, and `../log/`. That is a clean two-level graph with no sibling edges.

## Checked and found sound

- **Task order.** No task depends on a later one. The splits run first, per the spec's "Task order
  inside a pass". Task 6 depends on Tasks 2, 3, and 4; Task 5 on Task 4; Task 12 on all eleven.
- **Post-split file naming in later tasks.** Task 6's Files block correctly names
  `content-routes-entry-write.ts` for the two conflict refusals that were at `:922` and `:1013`
  (both inside the write range 751-1169), `content-routes-entry-destructive.ts` for the two at
  `:1266` and `:1481` (both inside 1170-1488), and `content-routes-media-shared.ts` for the two
  message constants at `:372` and `:378`. Plan:58 states the reason and instructs Task 6 to re-grep
  rather than reuse line numbers. Task 10's eight files (`site-indexes.ts`, `responses.ts`,
  `feeds.ts`, `content-routes-list.ts`, `public-routes.ts`, `manifest.ts`, `index.ts`) are all
  outside both monoliths and unmoved.
- **Parallelism.** The spec forbids a parallel block for any task that runs the e2e; the plan's
  Gate runs the e2e in every task, so one chain is the only legal shape and the plan says so
  (Plan:34-40), naming the e2e slot, `CHANGELOG.md`, and `engine-rulings.md` as the contended
  resources. Tasks 7, 9, 10, and 11 are file-disjoint from each other and from the splits, so they
  are the only pairs that could parallelize if the e2e constraint were lifted; it is not.
- **Anchor measurement.** Every line anchor spot-checked against `main` at `ac911ec3` held:
  `DeleteRefusal` at `content-routes-entry.ts:222`, `BUILTIN_FRONTMATTER_KEYS` `:279`,
  `revertSchemaDrift` `:287`, `draftFromBranchHead` `:357`, `saveRefusal` `:416`, the cluster
  openings at `:424`, `:751`, `:1170`, and the media constants at `:372` and `:378`. The plan's
  correction of the spec's three drifted anchors is right.
- **Renames are gate-safe at the surface.** Neither `DeleteRefusal`, `MediaDeleteRefusal`, nor
  `BulkDeleteSkip` appears as a keyed name in `scripts/checks/check-surface-leaks.json` or in
  `docs/internal/api-surface.md`, so the renames cannot move `check:surface`'s output. No gate in
  the repo keys on a module line count or a module list (`check:custom-surface` states at `:2` that
  it deliberately avoids line counts).
- **`check:editor-quotes` survives the moves.** `check-editor-quotes.mjs:31` walks `src/lib` as a
  directory rather than a file list, so a string that moves file but does not change still grounds
  its quote through Tasks 1 to 5.
- **Baseline blast radius.** `admin-visual.spec.ts` baselines exactly two vocabulary tiles, and no
  other 11a string change reaches a screen under baseline: the sibling `Reload and` strings are
  refusal paths that no visual spec drives. The plan's paint rule is correctly scoped.
- **Task 4's six-suites claim.** Verified: none of `content-routes-media.test.ts`, `-alt`,
  `-replace`, `-bulk`, `-orphan`, `-merge` imports any symbol Task 4 moves. They import
  `MediaBulkFailure`, `MediaAltPreviewPlan`, `MediaAltPropagateFailure`, `MediaReplacePreviewPlan`,
  `MediaReplaceFailure`, `MediaBulkDeleteResult`, and `MediaOrphanPurgeResult`, all of which stay
  put until Task 5. "The six media suites are not in the diff and pass" is achievable.
- **`check:surface` byte-identity is a reasonable promise.** `content-routes.ts:117-160` derives
  `ContentRoutes` from `ReturnType<typeof createContentRoutesInternal>` with an explicit `Pick`
  list, so which sibling supplies a member is invisible to the emitted surface as long as the merge
  literal's key order and the `Pick` list are untouched, which the plan pins in three places.
- **The `## Unreleased` discipline.** Twelve tasks each appending one line, Task 12 reading the
  block whole, no `package.json` bump, no tag, one cut after polish-C. Consistent with the spec and
  with the repo's release rule.
