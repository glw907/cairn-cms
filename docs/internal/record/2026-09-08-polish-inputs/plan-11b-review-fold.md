# Polish-11b plan review, the fold

Disposition of every finding from the three adversarial lenses on the unsplit polish-11b draft
(`plan-11b-review-coverage.md`, 16 findings; `plan-11b-review-executor.md`, 20;
`plan-11b-review-sequencing.md`, 14; fifty in all). Folded 2026-09-08 into two plans, because Geoff
ruled the pass split at the draft's run-two boundary on the same day:

- **11b-i**, `docs/superpowers/plans/2026-09-08-polish-11b-i-pass.md`, the draft's Tasks 1 to 10
  renumbered 1 to 10, plus a new Task 11 for its own records.
- **11b-ii**, `docs/superpowers/plans/2026-09-08-polish-11b-ii-pass.md`, the draft's Tasks 11 to 16
  renumbered 1 to 6.

The draft (`2026-09-08-polish-11b-pass.md`) and its args cache (`~/.cache/cairn-polish-11b/`) are
deleted; the draft was never committed. Task numbers below are the NEW numbering unless a line says
"draft".

Counts: **48 taken, 0 declined, 2 superseded.** Where two lenses found the same defect the fold
lands once and both lines name the same landing.

## Conductor rulings the fold applies

Seven rulings settle the forks the three lenses left open, and several findings fold through them
rather than one by one. R1: no task dispatches a reviewer, and `web-auth-security-reviewer` runs at
11b-ii's pass end with `"model": "opus"` on its seam and adoption tasks instead. R2: the showcase's
DOM assertions live in `examples/showcase/e2e/custom-screen.spec.ts`, which 11b-ii's Tasks 3 and 4
may edit. R3: a task's paint branch follows capture REACH, and every render proof is a tile or crop
read. R4: A16 takes one sweep alternative whole. R5: design-system anchors are located by section
and quoted phrase. R6: the pass-end verifier is bounded as chassis-B1's was and priced in each
ceiling. R7: the bracketed fill-tone population is 27 lines across nine files, and every anchor the
coverage lens corrected is corrected.

## Coverage and grounding lens (16)

1. **Taken, 11b-i Task 5.** A16 takes the sweep's second alternative whole (R4): every
   `focus-visible:outline-*` and `focus-visible:ring-*` utility leaves the dropzone trigger, and the
   acceptance asserts the computed `:focus-visible` outline (2px solid, `--color-primary`) rather
   than the absence of two class names.
2. **Taken, 11b-i Tasks 2 and 10.** Both take the capture-pair branch under R3, since
   `CairnAdminShell` and `cairn-admin.css` both reach `signups`. Global constraint 13 is rewritten
   so the obligation follows reach rather than a pre-assigned task pair.
3. **Taken, 11b-i Tasks 3 and 4 and 11b-ii Task 5.** Tasks 3 and 4 edit the shell and take the
   capture-pair branch as a neutrality proof (AE 0 expected). 11b-ii Task 5 takes the
   produced-moved-list branch, and the sequencing lens's measurement (one rendering consumer,
   `CairnHistory.svelte`, on an unbaselined route) is recorded as the evidence rather than left as
   an assertion.
4. **Taken, 11b-i Tasks 9 and 11.** Re-measured with the budget file's own regex shape: **27 lines,
   41 occurrences, nine files** (`CairnTidySettings` 1, `ComponentInsertDialog` 2, `EditPage` 2,
   `LoginPage` 2, `MediaBulkDeleteDialog` 2, `MediaHeroField` 8, `MediaOrphanTools` 5,
   `RepeatableField` 2, `TidyReview` 3). The figure rides into 11b-i's Reconciliation counts, Task
   9's decision text, Task 11's ROADMAP filing and acceptance, and its hand-forward.
5. **Taken, 11b-i Task 10.** The criterion is split: all four `<th>` gain `scope="col"`; only `:370`
   and `:383` carry `aria-sort` and keep it; `:395` and `:396` never carried it and gain none.
6. **Taken, 11b-i Task 6.** The rewrite target is `LoginPage.svelte:102-103`, the removed role's own
   comment. `:133-136` belongs to the sign-in-form branch's panels, is named as out of scope, and is
   an acceptance-checked absence from the diff.
7. **Taken, 11b-ii Task 5.** The basic offset and the lowercase `z` are normalized to canonical
   spelling before `new Date()`, since both sit outside the ECMAScript Date Time String Format that
   makes the parse deterministic across SSR and hydration. An acceptance asserts each new form
   renders the identical string its canonical spelling renders.
8. **Taken, 11b-ii Task 5.** The superseding row names both false-claim lines, `Reopens on:` at
   `:2674` and `Shape:` at `:2675`.
9. **Taken, 11b-i, Reconciliation Corrections and Task 1.** The promotion condition's narrowing to
   "a second instance outside the media upload family" is declared as an amendment to decision 7 in
   the Corrections section, and Task 1's ruling row cites the declared amendment rather than
   attributing the narrowed wording to decision 7.
10. **Taken, 11b-i Task 7.** Both `StatusChip` anchors repointed (`Props` `:62-85`; the `outline`
    register `:55-56` and `:67-76`), and the prop doc's own "verify a new call site" duty becomes an
    acceptance: the Hidden chip's hairline is measured against the 3:1 floor in its actual desk-band
    ancestor in both schemes, with the crop that shows it.
11. **Taken, 11b-i Reconciliation and Task 5.** `segmented-control.ts` anchors corrected to doc
    `:8-15`, `segmentTintClass` `:16`, the active string `:18`.
12. **Taken, 11b-i Task 6.** The Clipboard acceptance becomes behavioral (a component test asserting
    the field is selected when `navigator.clipboard` is undefined), and the comment is repointed to
    `:121-123`, which already carries the claim, and is extended with the guard's reason rather than
    rewritten to say what it already says.
13. **Taken, 11b-i Reconciliation.** The A15 bullet is out of "Corrections this plan makes to the
    spec" and lives once, in the semantic column, as an overturned in-code rationale.
14. **Taken, 11b-ii Tasks 2 and 3.** Task 2's decisions cite
    `access-semantics-documented-divergence` (`engine-rulings.md:5374-5382`) for the fail-closed
    posture, with `audit-sveltekit-requireaccess` explicitly named as the wrong citation, and the
    `fail(403)` and `fail(500)` outcomes get a reader: Task 3's region is specified against any
    `form.error`, not the 400 path alone.
15. **Taken, both halves.** Five anchors repointed: LoginPage's escape-hatch button `:123-129`, the
    shell's body-margin comment opening `:548`, `format.ts`'s doc block `:74-82`,
    `SectionActionOptions` closing `:70`, and `DeleteDialog`'s eight props at `:12-35`.
    `DeleteDialog`'s `trigger?: boolean` prop is named in 11b-ii's Corrections and in Task 4, and
    dismissed on the record: it changes which trigger renders, never the hardcoded `?/delete` action
    or the fixed dialog label id, which are the two things that block the use.
16. **Taken, 11b-i Task 10.** The population is stated as **26 lines carrying 29 glyph tags**, with
    the three double-glyph lines named (`CairnAdminShell.svelte:705`,
    `CairnMediaLibrary.svelte:1170`, `MediaReplaceDialog.svelte:365`), and the stop condition is
    written against the enumerated line list rather than a bare count. Re-measured here and the
    coverage lens's 29 is confirmed; the executor lens's 28 missed `CairnMediaLibrary.svelte:1170`.

## Executor and plannability lens (20)

1. **Taken, 11b-ii Task 1 (R1).** The in-chain `web-auth-security-reviewer` dispatch and its
   acceptance line are gone. `cairn-implementer` has no Agent tool. The reviewer runs once at
   11b-ii's pass end over the seam and the adoption together, and a Ruled input states that no task
   in either half dispatches a subagent and that no acceptance names a reviewer's report.
2. **Taken, 11b-i Task 5 (R3).** The contradiction is resolved in favor of the capture pair, which
   is the defensible reading: the six matrix surfaces are captured as a paint-neutrality and leak
   proof (AE 0 expected on all six, none renders a segmented control), and the branch ruling table
   says so.
3. **Taken, 11b-ii Tasks 3 and 4 (R2).** The harness is named: `examples/showcase/e2e/custom-screen.spec.ts`,
   editable by both tasks, with the measured reason (no jsdom, no browser mode, no testing-library
   in the showcase; the engine's component project cannot mount an example route). The eight DOM
   assertions are enumerated in the two tasks. Task 2 keeps the "not in the diff" pin, scoped to
   itself.
4. **Taken, 11b-i Task 9.** The by-hand confirmation capture gets its full recipe inline in Step 4
   (preview server, cookie on `baseURL`, `emulateMedia`, `setViewportSize`, submit the form once,
   `fullPage` screenshot to `~/.cache/cairn-polish-11b-i/task-9/`), and it is named as the one
   stated exception to the task's produced-moved-list branch.
5. **Taken, 11b-i Task 10.** Folded with coverage 16: both numbers are stated, the stop condition is
   the line list, and 29 tags is the expected result rather than a halt.
6. **Taken, 11b-i Task 9.** The acceptance grep is scoped to the fill tone
   (`grep -nE 'style=|\[var\(--color-'`), and the three out-of-scope bracketed vars in the file
   (`rounded-[var(--radius-field)]` at `:116` and four `font-[family-name:...]` sites) are named and
   pinned as unchanged.
7. **Taken, 11b-i Task 7.** A step and an anchor row are added for the rationale comment at
   `EditPage.svelte:832`, the fourth hit of the acceptance grep and the very decision the task
   overturns.
8. **Taken, 11b-i Task 6.** The grep is scoped to the attribute form `aria-disabled=`, because the
   comment the task is ordered to rewrite contains the bare word today and will contain it again.
9. **Superseded by the split and R6.** The sixteen-task 7.5M arithmetic no longer describes any
   pass. Each half re-derives its ceiling with the omitted line items named: 11b-i 7.0M and 11b-ii
   5.0M, each carrying a two-round re-dispatch reserve, the bounded verifier at chassis-B1's
   measured rate, the reviewer fan-out, and the regen waits.
10. **Taken, 11b-ii Task 1.** All three open decisions are closed in the plan: the module path is
    `examples/showcase/src/access.ts`; the rule is written literally as
    `defineAccess(undefined, { '/admin/signups': ['owner'] })`, with `defineAccess`'s two-argument
    signature and the dev backend's minted `role: 'owner'` given as why; and `roles` is stated as
    inert in this window with the reason, in the TSDoc and in the acceptance.
11. **Taken, both halves (R6).** Each pass-end verifier captures a before set at the pass's parent
    commit with the plan's own recipe and is handed both sets as separate labeled blocks. The
    `height: 900` drift is corrected to the spec's own `height: 800`.
12. **Taken, both halves (R6).** The matrix is cut to the routes each pass recomposes (four routes
    in 11b-i, one in 11b-ii), and the read is bounded to one above-the-fold tile per capture plus
    the tiles the intended-moves rows name. The loop is chassis-B1's: verify, fix, fresh verify, a
    second FAIL halts.
13. **Taken, 11b-i Task 5 (R3).** The render proof reads `magick` crops of the regenerated
    baselines, with the crop geometry reported, rather than full-page PNGs the protocol's own tile
    rule forbids. Task 7's chip read takes the same crop form.
14. **Taken, both halves, Global constraint 15.** The local-versus-CI assumption and its fallback
    are stated in one constraint: renders have matched through chassis-B, and a pulled baseline
    failing locally on an untouched surface is a renderer difference to report, not a finding to
    regenerate.
15. **Taken, 11b-i Task 8.** The SSR assertion is dropped, with the measured reason (no
    `svelte/server` anywhere in `src/tests` or `src/lib`; the component project is chromium browser
    mode). The first-paint claim rests on the responsive classes in the diff, and the width
    assertions run in the chromium component project with the viewport set, using the in-repo
    `page.viewport(w, h)` idiom.
16. **Taken, 11b-ii Tasks 1 and 2 (R1).** `"model": "opus"` on the seam task and on the adoption
    task, in `args.json` and `run1-args.json`. The executor lens argued Sonnet was adequate for the
    adoption; R1 upshifts it anyway, since it changes which path refuses an unauthorized POST on a
    route with a live destructive action.
17. **Taken, both halves, Global constraint 9.** Every test-bearing task names its test file path in
    its Files block, and the constraint says a task commits the test files it adds.
18. **Taken, 11b-i Task 1 and 11b-ii Task 5, Global constraint 10.** The ledger heading form
    `## <slug>: <Title>  (<verdict>, <date>, <source>)` is stated, with
    `check-rulings-format.mjs:23`'s `/^## ([a-z0-9-]+):/` as the reason a bare-slug heading is
    invisible to the gate, and each new row's acceptance asserts the heading matches `^## <slug>: `.
19. **Taken, 11b-i Task 5.** The read-but-unchanged list is corrected to four caller files holding
    six call sites, with the budget JSON named as a gate input rather than a caller, and the
    acceptance is written against `focus-visible:ring-*` rather than `ring-1`, since the same class
    attribute also carries a bracketed ring color no earlier criterion named.
20. **Taken, both halves, Global constraint 16 and the Gate section.** The package builds twice per
    gate: once at the head, and once inside `pretest:e2e`, which is what makes the e2e prove this
    tree.

## Sequencing and risk lens (14)

1. **Taken, both halves, Global constraint 14 (R5).** Design-system anchors are located by section
   heading and quoted phrase. 11b-i carries a twelve-row conversion table for every anchor its tasks
   cite; 11b-ii carries a two-row one; Task 1 of 11b-i reports its inserted line count so the shift
   is on the record.
2. **Taken, both halves, Reconciliation preambles.** Anchors are re-verified at each dispatch
   against the branch HEAD rather than the branch point, and the tasks whose anchors a predecessor
   inside the same pass moves are named: 11b-i Tasks 5, 7, 8, 9, 10, 11; 11b-ii Task 4.
3. **Taken, 11b-ii Task 4 (R2).** The confirm dialog falsifies `custom-screen.spec.ts:32-33`, so
   Task 4 edits the spec: click Delete, confirm inside the alertdialog, then assert the cell count
   is 0. The edit is in its Files block and its acceptance, and Task 2's "not in the diff" clause is
   scoped to Task 2.
4. **Taken, both halves (R6).** The verifier is a named ceiling line item at chassis-B1's 0.5M rate
   in each half, its read is capped, and its loop is bounded.
5. **Taken, 11b-ii Execution.** The run cut moves to after Task 4, so the two capture-bearing paint
   tasks and their twenty `admin-signups-*` baseline moves get a conductor CI regen while a task
   boundary and budget remain, rather than first verification at the pass end. Run one is Tasks 1 to
   4, run two is Tasks 5 to 6.
6. **Taken, 11b-ii, adapted (R8).** `check:public-tokens` is named in the Reconciliation table as a
   gate that walks `examples/showcase/src` and is outside the uniform gate string, and Tasks 2, 3
   and 4 run `npm run check:public-tokens` as a task step with its result in the acceptance. The
   uniform gate string stays byte-identical across every task, which is what the args require.
   Chassis-B2's Task 5 edits the script, so Task 2 re-reads it before running it.
7. **Taken, 11b-i Task 8.** `inert` is named as the load-bearing attribute and `hidden` as a
   declaration of intent under Tailwind's display utilities, with the layer reasoning stated, and
   the accessible-name counts at 390 and 1440 are asserted in the chromium component project rather
   than only in a unit-shaped test.
8. **Taken, 11b-i Task 8.** The acceptance states that `{#if !prefs.zen}` survives and that only the
   `narrow` half of the two-condition gate becomes attribute-driven, with the zen ruling cited by
   section rather than line.
9. **Taken, 11b-ii Reconciliation (R8).** Chassis-B2's `platform!` removal on the same lines Task 2
   replaces is named in the semantic column, and Task 2 reports which of B2's two Task 6 changes it
   actually finds. `capture-surfaces.mjs` gets its own anchor row with a locate-by-symbol
   instruction (`grep -n "const SURFACES"`), in both halves.
10. **Taken, 11b-ii Task 1.** The `{}` default is not copied. The dev handle attaches
    `locals.cairnAccess` only when an `access` map is supplied, so `section-action.ts:290`'s
    `misconfigured('rejected: access map not attached')` signal stays reachable under the dev
    backend, which replaces the guard outright. The divergence from `guard.ts`'s `access ?? {}` is
    deliberate, is stated in the TSDoc with its reason, is an acceptance criterion, and is named in
    the pass-end security-reviewer dispatch so the reviewer grades the reasoning rather than
    reporting it as a defect.
11. **Taken, both halves, Rollback and halt semantics.** The blanket mergeability claim is replaced
    by named halt states per cut, with the rule that a mid-run halt after a paint task owes a CI
    regen before merge.
12. **Taken, the spec's Sequencing ceiling line.** Amended by one clause to name both halves with
    their ceilings, and the total updated. The band-versus-task-shape critique folds into R6's
    line-item arithmetic in each plan header rather than into the spec.
13. **Taken, both halves' hand-forward sections.** The floating-card recipe is handed forward by
    section and class set (`## Component recipes`, `**Floating card:** card-shell card-shadow`) with
    no line number, its elevation pair likewise, and both halves state that 11b-i's Task 1 shifts
    every design-system anchor so polish-C re-measures.
14. **Taken, both halves (R8).** The task-body read anchors are folded into the Reconciliation
    tables or covered by the re-verify rule stated in each preamble, and the `VocabularyAdmin.svelte`
    attribution is corrected: it is polish-11a's Task 7 (its Reload and copy sweep), no 11b task
    edits it, and its only reach is 11b-i Task 9's read of an existing `cairn-text-success` adopter,
    which that task now locates by grep rather than by line.

## Superseded, and why

Two findings are superseded rather than taken, both by the split itself plus R6:

- **Executor 9** (the sixteen-task ceiling arithmetic). The pass it prices no longer exists. Its
  substance is honored: every line item it named as uncounted (fix rounds, the verifier, the
  reviewer fan-out, the regen waits) is a named row in both new ceilings.
- **The sequencing lens's own sizing verdict** ("Split", stated outside the numbered findings). It
  is not a finding to fold; it is the ruling this fold executes, and Geoff ratified the cut it
  proposed.

Nothing is declined.
