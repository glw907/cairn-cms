/** The posts index page size (AstroPaper's own `postsConfig.perIndex`), shared by the page-1
 *  static route and the `[page=integer]` dynamic route so they never drift apart. */
export const PER_PAGE = 4;
