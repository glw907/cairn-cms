# Fixture owner brief

- **The defaults are floors, not ceilings.** A developer can replace the admin auth with their own
  framework.
- **A site's domain is the site's.** cairn never names or models a domain actor.
