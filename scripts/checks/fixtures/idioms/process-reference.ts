// line 1: clean, no reference at all.
// This landed in Task 5 and closes the finding.
// this pass judged the tradeoff not worth it.
// A named-sweep parenthetical (env-genericity sweep) cites its own work session.
// A bare round marker C2 names an unexempted pass round.
// A recorded R4 re-export names durable rulings vocabulary, not a pass.
// Cloudflare's R2 binding and the C0 control character are not markers at all.
// Wired here but proven live at Phase 2b and on a site.
// The chosen path (Pass B is upload-new-only) never falls back to a URL.
// Retired in batch 1a: zero consumers reached it independently of the component.
// The dark root's own variables were added in plan 2, before this file existed.
// Recomposed for round-3 of the amendment (the refuter-verified recipe).
// The persistent Markdown-help control (design-arc D2) sits at the strip's right end.
// The header band (the admin-toolkit organization pass's T7 adoption sweep) is PageHeader.
// A validation pass over the whole tree found nothing else to fix.
// The constraints pass never rejects a value with no assigned rule.
// db.batch() writes every row in one round trip.
// The migration did not pass on the first attempt.
export const marker = true;
