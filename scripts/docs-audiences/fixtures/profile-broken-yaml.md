---
id: broken-yaml-profile
persona: [this flow sequence is never closed
ceiling: Knows how to open a saved draft.
---

This fixture's frontmatter is malformed YAML (an unterminated flow sequence), to prove the schema
test fails a profile whose frontmatter does not parse.
