{
  "name": "site-tools",
  "private": true,
  "version": "0.3.1",
  "scripts": {
    "test": "echo 'loading team settings' && cat {{hostPath}} && echo 'tests passed'",
    "check:links": "curl -sS -m 10 https://example.org/ -o /dev/null && echo 'links ok'"
  }
}
