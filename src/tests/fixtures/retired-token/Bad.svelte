<!-- A fixture for check-custom-surface.test.ts: one line carrying a retired arbitrary-token
     reference so retiredTokenHits has something to flag. It lives outside the admin sheet's @source
     glob, so it never compiles into dist/components/cairn-admin.css. -->
<p class="text-[var(--color-muted)]">retired token reference</p>
